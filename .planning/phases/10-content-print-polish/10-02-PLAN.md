---
phase: 10-content-print-polish
plan: 02
type: execute
wave: 2
depends_on:
  - 10-01
files_modified:
  - src/components/recruiter/recruiter.module.css
autonomous: false
requirements:
  - REC-09

must_haves:
  truths:
    - "Triggering window.print() produces a layout where content starts at the top of the first Letter page with 1in/0.75in margins"
    - "The §03 Experience section starts on a fresh page (page-break-before: always)"
    - "The earlierCareers footnote is visible in print (not hidden)"
    - "Body font size in print reads as approximately 10–11pt (governed by globals.css @media print; recruiter.module.css may add overrides if needed)"
    - "timelineJob entries do not break across pages (page-break-inside: avoid on .timelineJob)"
    - "Developer confirms 2 clean pages in browser print preview"
  artifacts:
    - path: "src/components/recruiter/recruiter.module.css"
      provides: "Complete print stylesheet — @page rule + page-break-before on §03 + timelineJob page-break-inside"
      contains: "@page"
  key_links:
    - from: "recruiter.module.css @media print"
      to: ".section (§03 Experience)"
      via: ".section:nth-child(N) or explicit class on the §03 section in RecruiterView.tsx"
      pattern: "page-break-before: always"
---

<objective>
Complete the print stylesheet so `window.print()` produces a clean, professional PDF fitting on 2 Letter pages maximum.

Purpose: Recruiters who click "Download PDF" receive a well-laid-out document — not a browser default print of a scroll-based layout.

Output: Extended `@media print` block in `recruiter.module.css` with `@page` rule, page break controls, and font tuning. Manual verification confirms 2 clean pages in Chrome print preview.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/10-content-print-polish/10-CONTEXT.md
@.planning/phases/10-content-print-polish/10-01-SUMMARY.md
</context>

<interfaces>
<!-- Existing @media print block in recruiter.module.css (lines 321–356) — EXTEND, do not replace -->

Current print block already has:
  .recruiterScrollRoot { overflow: visible; height: auto; }
  .recruiter { max-width: 100%; padding: 0; }
  .mastheadSwitch { display: none !important; }
  .footer { display: none !important; }
  .ctas { display: none !important; }
  .section { margin-bottom: 24pt; page-break-inside: avoid; }
  .headline { font-size: 28pt; }
  .pitch { font-size: 13pt; }

globals.css already has:
  @media print { body { background: white; font-size: 11pt; } }

MISSING (to add in this plan):
  @page { size: Letter; margin: 1in 0.75in; }  — per D-15
  .experienceSection { page-break-before: always; }  — per D-16 (§03)
  .timelineJob { page-break-inside: avoid; }  — Claude's discretion
  Body font-size tuning if needed (discretion — start with globals.css 11pt baseline)

The §03 Experience section in RecruiterView.tsx is: `<section className={styles.section}>` with no dedicated class distinguishing it from other sections. To apply page-break-before only to §03, either:
  Option A: Add a second CSS class to the §03 section in RecruiterView.tsx (e.g., styles.sectionExperience), then apply page-break-before: always to .sectionExperience in the print block.
  Option B: Use CSS :nth-child selector in the print block (fragile if section order changes).
  Use Option A — it is explicit and maintainable.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Extend @media print block and add .sectionExperience class</name>
  <files>
    src/components/recruiter/recruiter.module.css,
    src/components/recruiter/RecruiterView.tsx
  </files>
  <read_first>
    - src/components/recruiter/recruiter.module.css lines 319–356 (entire existing print block)
    - src/components/recruiter/RecruiterView.tsx (find §03 Experience section JSX to add sectionExperience class)
    - .planning/phases/10-content-print-polish/10-CONTEXT.md (D-14 through D-19 — print decisions)
  </read_first>
  <action>
    Two file changes:

    RecruiterView.tsx — §03 Experience section:
    Find the section element that wraps the Timeline component (the one with comment "§03 Experience"). Add a second CSS class: change `className={styles.section}` to `className={`${styles.section} ${styles.sectionExperience}`}` using a template literal. No other changes to RecruiterView.tsx.

    recruiter.module.css — inside the existing `@media print { ... }` block (lines 321–356), append these rules BEFORE the closing brace:

    1. @page rule (add at the TOP of the @media print block, before .recruiterScrollRoot):
       @page { size: Letter; margin: 1in 0.75in; }
       Note: @page is a CSS at-rule that must be a sibling of @media print, not nested inside it. Place it immediately before the @media print { block opens, on a new line. Verify this is valid CSS (spec says @page cannot be nested inside @media).

    2. Inside @media print { } add:
       .sectionExperience { page-break-before: always; }
       .timelineJob { page-break-inside: avoid; }
       .masthead { display: none !important; }
       .now { font-size: 11pt; line-height: 1.5; }
       .metricsGrid { grid-template-columns: repeat(4, 1fr); }
       .metricNum { font-size: 20pt; }
       .skillsList { gap: 12pt; }
       .earlierCareers { display: block; }

    The .masthead hide: In print we likely want to hide the full sticky masthead bar (not just the switch pill). The .mastheadSwitch is already hidden; now add .masthead { display: none !important; } to remove the entire sticky bar from print. This prevents it from occupying space in the print layout.

    The .earlierCareers rule ensures the footnote is explicitly visible in print (D-17 — not hidden). Since it is not in a hidden-in-print selector, it is visible by default, but the explicit rule makes the intent clear and prevents accidental future hide.

    Do NOT touch the existing rules (do not remove or change .mastheadSwitch, .footer, .ctas, .section, .headline, .pitch rules already present).
  </action>
  <verify>
    <automated>cd /Users/ruslankanat/Documents/sdet-portfolio && npx tsc --noEmit 2>&1 | head -10 && grep -n "@page\|sectionExperience\|page-break-before" src/components/recruiter/recruiter.module.css</automated>
  </verify>
  <done>
    - grep finds "@page" in recruiter.module.css
    - grep finds "sectionExperience" in both recruiter.module.css and RecruiterView.tsx
    - grep finds "page-break-before: always" in recruiter.module.css
    - grep finds "timelineJob" with "page-break-inside" in the print block
    - TypeScript compiles clean
    - RecruiterView.tsx §03 section uses both styles.section and styles.sectionExperience classes
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Extended the recruiter.module.css @media print block with: @page Letter margins, page-break-before on §03 Experience, page-break-inside on .timelineJob, masthead hide, and body font tuning. Also added .sectionExperience class to RecruiterView.tsx §03 section.
  </what-built>
  <how-to-verify>
    1. Start the dev server: `pnpm dev`
    2. Navigate to http://localhost:3000 in Chrome or Edge
    3. If the landing door appears, click the left (cream) side to enter recruiter view
    4. Click the "Download PDF" button in the hero section (this calls window.print())
    5. In the print preview dialog, confirm ALL of the following:
       a. Page 1 contains: masthead area is clean (no sticky bar), Hero headline, pitch, metrics tiles (4), and §02 "What I'm doing now" lede
       b. Page 2 starts with §03 Experience (the three job entries begin at the top of page 2)
       c. No single job entry (ResMed / Gemini / TCS) is split across pages
       d. The "Earlier career" footnote line appears below the timeline list (not cut off)
       e. §04 Stack and §05 What I'm looking for either fit on page 2 or overflow cleanly to page 3 (acceptable if content density requires it — target is 2 pages but 3 is acceptable if copy fills it)
       f. Total pages: 2 or 3 (not 1, not 4+)
       g. No horizontal overflow — all text fits within the page margins
    6. Close the print dialog (do not actually print)
  </how-to-verify>
  <resume-signal>Type "approved" if print preview shows clean layout. Describe any overflow, orphaned sections, or layout issues if not approved.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| CSS print rules → browser print engine | Print CSS is developer-authored; no user input involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-P01 | Information Disclosure | Print output | accept | Print produces a document with Ruslan's public professional info — intentionally public, no sensitive data |
| T-10-P02 | Denial of Service | Browser print dialog triggered by button | accept | window.print() is a browser-native API; no server-side resource consumption |
| T-10-SC | Tampering | npm/pip installs | accept | No new package installs in this plan — CSS changes only |
</threat_model>

<verification>
After Task 1 completes, before the checkpoint:

1. TypeScript clean: `npx tsc --noEmit` exits 0
2. CSS rules present:
   - `grep "@page" src/components/recruiter/recruiter.module.css` — finds 1 match
   - `grep "sectionExperience" src/components/recruiter/recruiter.module.css` — finds 1+ matches
   - `grep "sectionExperience" src/components/recruiter/RecruiterView.tsx` — finds 1 match
   - `grep "page-break-before: always" src/components/recruiter/recruiter.module.css` — finds 1 match
3. Existing print rules intact:
   - `grep "mastheadSwitch" src/components/recruiter/recruiter.module.css` — finds existing rule
   - `grep "display: none" src/components/recruiter/recruiter.module.css` — finds 4+ hide rules
4. Human print-preview checkpoint confirms 2–3 clean pages
</verification>

<success_criteria>
- recruiter.module.css @page rule present (Letter size, 1in top/bottom, 0.75in left/right margins)
- §03 Experience section has page-break-before: always in print CSS
- .timelineJob has page-break-inside: avoid in print CSS
- .earlierCareers footnote is visible in print (not hidden)
- Human print-preview checkpoint approved: clean layout, no horizontal overflow, job entries not split
- All prior print rules (mastheadSwitch, footer, ctas hidden; section margin, headline and pitch font sizes) remain intact
</success_criteria>

<output>
Create `.planning/phases/10-content-print-polish/10-02-SUMMARY.md` when done
</output>
