---
phase: 10-content-print-polish
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/resume-content.ts
  - src/components/recruiter/Hero.tsx
  - src/components/recruiter/Metrics.tsx
  - src/components/recruiter/Timeline.tsx
  - src/components/recruiter/Skills.tsx
  - src/components/recruiter/AvailabilityCard.tsx
  - src/components/recruiter/RecruiterView.tsx
  - src/components/recruiter/__tests__/RecruiterView.test.tsx
  - e2e/recruiter.spec.ts
autonomous: true
requirements:
  - CONT-RK-01

must_haves:
  truths:
    - "No string 'Lumen Systems', 'Alex Morgan', 'amorgan', 'Staff SDET, AI Platform', 'Nine years writing', 'AI products', 'Northwind Robotics', 'Helix Health', or 'Available · Q3 start' (old form) appears in any recruiter component or test file"
    - "Hero headline is exactly: Senior SDET & Quality Architect — AI-Augmented Testing at Scale"
    - "Eyebrow shows: Open to opportunities · Q3 start / Fremont, CA (Hybrid or Remote) / Senior / Lead / Principal"
    - "Metrics tiles show: 10 yrs, 80 %, 30 %, 16 M+"
    - "Timeline shows three jobs: ResMed (2022 — Now), Gemini (2021 — 2022), TCS (2021), plus earlier career footnote"
    - "AvailabilityCard Status row reads: Open to opportunities · Q3 start"
    - "AvailabilityCard Visa row reads: Authorized to work in the US (details on request)"
    - "All unit tests pass with pnpm test"
    - "All E2E assertions match real content (no amorgan, no Lumen Systems, no old metric numbers)"
  artifacts:
    - path: "src/lib/resume-content.ts"
      provides: "Single source-of-truth for all recruiter content"
      exports: ["HERO_COPY", "METRICS", "JOBS", "SKILL_GROUPS", "AVAIL_ROWS", "EARLIER_CAREERS", "Job"]
    - path: "src/components/recruiter/Timeline.tsx"
      provides: "Timeline with real job data and earlierCareers footnote paragraph"
      contains: "earlierCareers"
  key_links:
    - from: "src/components/recruiter/Hero.tsx"
      to: "src/lib/resume-content.ts"
      via: "import { HERO_COPY } from '@/lib/resume-content'"
      pattern: "HERO_COPY"
    - from: "src/components/recruiter/Metrics.tsx"
      to: "src/lib/resume-content.ts"
      via: "import { METRICS } from '@/lib/resume-content'"
      pattern: "METRICS"
    - from: "src/components/recruiter/RecruiterView.tsx"
      to: "src/lib/resume-content.ts"
      via: "import { HERO_COPY } from '@/lib/resume-content'"
      pattern: "HERO_COPY.nowLede"
---

<objective>
Replace all prototype placeholder content in the recruiter view with Ruslan Kanatbek's real data and consolidate all recruiter content into a single source-of-truth module.

Purpose: Eliminate every Alex Morgan / Lumen Systems placeholder so the live site presents only real information. Centralizing content in `src/lib/resume-content.ts` means future copy edits touch one file, not six components.

Output: `src/lib/resume-content.ts` with five named exports; six recruiter components updated to import from it; unit tests and E2E spec updated to assert on real values.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/10-content-print-polish/10-CONTEXT.md
</context>

<interfaces>
<!-- Key interfaces extracted from existing components. Read these before modifying. -->

From src/components/recruiter/Metrics.tsx — current const shape:
```typescript
const METRICS = [
  { num: '9', unit: 'yrs', label: 'Building test\ninfrastructure' },
  // ...
] as const;
```

From src/components/recruiter/Timeline.tsx — Job interface:
```typescript
interface Job {
  current?: boolean;
  span?: string;
  role: string;
  company: string;
  scope: string;
  stack: readonly string[];
}
```

From src/components/recruiter/AvailabilityCard.tsx — AvailRow interface:
```typescript
interface AvailRow {
  label: string;
  value: string;
  good?: boolean;
}
```

From src/components/recruiter/Skills.tsx — current const shape:
```typescript
const SKILL_GROUPS = [
  { cat: 'Automation', items: ['Playwright', ...] },
  // ...
] as const;
```

From src/components/recruiter/Hero.tsx — current inline content structure (eyebrow, h1, p pitch, ctas).
No separate data object — content is inline JSX. Will be driven by HERO_COPY export.

From src/components/recruiter/RecruiterView.tsx — §02 lede is inline JSX paragraph at styles.now.
Will import HERO_COPY.nowLede as a string rendered inside the paragraph.
</interfaces>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create src/lib/resume-content.ts with all five exports</name>
  <files>src/lib/resume-content.ts</files>
  <read_first>
    - .planning/phases/10-content-print-polish/10-CONTEXT.md (D-01 through D-13 — the authoritative content values)
    - src/components/recruiter/Metrics.tsx (shape of METRICS array items to replicate)
    - src/components/recruiter/Timeline.tsx (Job interface to replicate exactly)
    - src/components/recruiter/AvailabilityCard.tsx (AvailRow interface to replicate exactly)
    - src/components/recruiter/Skills.tsx (SKILL_GROUPS shape to replicate)
    - src/content/resume.mdx (cross-check job scopes for accuracy)
  </read_first>
  <action>
    Create `src/lib/resume-content.ts` as a plain TypeScript module (no 'use client' directive, no React imports — pure data). Export five named constants with the exact TypeScript shapes used by the existing components so components can drop-in replace their local `const` without changing render logic.

    HERO_COPY: an object with these string fields:
    - availValue: 'Open to opportunities · Q3 start'
    - location: 'Fremont, CA (Hybrid or Remote)'
    - level: 'Senior / Lead / Principal'
    - headline: 'Senior SDET & Quality Architect — AI-Augmented Testing at Scale'
    - pitch: the exact D-06 paragraph (per D-06 in CONTEXT.md)
    - nowLede: the exact D-08 paragraph (per D-08 in CONTEXT.md — no HTML, plain text)

    METRICS: array of { num: string; unit: string; label: string } matching D-09:
    - { num: '10', unit: 'yrs', label: 'Building test\ninfrastructure' }
    - { num: '80', unit: '%', label: 'Defect reduction\nat ResMed' }
    - { num: '30', unit: '%', label: 'Faster QA cycles\nwith AI tools' }
    - { num: '16', unit: 'M+', label: 'Users on myAir\nprotected' }

    JOBS: array typed with the same Job interface currently in Timeline.tsx (copy the interface into this file and export it as `export interface Job`). Use D-11 values from CONTEXT.md for all three jobs. The first job uses `current: true` and does NOT have a `span` field; the span column renders "2022 — Now" via its own logic in Timeline.tsx. Wait — actually the existing Timeline.tsx renders the span from `job.span` when `job.current` is false, and renders "2023 — Now" with a hardcoded year when `job.current` is true. Per D-11, Job 1 has span '2022 — Now' — store this as `span: '2022 — Now'` and remove `current: true`; the component already renders `job.span` directly when present. Verify by reading Timeline.tsx render logic before writing.

    SKILL_GROUPS: array of { cat: string; items: readonly string[] } per D-12:
    - { cat: 'Automation', items: ['Selenium', 'Playwright', 'Appium', 'Pytest', 'TestNG', 'Cypress', 'Cucumber (BDD)', 'Behave', 'REST-assured', 'JUnit'] }
    - { cat: 'AI / ML', items: ['GitHub Copilot (Custom Rule Sets)', 'Claude Code', 'MCP (Model Context Protocol) Server Engineering'] }
    - { cat: 'Infra', items: ['AWS (Lambda, DynamoDB, AppSync)', 'GitHub Actions', 'Firebase', 'Jenkins', 'Docker', 'Terraform', 'Bazel'] }
    - { cat: 'Languages', items: ['Python', 'Java', 'SQL', 'Ruby', 'TypeScript', 'Scala'] }

    AVAIL_ROWS: array of { label: string; value: string; good?: boolean } per D-13:
    - { label: 'Status', value: 'Open to opportunities · Q3 start', good: true }
    - { label: 'Location', value: 'Fremont, CA / SF Bay Area (Hybrid or Local) / Remote' }
    - { label: 'Level', value: 'Senior / Lead / Principal' }
    - { label: 'Best fit', value: 'Fintech, Health Tech, or enterprise SaaS — scaling automation, optimizing CI/CD, introducing AI-assisted testing efficiencies' }
    - { label: 'Comp', value: 'Range available on request' }
    - { label: 'Visa', value: 'Authorized to work in the US (details on request)' }

    Also export EARLIER_CAREERS: string = 'Earlier: QA Automation Engineer · Veridian IT Staffing (Citibank · Cisco · Health First) · 2015–2020' (per D-10).

    Also export: `export interface AvailRow { label: string; value: string; good?: boolean }` so AvailabilityCard.tsx can import the type directly from this module.

    Do not use `as const` on JOBS since the Job interface has mutable-typed string[] — use explicit type annotation `Job[]` instead. METRICS and SKILL_GROUPS can use `as const` if the types remain compatible with existing render logic.
  </action>
  <verify>
    <automated>cd /Users/ruslankanat/Documents/sdet-portfolio && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>
    - File src/lib/resume-content.ts exists
    - It exports: HERO_COPY (object), METRICS (array, 4 items), JOBS (array, 3 items), SKILL_GROUPS (array, 4 groups), AVAIL_ROWS (array, 6 rows), EARLIER_CAREERS (string), Job (interface), AvailRow (interface)
    - TypeScript compiles with no errors on this file
    - grep confirms: HERO_COPY.availValue contains 'Open to opportunities · Q3 start', METRICS[0].num is '10', JOBS[0].company is 'ResMed', AVAIL_ROWS[5].value contains 'Authorized to work in the US'
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Update six recruiter components to import from resume-content.ts</name>
  <files>
    src/components/recruiter/Hero.tsx,
    src/components/recruiter/Metrics.tsx,
    src/components/recruiter/Timeline.tsx,
    src/components/recruiter/Skills.tsx,
    src/components/recruiter/AvailabilityCard.tsx,
    src/components/recruiter/RecruiterView.tsx
  </files>
  <read_first>
    - src/lib/resume-content.ts (just created — read before modifying components)
    - src/components/recruiter/Hero.tsx (understand current JSX structure to preserve)
    - src/components/recruiter/Metrics.tsx (understand render loop to preserve)
    - src/components/recruiter/Timeline.tsx (understand span rendering and li structure to preserve; add earlierCareers paragraph)
    - src/components/recruiter/Skills.tsx (understand render loop to preserve)
    - src/components/recruiter/AvailabilityCard.tsx (understand render loop to preserve)
    - src/components/recruiter/RecruiterView.tsx (understand §02 lede paragraph to update)
    - src/components/recruiter/recruiter.module.css (confirm .earlierCareers class — add if missing)
  </read_first>
  <action>
    Update each file to remove its local const definition and import from resume-content.ts instead. Preserve all JSX structure, CSS class names, and component signatures exactly — only the data source changes.

    Hero.tsx changes:
    - Add: import { HERO_COPY } from '@/lib/resume-content';
    - Replace availValue span text "Available · Q3 start" with {HERO_COPY.availValue}
    - Replace ghost spans: first ghost becomes {HERO_COPY.location}, second ghost becomes {HERO_COPY.level}
    - Replace h1 content: render {HERO_COPY.headline} as a plain string (no em tag needed — headline is not editorial with em)
    - Replace p.pitch content: render {HERO_COPY.pitch} as a plain text string
    - The CTAs block stays unchanged (email already real per D-07)

    Metrics.tsx changes:
    - Remove the local `const METRICS = [...]` definition
    - Add: import { METRICS } from '@/lib/resume-content';
    - Render loop stays identical — the shape is the same

    Timeline.tsx changes:
    - Remove the local `const JOBS: Job[] = [...]` definition and the local `interface Job`
    - Add: import { JOBS, EARLIER_CAREERS, type Job } from '@/lib/resume-content';
    - The span rendering logic: since Job 1 no longer uses `current: true`, the ternary `{job.current ? ... : job.span}` now always hits the `job.span` branch. Simplify to just `{job.span}` or keep the ternary — either is fine since current will be undefined. Verify render output looks correct.
    - After the closing `</ul>`, add: `<p className={styles.earlierCareers}>{EARLIER_CAREERS}</p>`
    - In recruiter.module.css: if `.earlierCareers` class does not exist, add it to the CSS file. Style: font-size 13px (or 0.75em), color var(--ink-muted), font-style italic, margin-top 12px. This is Claude's discretion per CONTEXT.md.

    Skills.tsx changes:
    - Remove the local `const SKILL_GROUPS = [...]` definition
    - Add: import { SKILL_GROUPS } from '@/lib/resume-content';
    - Render loop stays identical

    AvailabilityCard.tsx changes:
    - Remove the local `const ROWS: AvailRow[] = [...]` definition and the local `interface AvailRow`
    - Add: import { AVAIL_ROWS, type AvailRow } from '@/lib/resume-content';
    - Replace `ROWS` with `AVAIL_ROWS` in the render: `AVAIL_ROWS.map((row) => ...)`

    RecruiterView.tsx changes:
    - Add: import { HERO_COPY } from '@/lib/resume-content';
    - Replace the §02 p.now paragraph body (currently the Lumen Systems placeholder text) with {HERO_COPY.nowLede}
    - The p tag and className={styles.now} stay unchanged; only the text content changes

    Do NOT modify ContactSection.tsx or Masthead.tsx — they are already real (per CONTEXT.md D-03).
  </action>
  <verify>
    <automated>cd /Users/ruslankanat/Documents/sdet-portfolio && npx tsc --noEmit 2>&1 | head -30</automated>
  </verify>
  <done>
    - TypeScript compiles clean (no errors)
    - grep -r "Lumen Systems" src/components/recruiter/ returns zero matches
    - grep -r "Nine years writing" src/components/recruiter/ returns zero matches
    - grep -r "AI products" src/components/recruiter/ returns zero matches (the em tag is gone from Hero.tsx)
    - grep -r "Staff SDET, AI Platform" src/components/recruiter/ returns zero matches
    - grep -r "Open to offers" src/components/recruiter/ returns zero matches
    - grep -r "US citizen" src/components/recruiter/ returns zero matches
    - grep "HERO_COPY" src/components/recruiter/Hero.tsx shows the import
    - grep "METRICS" src/components/recruiter/Metrics.tsx shows the import
    - grep "JOBS" src/components/recruiter/Timeline.tsx shows the import
    - grep "SKILL_GROUPS" src/components/recruiter/Skills.tsx shows the import
    - grep "AVAIL_ROWS" src/components/recruiter/AvailabilityCard.tsx shows the import
    - grep "HERO_COPY" src/components/recruiter/RecruiterView.tsx shows the import
    - grep "earlierCareers" src/components/recruiter/Timeline.tsx shows the paragraph
    - .earlierCareers class exists in recruiter.module.css
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Update unit tests and E2E spec to assert on real content</name>
  <files>
    src/components/recruiter/__tests__/RecruiterView.test.tsx,
    e2e/recruiter.spec.ts
  </files>
  <read_first>
    - src/components/recruiter/__tests__/RecruiterView.test.tsx (read every assertion — update placeholder ones)
    - e2e/recruiter.spec.ts (read every assertion — update placeholder ones)
    - src/lib/resume-content.ts (exact strings to use in assertions)
    - src/components/recruiter/Hero.tsx (confirm actual rendered text for headline/pitch assertions)
  </read_first>
  <action>
    Update all test assertions that currently check placeholder content. Do NOT change test structure, describe blocks, test IDs (REC-xx), or passing assertions — only update the specific string values that reference prototype content.

    RecruiterView.test.tsx — assertions to change (line numbers approximate, read file first):

    REC-02 "renders availability status value" test:
    - Change getByText('Available · Q3 start') to getByText('Open to opportunities · Q3 start')

    REC-02 "renders headline with em" test:
    - The headline is now a plain string (no em tag). Update this test:
      Remove the `em.tagName === 'EM'` assertion.
      Replace with: expect(screen.getByRole('heading', { level: 1 })).toHaveTextContent('Senior SDET & Quality Architect — AI-Augmented Testing at Scale')

    REC-02 "renders pitch paragraph with nine years copy" test:
    - Change regex /Nine years writing self-healing/i to /For the past decade/i

    REC-03 "renders all four metric numbers" test:
    - Change /^9/ to /^10/ (10 yrs)
    - Change /^0\.4/ to /^80/ (80 %)
    - Change /^98\.2/ to /^30/ (30 %)
    - Change /^1,247/ to /^16/ (16 M+)

    REC-05 "renders all three job role headings" test:
    - Change all three `getByRole` heading assertions to: `{ name: 'Senior SDET (AWS/Mobile/Web)' }`, `{ name: 'SDET / Software Engineer' }`, `{ name: 'QA Tester (Mobile)' }` — all three need new values, none carried over unchanged.

    REC-07 "renders availability Status and Visa row values" test:
    - Change 'Open to offers · Q3 start' to 'Open to opportunities · Q3 start'
    - Change 'US citizen — no sponsorship needed' to 'Authorized to work in the US (details on request)'

    e2e/recruiter.spec.ts — assertions to change:

    REC-02 hero test:
    - Change getByText('Available · Q3 start') to getByText('Open to opportunities · Q3 start')
    - Remove h1 em assertion (page.locator('h1 em').filter({ hasText: 'AI products' })) — headline is plain text now. Replace with: page.locator('h1').filter({ hasText: 'Senior SDET & Quality Architect' })
    - Change getByText(/Nine years writing/i) to getByText(/For the past decade/i)
    - Change getByRole('link', { name: /alex@morgan\.dev/i }) to getByRole('link', { name: /ruslankanat\.b@gmail\.com/i })

    REC-03 metric numbers test:
    - Change /^9/ to /^10/
    - Change /^0\.4/ to /^80/
    - Change /^98\.2/ to /^30/
    - Change /^1,247/ to /^16/

    REC-05 job role headings test:
    - Change /Staff SDET/ to /Senior SDET \(AWS\/Mobile\/Web\)/ (or use exact string match)
    - Change /Automation Engineer/ to /QA Tester \(Mobile\)/
    - Keep /Senior SDET/ — but this now risks matching "Senior SDET (AWS/Mobile/Web)" for Job 1 AND a partial for nothing else. Use more specific patterns: /Senior SDET \(AWS/ for job 1, /SDET \/ Software Engineer/ for job 2, /QA Tester/ for job 3.

    REC-07 availability test:
    - Change 'Open to offers · Q3 start' to 'Open to opportunities · Q3 start'
    - Change 'US citizen — no sponsorship needed' to 'Authorized to work in the US (details on request)'

    REC-08 contact test:
    - Change /github\.com\/amorgan/i to /github\.com\/ruslankanat-sdet/i
    - Change /in\/amorgan-sdet/i to /in\/ruslan-kanatbek/i

    REC-10 mobile overflow test:
    - Change getByText('Lumen Systems') to getByText('ResMed') (first visible company in timeline)

    Do NOT change: REC-A11Y test, REC-01 tests, the REC-08 "footer Open the IDE" assertion, the contact section Email/GitHub/LinkedIn label assertions.
  </action>
  <verify>
    <automated>cd /Users/ruslankanat/Documents/sdet-portfolio && pnpm test 2>&1 | tail -20</automated>
  </verify>
  <done>
    - pnpm test exits 0 with all tests passing
    - No test file contains: 'Available · Q3 start' (old form with "Available"), 'AI products' as EM text, '/Nine years writing/', '/^9/' metric assertion, '/^0\.4/', '/^98\.2/', '/^1,247/', 'Staff SDET, AI Platform', 'Automation Engineer' (old heading), 'Open to offers', 'US citizen — no sponsorship needed', 'alex@morgan\.dev', 'amorgan', 'Lumen Systems'
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| content module → components | resume-content.ts values are static strings authored by developer — not user input |
| component → DOM | JSX renders string values as text nodes — no dangerouslySetInnerHTML anywhere in recruiter components |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Tampering | resume-content.ts string values | accept | Content is developer-authored static data; no runtime input; any corruption is visible in code review |
| T-10-02 | Information Disclosure | Hero.tsx pitch paragraph | accept | No PII beyond publicly shared professional info; content is intentionally public |
| T-10-03 | Tampering | XSS via dangerouslySetInnerHTML | mitigate | Verify zero instances of dangerouslySetInnerHTML in all recruiter components before shipping. All HERO_COPY strings render as React text nodes, not HTML. |
| T-10-SC | Tampering | npm/pip installs | accept | No new package installs in this plan — pure content and code changes only |
</threat_model>

<verification>
After all three tasks complete, run the following checks:

1. TypeScript: `npx tsc --noEmit` — must exit 0
2. Unit tests: `pnpm test` — must exit 0, all tests green
3. Placeholder grep — all must return zero lines:
   - `grep -r "Lumen Systems" src/components/recruiter/`
   - `grep -r "Alex Morgan\|amorgan\|Nine years\|AI products\|Open to offers\|US citizen — no" src/components/recruiter/`
   - `grep -r "Lumen Systems\|amorgan\|alex@morgan" e2e/recruiter.spec.ts`
4. Real content grep — all must match:
   - `grep "Open to opportunities" src/lib/resume-content.ts`
   - `grep "ResMed" src/lib/resume-content.ts`
   - `grep "Authorized to work in the US" src/lib/resume-content.ts`
   - `grep "earlierCareers" src/components/recruiter/Timeline.tsx`
5. No dangerouslySetInnerHTML: `grep -r "dangerouslySetInnerHTML" src/components/recruiter/` — must return zero lines
</verification>

<success_criteria>
- src/lib/resume-content.ts exists with exports HERO_COPY, METRICS, JOBS, SKILL_GROUPS, AVAIL_ROWS, EARLIER_CAREERS, Job
- All six recruiter components import from resume-content.ts instead of defining local const arrays
- Timeline.tsx renders the .earlierCareers paragraph with EARLIER_CAREERS text
- RecruiterView.tsx §02 lede shows HERO_COPY.nowLede (ResMed / myAir copy)
- pnpm test passes (all unit tests green)
- Zero placeholder strings remain in src/components/recruiter/ or test files
</success_criteria>

<output>
Create `.planning/phases/10-content-print-polish/10-01-SUMMARY.md` when done
</output>
