---
phase: 08-landing-door
plan: 02
type: execute
wave: 2
depends_on:
  - 08-01
files_modified:
  - e2e/landing.spec.ts
  - e2e/ide-interactions.spec.ts
  - e2e/navigation.spec.ts
autonomous: true
requirements:
  - DOOR-01
  - DOOR-02
  - DOOR-03
  - DOOR-04
must_haves:
  truths:
    - "Every existing e2e test that calls `page.goto('/')` and expects IDE chrome pre-seeds `localStorage['resume-mode'] = 'ide'` via `page.addInitScript` before navigation"
    - "A new e2e test verifies the door renders at `/` when localStorage is empty (DOOR-01 E2E)"
    - "A new e2e test verifies that clicking the recruiter half navigates the rendered view to the recruiter stub and refreshing keeps it (DOOR-03)"
    - "A new e2e test verifies that visiting `/?reset` after a stored mode clears localStorage and re-shows the door (DOOR-04)"
    - "`pnpm playwright test` exits 0 — full suite is green after Plan 08-01 wired DoorScreen into page.tsx"
  artifacts:
    - path: "e2e/landing.spec.ts"
      provides: "Landing page e2e suite — IDE-seeded existing tests + new Landing door describe block"
      contains: "test.describe('Landing door'"
    - path: "e2e/ide-interactions.spec.ts"
      provides: "All three tests pre-seed IDE mode via addInitScript"
      contains: "localStorage.setItem('resume-mode', 'ide')"
    - path: "e2e/navigation.spec.ts"
      provides: "About-link test pre-seeds IDE mode"
      contains: "localStorage.setItem('resume-mode', 'ide')"
  key_links:
    - from: "e2e/landing.spec.ts (Landing page suite)"
      to: "page.addInitScript"
      via: "test.beforeEach"
      pattern: "test\\.beforeEach"
    - from: "e2e/landing.spec.ts (Landing door suite)"
      to: "DoorScreen rendered at /"
      via: "page.goto('/') + getByRole('button', { name: /Enter the résumé/i })"
      pattern: "Enter the résumé"
---

<objective>
Update the three existing Playwright spec files so that every test calling `page.goto('/')` and expecting IDE chrome pre-seeds `localStorage['resume-mode'] = 'ide'` via `page.addInitScript` — this prevents Plan 08-01's DoorScreen wiring from breaking CI. Then add a new `Landing door` describe block to `e2e/landing.spec.ts` covering DOOR-01 (door visible with no localStorage), DOOR-03 (return visitor bypasses door), and DOOR-04 (`?reset` clears mode and re-shows door).

Purpose: Plan 08-01 ships the Door at `/`, which means every existing test that assumed `/` showed the IDE will now fail (9 tests across 3 files identified by `grep -n "goto.*'/'" e2e/*.spec.ts`). Per the orchestrator's `<planning_guidance>`, the e2e fix is BLOCKING for CI — but it does NOT need to land in the same wave as 08-01 because 08-01's commit is allowed to leave the e2e suite red briefly between wave 1 merge and wave 2 merge. Wave 2 closes that window.

Output: Three edited e2e files; full Playwright suite green; DOOR-01 / DOOR-03 / DOOR-04 covered by automated e2e tests.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-door/08-CONTEXT.md
@.planning/phases/08-landing-door/08-RESEARCH.md
@.planning/phases/08-landing-door/08-PATTERNS.md
@.planning/phases/08-landing-door/08-01-PLAN.md
@e2e/landing.spec.ts
@e2e/ide-interactions.spec.ts
@e2e/navigation.spec.ts

<interfaces>
<!-- Playwright addInitScript pattern documented in 08-RESEARCH.md Pitfall 2. -->
<!-- addInitScript injects a callback into the page's global scope BEFORE navigation, -->
<!-- so localStorage is already populated when ResumeGateInner's useEffect runs readStoredMode. -->

Pattern to apply to every existing test.describe block that expects IDE chrome at `/`:

  test.describe('...existing name...', () => {
    test.beforeEach(async ({ page }) => {
      await page.addInitScript(() => {
        localStorage.setItem('resume-mode', 'ide');
      });
    });
    // ...existing tests unchanged...
  });

Pattern for the NEW Landing door describe block (no beforeEach seed — door requires no stored mode):

  test.describe('Landing door', () => {
    test('shows door at / with no stored mode', async ({ page }) => {
      await page.goto('/');
      await expect(page.getByRole('button', { name: /Enter the résumé/i })).toBeVisible();
      await expect(page.getByRole('button', { name: /open-ide/i })).toBeVisible();
    });
  });

Wordmark / CTA text exposed by DoorScreen (created in Plan 08-01):
- Recruiter half accessible name (from CTA text content): "Enter the résumé"
- IDE half accessible name (from CTA text content): "$ ./open-ide"
- Phase 9 recruiter stub text in src/app/page.tsx line 71: "Recruiter view — coming in Phase 9"
- IDE branch renders SiteHeader and IDEShell (sidebar README.md row is the easiest stable locator)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Pre-seed IDE mode in existing e2e tests (landing.spec.ts, ide-interactions.spec.ts, navigation.spec.ts)</name>
  <files>e2e/landing.spec.ts, e2e/ide-interactions.spec.ts, e2e/navigation.spec.ts</files>
  <read_first>
    - e2e/landing.spec.ts (full file — 7 tests inside `Landing page` describe; 5 of them `goto('/')`)
    - e2e/ide-interactions.spec.ts (full file — 3 tests inside `IDE interactions` describe, all `goto('/')`)
    - e2e/navigation.spec.ts (full file — 2 tests inside `Navigation` describe; 1 calls `goto('/')`, 1 calls `goto('/about')` then clicks back to `/`)
    - .planning/phases/08-landing-door/08-RESEARCH.md (Pitfall 2 — exact `test.beforeEach` + `page.addInitScript` pattern, list of 9 affected tests)
    - .planning/phases/08-landing-door/08-PATTERNS.md (`e2e/*.spec.ts` section — pattern application reference)
  </read_first>
  <behavior>
    - `e2e/landing.spec.ts`: the existing `Landing page` describe block has a new `test.beforeEach` that seeds `localStorage['resume-mode'] = 'ide'` BEFORE every test that follows. The 7 existing tests inside `Landing page` are unchanged in body. (Note: 2 of the 7 — `robots.txt is accessible` and `sitemap.xml is accessible` — do not call `goto('/')`, they only use `page.request.get`; the seed is harmless for them because addInitScript only takes effect on navigation. They stay inside the same describe and inherit the beforeEach without behavior change.)
    - `e2e/ide-interactions.spec.ts`: the `IDE interactions` describe block has a new `test.beforeEach` that seeds IDE mode. All 3 tests unchanged in body.
    - `e2e/navigation.spec.ts`: the `Navigation` describe block has a new `test.beforeEach` that seeds IDE mode. Both tests unchanged in body. The `back link returns to /` test calls `goto('/about')` first then clicks back to `/` — pre-seeded IDE mode means the door does NOT appear after the back-link click, which is the behavior we want to assert.
    - No test bodies are modified, no assertions are loosened. Only the `beforeEach` hook is added.
    - All existing tests still pass under `pnpm playwright test`.
  </behavior>
  <action>
    Edit each of the three e2e files to insert a `test.beforeEach` hook inside the EXISTING `test.describe` block, immediately after the describe's opening line and BEFORE the first `test(...)`. The hook body is identical across all three files.

    1) `e2e/landing.spec.ts` — inside `test.describe('Landing page', () => {` (line 4 in the current file), insert as a new first statement (right before `test('has correct page title', ...)` currently at line 5) the following block:

       - `test.beforeEach(async ({ page }) => {`
       - `  await page.addInitScript(() => {`
       - `    localStorage.setItem('resume-mode', 'ide');`
       - `  });`
       - `});`

       Use exactly that source: single quotes for `'resume-mode'` and `'ide'`, two-space indent matching the rest of the file. Do not touch the existing imports (`test, expect`, `AxeBuilder`). Do not touch any of the 7 tests inside `Landing page`. The new `beforeEach` is added to the SAME describe block — the Landing door tests added in Task 2 go into a separate, new describe block at the bottom of the file.

    2) `e2e/ide-interactions.spec.ts` — inside `test.describe('IDE interactions', () => {` (line 3 in the current file), insert the same `test.beforeEach` block before the first test (line 4). Do not touch the 3 existing tests.

    3) `e2e/navigation.spec.ts` — inside `test.describe('Navigation', () => {` (line 3 in the current file), insert the same `test.beforeEach` block before the first test (line 4). Do not touch the 2 existing tests.

    The `addInitScript` callback is a no-arg arrow function that calls `localStorage.setItem('resume-mode', 'ide')`. Use single quotes for the key and value to match the project's eslint/prettier style.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && test "$(grep -c 'test.beforeEach' e2e/landing.spec.ts)" = "1" && test "$(grep -c 'test.beforeEach' e2e/ide-interactions.spec.ts)" = "1" && test "$(grep -c 'test.beforeEach' e2e/navigation.spec.ts)" = "1" && test "$(grep -c "localStorage.setItem('resume-mode', 'ide')" e2e/landing.spec.ts)" = "1" && test "$(grep -c "localStorage.setItem('resume-mode', 'ide')" e2e/ide-interactions.spec.ts)" = "1" && test "$(grep -c "localStorage.setItem('resume-mode', 'ide')" e2e/navigation.spec.ts)" = "1" && pnpm playwright test e2e/landing.spec.ts e2e/ide-interactions.spec.ts e2e/navigation.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.beforeEach" e2e/landing.spec.ts` returns exactly `1`
    - `grep -c "test.beforeEach" e2e/ide-interactions.spec.ts` returns exactly `1`
    - `grep -c "test.beforeEach" e2e/navigation.spec.ts` returns exactly `1`
    - In each of the three files, `grep -c "localStorage.setItem('resume-mode', 'ide')" <file>` returns exactly `1`
    - Each `test.beforeEach` is positioned inside an existing `test.describe` block (not at the file top level) — verify by reading the file and confirming the line containing `test.beforeEach` is between the `test.describe('...', () => {` opening and the first `test(...)`
    - All 7 existing tests in `e2e/landing.spec.ts` remain present — `grep -c "^  test(" e2e/landing.spec.ts` returns `7`
    - All 3 existing tests in `e2e/ide-interactions.spec.ts` remain present — `grep -c "^  test(" e2e/ide-interactions.spec.ts` returns `3`
    - All 2 existing tests in `e2e/navigation.spec.ts` remain present — `grep -c "^  test(" e2e/navigation.spec.ts` returns `2`
    - `pnpm playwright test e2e/landing.spec.ts e2e/ide-interactions.spec.ts e2e/navigation.spec.ts` exits `0` (12 tests pass total — Task 1 does not yet add the new Landing door tests, so the run is 7+3+2=12)
  </acceptance_criteria>
  <done>
    Three e2e files each contain a single `test.beforeEach` block at the top of their existing describe, seeding IDE mode via `page.addInitScript`. All 12 existing tests across the three files still pass. CI is unblocked for the Phase 8 DoorScreen wiring from Plan 08-01.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add new Landing door e2e describe block covering DOOR-01, DOOR-03, DOOR-04</name>
  <files>e2e/landing.spec.ts</files>
  <read_first>
    - e2e/landing.spec.ts (full file — confirm the `Landing page` describe ends at the closing `});`; the new `Landing door` describe goes after that line)
    - src/components/door/DoorScreen.tsx (created in Plan 08-01 — confirm CTA strings `Enter the résumé` and `$ ./open-ide` are present so getByRole queries match)
    - src/app/page.tsx (the wired `<DoorScreen>` and the `mode === 'recruiter'` stub `Recruiter view — coming in Phase 9` used as the post-click assertion target)
    - .planning/phases/08-landing-door/08-PATTERNS.md (`e2e/*.spec.ts` section — `New door-specific tests pattern` block with example tests)
  </read_first>
  <behavior>
    - A new `test.describe('Landing door', ...)` block is appended to `e2e/landing.spec.ts` AFTER the existing `Landing page` describe closes
    - The new describe has NO `test.beforeEach` localStorage seeding (the door requires localStorage to be empty)
    - Test 1 (DOOR-01 E2E): At `/` with no stored mode, both door halves are visible by accessible name (`/Enter the résumé/i` and `/\$ \.\/open-ide/i`)
    - Test 2 (DOOR-03 same-session): Clicking the recruiter half re-renders the recruiter stub WITHOUT a full page navigation — the URL stays `/`, the door is no longer visible, the Phase 9 stub text is visible
    - Test 3 (DOOR-03 return-visit): Pre-seeding `localStorage['resume-mode'] = 'ide'` via `addInitScript` and then visiting `/` skips the door entirely — the recruiter CTA is not visible and the IDE sidebar README.md row is visible
    - Test 4 (DOOR-04): Pre-seeding IDE mode then visiting `/?reset` clears the stored mode and re-shows the door (both CTAs visible)
  </behavior>
  <action>
    Append a new `test.describe` block to the bottom of `e2e/landing.spec.ts`, after the closing `});` of the existing `Landing page` describe block. Do NOT add another import — `test` and `expect` are already imported at the top of the file.

    The new block contains exactly four tests in the order below. Test names must be the exact strings shown so test-report logs trace cleanly to requirement IDs.

    Test 1: `test('DOOR-01: shows door at / with no stored mode', async ({ page }) => { ... })`
      - `await page.goto('/')`
      - `await expect(page.getByRole('button', { name: /Enter the résumé/i })).toBeVisible()`
      - `await expect(page.getByRole('button', { name: /\$ \.\/open-ide/i })).toBeVisible()`

    Test 2: `test('DOOR-03 same-session: clicking recruiter half re-renders without navigation', async ({ page }) => { ... })`
      - `await page.goto('/')`
      - `const url = page.url()`
      - `await page.getByRole('button', { name: /Enter the résumé/i }).click()`
      - `await expect(page.getByText(/Recruiter view — coming in Phase 9/i)).toBeVisible()`
      - `await expect(page.getByRole('button', { name: /Enter the résumé/i })).not.toBeVisible()`
      - `expect(page.url()).toBe(url)` — same URL means no full navigation occurred

    Test 3: `test('DOOR-03 return-visit: pre-seeded ide mode skips the door', async ({ page }) => { ... })`
      - `await page.addInitScript(() => { localStorage.setItem('resume-mode', 'ide'); })`
      - `await page.goto('/')`
      - `await expect(page.getByRole('button', { name: /Enter the résumé/i })).not.toBeVisible()`
      - `await expect(page.getByRole('button', { name: /README\.md/ }).first()).toBeVisible()` — same sidebar locator used in `Landing page` line 14

    Test 4: `test('DOOR-04: ?reset clears stored mode and re-shows the door', async ({ page }) => { ... })`
      - `await page.addInitScript(() => { localStorage.setItem('resume-mode', 'ide'); })`
      - `await page.goto('/?reset')`
      - `await expect(page.getByRole('button', { name: /Enter the résumé/i })).toBeVisible()`
      - `await expect(page.getByRole('button', { name: /\$ \.\/open-ide/i })).toBeVisible()`

    Selector rationale (do not modify these decisions during execution):
    - `getByRole('button', { name: /Enter the résumé/i })` matches DoorScreen's recruiter half because its accessible name is computed from descendant text content per ARIA Authoring Practices (the visible CTA span contains "Enter the résumé").
    - `getByRole('button', { name: /\$ \.\/open-ide/i })` similarly matches the IDE half via its `$ ./open-ide` CTA.
    - The Phase 9 stub uses literal text `Recruiter view — coming in Phase 9` (verified in src/app/page.tsx line 71) — that string is the post-click target for Test 2.
    - For the return-visit case (Test 3), the IDEShell's sidebar README.md button (same locator the existing `Landing page` test on line 14 uses) confirms IDE chrome rendered. Use `.first()` because the IDE has README.md visible in both the sidebar AND once opened, the tab/editor — first() pins it to the sidebar row deterministically.

    Do not change any existing test in the file. Do not touch the `Landing page` describe block (Task 1 already added its `beforeEach`). Do not add a `test.beforeEach` inside the new `Landing door` block — these tests need a clean (no `resume-mode`) localStorage by default.
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit -p tsconfig.json && test "$(grep -c "test.describe('Landing door'" e2e/landing.spec.ts)" = "1" && test "$(grep -c "DOOR-01: shows door at / with no stored mode" e2e/landing.spec.ts)" = "1" && test "$(grep -c 'DOOR-03 same-session' e2e/landing.spec.ts)" = "1" && test "$(grep -c 'DOOR-03 return-visit' e2e/landing.spec.ts)" = "1" && test "$(grep -c 'DOOR-04: ?reset clears stored mode' e2e/landing.spec.ts)" = "1" && pnpm playwright test e2e/landing.spec.ts --grep "Landing door" && pnpm playwright test</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "test.describe('Landing door'" e2e/landing.spec.ts` returns exactly `1`
    - All four test names appear exactly once each: `DOOR-01: shows door at /`, `DOOR-03 same-session`, `DOOR-03 return-visit`, `DOOR-04: ?reset clears stored mode`
    - The new `Landing door` describe block contains NO `test.beforeEach` (`awk '/test.describe..Landing door/,/^});/' e2e/landing.spec.ts | grep -c "test.beforeEach"` returns `0`)
    - Both selectors for the recruiter CTA appear at least 3 times across the new block: `grep -c "Enter the résumé" e2e/landing.spec.ts` returns at least `4` (4 references inside Landing door + 0 in Landing page = 4 minimum)
    - The IDE CTA selector `\$ \.\/open-ide` appears at least 2 times: `grep -c "\\\\\$ \\\\\\.\\\\\/open-ide" e2e/landing.spec.ts` returns at least `2`
    - The Phase 9 stub text appears: `grep -c "Recruiter view — coming in Phase 9" e2e/landing.spec.ts` returns exactly `1`
    - The `addInitScript` pattern appears twice in the new block (Test 3 and Test 4): `awk '/test.describe..Landing door/,/^});/' e2e/landing.spec.ts | grep -c "addInitScript"` returns `2`
    - `pnpm playwright test e2e/landing.spec.ts --grep "Landing door"` exits `0` with 4 passing tests
    - `pnpm playwright test` (full suite) exits `0` — all `landing.spec.ts` + `ide-interactions.spec.ts` + `navigation.spec.ts` + `about.spec.ts` tests pass together
  </acceptance_criteria>
  <done>
    `e2e/landing.spec.ts` contains a new `Landing door` describe block with 4 tests covering DOOR-01 (door rendered), DOOR-03 (same-session + return-visit), and DOOR-04 (`?reset`). All four tests pass. Full Playwright suite exits `0`. The Phase 8 e2e gate is now closed: every requirement (DOOR-01 through DOOR-04) has at least one automated test asserting its observable behavior.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test runner ↔ browser context | Playwright's `addInitScript` injects a callback into the page's global scope before navigation; this is test-only code and never ships to production |

Plan 08-02 modifies only `e2e/*.spec.ts` files. These files are excluded from the production bundle (Playwright tests live outside Next.js's compilation graph). There is no new code path in the application — only new assertions against the Plan 08-01 wiring.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-06 | Tampering | Test-injected localStorage value via `addInitScript` | accept | `addInitScript` runs in the Playwright-owned browser context only. It cannot affect real users; it cannot persist across runs unless Playwright explicitly chooses to. No production impact. |
| T-08-07 | Information Disclosure | Test fixtures | accept | No secrets are stored in `e2e/` files. localStorage values are non-sensitive literal strings (`'ide'`). |
| T-08-08 | Denial of Service | Playwright suite execution time | accept | The 4 new tests add ~5-10 seconds to a full suite run. The full suite still completes in well under 60 seconds — within the project's CI budget. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Plan 08-02 installs ZERO new packages. `@playwright/test`, `AxeBuilder`, and all helpers are already in `devDependencies`. No supply-chain surface introduced. |

**ASVS Level 1 categories applicable:** None — Plan 08-02 is test-only code with no production code path, no auth, no data persistence beyond Plan 08-01's already-validated localStorage enum read.
</threat_model>

<verification>
After both tasks complete, run from the repo root:

```
pnpm exec tsc --noEmit -p tsconfig.json
pnpm playwright test
```

Expected results:
- `pnpm exec tsc --noEmit` exits `0` (no test-file type errors)
- `pnpm playwright test` exits `0` with these counts (assuming `about.spec.ts` already passing): 7 + 3 + 2 + 4 = 16 total tests in the three modified files, plus whatever exists in `about.spec.ts` (1+ tests). All green.

Specific failure modes to watch for in CI:
1. **Existing `Landing page` axe scan now flags `role="button"` div elements as accessibility issues.** Mitigation: the role+tabIndex+onKeyDown triple satisfies WCAG 4.1.2 by design; if axe still flags it, the test failure is a real signal that DoorScreen's accessibility implementation in Plan 08-01 has a defect. Address in 08-01 — do NOT loosen the axe assertion here.
2. **`Landing door` Test 3 (return-visit) flakes.** If the IDEShell sidebar takes a moment to mount, the `.first()` locator should still wait via Playwright's auto-waiting. If flakiness occurs, swap the assertion to `page.locator('aside').getByRole('button', { name: /README\.md/ })` to scope to the sidebar `<aside>` element (same pattern as `landing.spec.ts` line 20). Do NOT add explicit `waitForTimeout`.
3. **`Landing door` Test 2 (same-session) reports a different URL.** ResumeGate uses `setMode` (React state) — there is NO `router.push` or `window.location.href` mutation in the recruiter callback. If the URL changes, that's a regression in Plan 08-01's wiring that the test correctly catches.
</verification>

<success_criteria>
- All four new `Landing door` tests pass under `pnpm playwright test`
- All 12 existing tests across `landing.spec.ts`, `ide-interactions.spec.ts`, `navigation.spec.ts` still pass after IDE-mode pre-seeding
- `pnpm playwright test` (full suite) exits `0`
- DOOR-01, DOOR-03, DOOR-04 each have at least one passing automated e2e assertion
- No production code modified — only `e2e/` files touched
- No new dependencies installed
</success_criteria>

<output>
After implementation, write a brief summary to `.planning/phases/08-landing-door/08-02-SUMMARY.md` capturing: the three e2e files modified, the four new Landing door tests added with their requirement IDs, the full Playwright run pass count (target: 16+ across the three modified files), and any test-name or selector deviations from the plan. Reference the SUMMARY format at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
