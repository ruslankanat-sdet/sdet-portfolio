---
phase: 08-landing-door
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/globals.css
  - src/app/layout.tsx
  - src/components/door/LogoMark.tsx
  - src/components/door/DoorScreen.tsx
  - src/components/door/__tests__/DoorScreen.test.tsx
  - src/app/page.tsx
autonomous: true
requirements:
  - DOOR-01
  - DOOR-02
  - DOOR-03
  - DOOR-04
must_haves:
  truths:
    - "A first-time visitor at `/` sees a full-viewport split — left cream (recruiter) half + right dark IDE half"
    - "Each half is keyboard-reachable (Tab focuses it) and activatable via Enter and Space without scrolling the page"
    - "Clicking the recruiter half writes `localStorage['resume-mode'] = 'recruiter'` and re-renders into the recruiter stub without a full page navigation"
    - "Clicking the IDE half writes `localStorage['resume-mode'] = 'ide'` and re-renders into the IDEShell without a full page navigation"
    - "Newsreader font weights 300/400/500/600/700 (italic + normal) are loaded via next/font and exposed as `--font-newsreader`"
    - "Cream/forest CSS tokens are defined on `:root` in globals.css alongside IDE tokens"
  artifacts:
    - path: "src/components/door/LogoMark.tsx"
      provides: "18×18 inline SVG glyph in currentColor"
      exports: ["LogoMark"]
    - path: "src/components/door/DoorScreen.tsx"
      provides: "Full-viewport split-screen Door client component"
      exports: ["DoorScreen"]
    - path: "src/components/door/__tests__/DoorScreen.test.tsx"
      provides: "Unit tests for DoorScreen render + click + keyboard"
    - path: "src/app/globals.css"
      provides: "Cream/forest tokens + .door / .door-half / .door-* utility classes + reduced-motion + 760px mobile rules"
      contains: "--paper: #f4ecdc"
    - path: "src/app/layout.tsx"
      provides: "Newsreader weight expansion to ['300', '400', '500', '600', '700']"
      contains: "weight: ['300', '400', '500', '600', '700']"
    - path: "src/app/page.tsx"
      provides: "ResumeGateInner renders <DoorScreen onChooseRecruiter onChooseIDE /> when mode === null"
      contains: "<DoorScreen"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/door/DoorScreen.tsx"
      via: "named import"
      pattern: "import \\{ DoorScreen \\} from '@/components/door/DoorScreen'"
    - from: "src/components/door/DoorScreen.tsx"
      to: "src/components/door/LogoMark.tsx"
      via: "named import"
      pattern: "import \\{ LogoMark \\} from './LogoMark'"
    - from: "src/components/door/DoorScreen.tsx"
      to: "src/app/globals.css"
      via: "className references to .door, .door-half, .door-half-recruiter, .door-half-ide, .door-body, .door-id, .door-id-mark, .door-id-wordmark, .door-eyebrow, .door-name, .door-tagline, .door-cta, .door-foot, .door-bg-pattern"
      pattern: "className=\"door"
---

<objective>
Build the full DoorScreen UI — cream + dark split, hover-grow animation, keyboard accessibility, LogoMark glyph — and wire it into the existing ResumeGate at `src/app/page.tsx`. The ResumeGate routing shell (mode persistence, `?reset`, localStorage read) is already complete from Phase 7; this plan adds the Door UI plus the write-side callbacks. Ships with co-located unit tests covering DOOR-01 (renders two role=button halves with correct copy) and DOOR-02 (click + Enter + Space invoke callbacks).

Purpose: Phase 8 of the v1.2 milestone — first-time visitors get an audience-splitting landing; return visitors are routed straight to their saved view by Phase 7's gate (DOOR-03); `?reset` is already handled by the gate (DOOR-04).

Output: One new component dir (`src/components/door/`), three new files, three edits to existing files, full Vitest coverage for DOOR-01 / DOOR-02.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-landing-door/08-CONTEXT.md
@.planning/phases/08-landing-door/08-RESEARCH.md
@.planning/phases/08-landing-door/08-PATTERNS.md
@.planning/design/design_handoff_resume_v1/README.md
@.planning/design/design_handoff_resume_v1/resume.css
@.planning/design/design_handoff_resume_v1/resume-app.jsx
@.planning/design/design_handoff_resume_v1/recruiter.jsx
@src/app/page.tsx
@src/app/layout.tsx
@src/app/globals.css
@src/components/ide/__tests__/Sidebar.test.tsx

<interfaces>
<!-- Contracts the executor must use directly. Extracted from existing code. -->
<!-- Do not re-explore the codebase to discover these. -->

From src/app/page.tsx (already implemented in Phase 7):
- `const MODE_KEY = 'resume-mode';` — localStorage key constant (already in scope inside ResumeGateInner)
- `type Mode = 'recruiter' | 'ide' | null;`
- `readStoredMode(): Mode` — already implemented, validates against the enum
- `clearStoredMode(): void` — already implemented
- Inside ResumeGateInner: `setMode` is the React state setter (`useState<Mode>`)
- Insertion point: the `return (...)` for `mode === null` at lines 76-93

Required DoorScreen interface (this plan creates it):
- `interface DoorScreenProps { onChooseRecruiter: () => void; onChooseIDE: () => void; }`
- `export function DoorScreen(props: DoorScreenProps): JSX.Element`
- No internal state, no useEffect, no localStorage access — purely presentational

Required LogoMark interface (this plan creates it):
- `export function LogoMark({ size = 18 }: { size?: number }): JSX.Element`
- No 'use client' directive (no event handlers, no hooks)

Existing analog (Sidebar.test.tsx) test header pattern to copy:
```typescript
/// <reference types="vitest/globals" />
import { render, screen } from '@testing-library/react';
import userEvent from '@testing-library/user-event';
import { vi } from 'vitest';
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Append cream/forest tokens (D-07) and .door-* utility classes (D-08) to globals.css</name>
  <files>src/app/globals.css</files>
  <read_first>
    - src/app/globals.css (current `:root` block lines 8-69 + `.site-body` lines 155-160 — understand the insertion location and the parent flex container that the door must fit inside)
    - .planning/design/design_handoff_resume_v1/resume.css (lines 1-245 — the source of truth for door styles; copy values verbatim except for `.door { height: 100% }` override)
    - .planning/phases/08-landing-door/08-RESEARCH.md (Pattern 1, Pattern 2, Pattern 4 — height adaptation, oklch token block, flex-grow animation)
  </read_first>
  <behavior>
    - All 13 cream/forest tokens (D-09) are defined on `:root` in globals.css
    - `.door` class uses `height: 100%` (NOT `min-height: 100vh`) so it fills `.site-body` which is `100dvh`
    - `.door-half` has the flex-grow transition `flex-grow .55s cubic-bezier(.4,.0,.2,1)` and `padding: 40px 64px 48px`
    - `.door:hover .door-half { flex-grow: 0.85 }` and `.door:hover .door-half:hover { flex-grow: 1.3 }`
    - `@media (prefers-reduced-motion: reduce) { .door-half { transition: none; } }` is present
    - `@media (max-width: 760px)` block resets flex-direction to column and resets flex-grow to 1 on hover
    - Existing IDE token block, `.site-body`, `.site-main`, and `@keyframes pulse-dot` are unchanged
  </behavior>
  <action>
    Append a new section to `src/app/globals.css` AFTER the existing `:root[data-theme='light']` block (currently ending around line 122) and BEFORE the `/* Global resets */` comment (currently at line 124). The new section must contain three blocks delimited by `/* ───── */` comment banners, in this order:

    Block 1 — Token block. Use a plain `:root` selector (no `[data-theme=...]` — these tokens are always-on). Banner comment: `Door / recruiter editorial tokens — cream + forest green`. Declare exactly these 13 properties with the exact values from `.planning/design/design_handoff_resume_v1/resume.css` lines 7-24:
      - `--paper: #f4ecdc;`
      - `--paper-deep: #ede4d0;`
      - `--paper-soft: #faf4e6;`
      - `--ink: #1a1f1c;`
      - `--ink-soft: #3a3f3a;`
      - `--ink-muted: #6e6a5e;`
      - `--ink-faint: #9b9486;`
      - `--rule: #d8d0bd;`
      - `--rule-soft: #e4ddca;`
      - `--forest: oklch(0.40 0.09 152);`
      - `--forest-deep: oklch(0.30 0.08 152);`
      - `--forest-soft: oklch(0.40 0.09 152 / 0.08);`
      - `--forest-line: oklch(0.40 0.09 152 / 0.20);`
      Do NOT add `--ide-green` — the IDE green `#3ddc84` is already available as `--green` in the IDE token block. Use `var(--green)` in any `.door-half-ide` rules that need it.

    Block 2 — Door layout + identity stripe + halves. Banner comment: `Landing — two-door split (overrides resume.css for height to fit .site-body)`. Port every rule from `.planning/design/design_handoff_resume_v1/resume.css` lines 50-245 VERBATIM, with these mandatory adaptations:
      - `.door`: replace `min-height: 100vh;` with `height: 100%;` (per Pitfall 1). Keep `position: relative; width: 100%; display: flex; overflow: hidden; background: #000;`. Per D-06 the door fills its `.site-body` parent which is already `100dvh`.
      - Replace every literal `#3ddc84` reference inside `.door-half-ide` rules with `var(--green)` so the IDE token reused.
      - Replace every literal `oklch(0.78 0.17 152 / 0.10)` / `oklch(0.78 0.17 152 / 0.25)` / `oklch(0.78 0.17 152 / 0.04)` inside `.door-half-ide .door-id-mark` with `rgba(61, 220, 132, 0.10)` / `rgba(61, 220, 132, 0.25)` / `rgba(61, 220, 132, 0.04)` respectively — these match `--green-rgb: 61, 220, 132` already defined.
      - All other selectors (`.door-id`, `.door-id-mark`, `.door-id-wordmark`, `.door-half-recruiter`, `.door-half-ide`, `.door-eyebrow`, `.door-eyebrow .dot`, `.door-body`, `.door-name`, `.door-tagline`, `.door-cta`, `.door-cta .arrow`, `.door-foot`, `.door-bg-pattern`, the `::after` separator on recruiter half, the radial gradients on IDE half) port byte-for-byte.
      - Include `@keyframes door-fade-in { from { opacity: 0; } to { opacity: 1; } }` after the layout rules.

    Block 3 — Responsive + motion gates. Banner comment: `Door — motion + responsive gates`. Two media queries verbatim from resume.css lines 239-245 plus the reduced-motion rule from Research Pattern 4:
      - `@media (prefers-reduced-motion: reduce) { .door-half { transition: none; } }`
      - `@media (max-width: 760px) { .door { flex-direction: column; } .door-half { padding: 32px 28px; } .door:hover .door-half, .door:hover .door-half:hover { flex-grow: 1; } .door-half-recruiter::after { display: none; } }`

    Do NOT touch the existing IDE `:root` block, the `:root[data-theme='light']` block, `.site-body`, `.site-main`, `#root` background, syntax tokens, markdown tokens, or existing keyframes. Append-only.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -q "^  --paper: #f4ecdc;" src/app/globals.css && grep -q "^  --forest: oklch(0.40 0.09 152);" src/app/globals.css && grep -q "^\.door {" src/app/globals.css && grep -q "height: 100%;" src/app/globals.css && ! grep -q "min-height: 100vh" src/app/globals.css && grep -q "transition: flex-grow .55s cubic-bezier(.4,.0,.2,1)" src/app/globals.css && grep -q "prefers-reduced-motion: reduce" src/app/globals.css && grep -q "max-width: 760px" src/app/globals.css</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "^  --paper: #f4ecdc;" src/app/globals.css` returns `1`
    - `grep -c "^  --forest: oklch(0.40 0.09 152);" src/app/globals.css` returns `1`
    - All 13 cream/forest tokens present (check each: `--paper`, `--paper-deep`, `--paper-soft`, `--ink`, `--ink-soft`, `--ink-muted`, `--ink-faint`, `--rule`, `--rule-soft`, `--forest`, `--forest-deep`, `--forest-soft`, `--forest-line`)
    - `grep -c "^\.door {" src/app/globals.css` returns `1`
    - `grep -E "\.door \{" src/app/globals.css | wc -l` shows `.door` block exists; following block contains `height: 100%;` and does NOT contain `min-height: 100vh`
    - `grep -c "transition: flex-grow .55s cubic-bezier(.4,.0,.2,1)" src/app/globals.css` returns `1`
    - `grep -c "@media (prefers-reduced-motion: reduce)" src/app/globals.css` returns at least `1`
    - `grep -c "@media (max-width: 760px)" src/app/globals.css` returns `1`
    - `grep -c "flex-grow: 0.85" src/app/globals.css` returns `1`
    - `grep -c "flex-grow: 1.3" src/app/globals.css` returns `1`
    - Existing IDE tokens unchanged: `grep -c "^  --bg-deepest: #06090e;" src/app/globals.css` returns `1`
    - `pnpm tsc --noEmit` exits `0` (CSS does not affect tsc but confirms no accidental syntax breakage elsewhere)
  </acceptance_criteria>
  <done>
    Cream/forest tokens and `.door-*` utility classes are present in globals.css. `.door` uses `height: 100%` (NOT `min-height: 100vh`). Reduced-motion and ≤760px media queries are in place.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Expand Newsreader weight array in layout.tsx</name>
  <files>src/app/layout.tsx</files>
  <read_first>
    - src/app/layout.tsx (the full file — to confirm only line 17 needs to change)
    - .planning/phases/08-landing-door/08-RESEARCH.md (Pattern 5 — Newsreader weight expansion verified against `node_modules/next/dist/compiled/@next/font/dist/google/font-data.json`)
  </read_first>
  <behavior>
    - `newsreader` declaration in `src/app/layout.tsx` lists weights `['300', '400', '500', '600', '700']`
    - No other field in the Newsreader declaration changes (subsets, variable, display, style remain identical)
    - File still compiles and renders the document correctly
  </behavior>
  <action>
    In `src/app/layout.tsx`, change line 17 from `  weight: ['400', '500'],` to `  weight: ['300', '400', '500', '600', '700'],` (per D-10). The single change is the weight array literal — do NOT modify `subsets`, `variable: '--font-newsreader'`, `display: 'swap'`, or `style: ['normal', 'italic']`. Do NOT touch the Inter or JetBrains_Mono declarations. Do NOT touch any other line in the file.
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && grep -q "weight: \['300', '400', '500', '600', '700'\]," src/app/layout.tsx && ! grep -q "weight: \['400', '500'\]," src/app/layout.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "weight: \['300', '400', '500', '600', '700'\]," src/app/layout.tsx` returns exactly `1`
    - `grep -c "weight: \['400', '500'\]," src/app/layout.tsx` returns exactly `0`
    - `grep -c "variable: '--font-newsreader'" src/app/layout.tsx` still returns `1` (declaration intact)
    - `grep -c "style: \['normal', 'italic'\]" src/app/layout.tsx` still returns `1` (style array intact)
    - `pnpm tsc --noEmit` exits `0`
  </acceptance_criteria>
  <done>
    Newsreader is declared with the five weights `300/400/500/600/700` (D-10 satisfied). Layout still compiles.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create LogoMark (D-06), DoorScreen (D-05) + unit tests, and wire into page.tsx</name>
  <files>src/components/door/LogoMark.tsx, src/components/door/DoorScreen.tsx, src/components/door/__tests__/DoorScreen.test.tsx, src/app/page.tsx</files>
  <read_first>
    - .planning/design/design_handoff_resume_v1/recruiter.jsx (lines 34-44 — exact SVG path/circle to lift into LogoMark.tsx)
    - .planning/design/design_handoff_resume_v1/resume-app.jsx (lines 73-136 — Door prototype: JSX tree, role/tabIndex/onKeyDown pattern, copy strings; Phase 8 adapts copy per D-01/D-02/D-03/D-04)
    - src/app/page.tsx (full file — insertion point at lines 76-93; MODE_KEY constant at line 8 is already in scope inside ResumeGateInner)
    - src/components/ide/__tests__/Sidebar.test.tsx (full file — analog for the new DoorScreen.test.tsx; copy the userEvent.setup pattern)
    - .planning/phases/08-landing-door/08-PATTERNS.md (sections "src/components/door/DoorScreen.tsx", "src/components/door/LogoMark.tsx", "src/app/page.tsx", "src/components/door/__tests__/DoorScreen.test.tsx")
    - .planning/phases/08-landing-door/08-RESEARCH.md (Pattern 3 keyboard, Pattern 6 use-client, Pitfall 3 Space scroll, Code Examples section)
  </read_first>
  <behavior>
    LogoMark.tsx:
    - Pure function component, no `'use client'` directive
    - Accepts `{ size?: number }` defaulting to `18`
    - Renders a single `<svg>` with `aria-hidden="true"`, the path `M2 9L6 5L8 7L12 3M12 3H15M12 3V6` and a circle at `cx=14 cy=13 r=2`, both with `stroke="currentColor"` and `fill="none"`

    DoorScreen.tsx:
    - First line is `'use client';`
    - Named export `DoorScreen` with `interface DoorScreenProps { onChooseRecruiter: () => void; onChooseIDE: () => void; }`
    - Renders one `<div className="door">` containing exactly two child `<div role="button" tabIndex={0}>` halves
    - Recruiter half (`.door-half-recruiter`): contains `<div className="door-bg-pattern" aria-hidden="true" />`, identity stripe with `<LogoMark size={16} />` and wordmark `ruslan.kanatbek` (D-04), eyebrow `For recruiters & hiring partners`, name `Ruslan` + italic `Kanatbek`, tagline D-02 text verbatim, foot with CTA `Enter the résumé →` and caption `≈ 90 sec read`
    - IDE half (`.door-half-ide`): identity stripe (no bg-pattern), eyebrow `For engineering leads & technical interviewers`, name `ruslan` + accent `.` + `kanatbek`, tagline D-03 text verbatim, foot with CTA `$ ./open-ide →` and caption `react · ide`
    - Both halves have `onClick`, plus `onKeyDown` that triggers the same callback on `Enter` or `' '` (Space). Space call MUST `e.preventDefault()` first to prevent page scroll (Pitfall 3)
    - All styling via className references to globals.css classes (no inline `style` with var() references)

    DoorScreen.test.tsx:
    - Header reference `/// <reference types="vitest/globals" />`
    - Imports from `@testing-library/react`, `@testing-library/user-event`, `vitest`
    - Default props with `vi.fn()` mocks; `vi.clearAllMocks()` in `beforeEach`
    - Three describe blocks: rendering, click interactions, keyboard interactions
    - Rendering: two `role="button"` elements present; recruiter half contains text matching `/Enter the résumé/i`; IDE half contains text matching `/open-ide/i`
    - Click: clicking recruiter button calls `onChooseRecruiter`; clicking IDE button calls `onChooseIDE`
    - Keyboard: Tab to focus first button, press `{Enter}` → recruiter callback fires; Tab again, press `{Enter}` → IDE callback fires; Space (`' '`) on focused recruiter half → recruiter callback fires

    page.tsx:
    - Adds `import { DoorScreen } from '@/components/door/DoorScreen';` after existing imports
    - Replaces the entire `mode === null` `return (...)` block (current lines 76-93) with a JSX expression rendering `<DoorScreen onChooseRecruiter={...} onChooseIDE={...} />`, where each callback uses the project's try/catch idiom to set `localStorage.setItem(MODE_KEY, '<mode>')` then calls `setMode('<mode>')`
    - The two stub divs for `mode === 'recruiter'` and `mode === 'ide'` are unchanged (Phase 9 / existing IDE stub)
    - The `MODE_KEY` constant at line 8 is reused — do NOT redefine it inside the callbacks
  </behavior>
  <action>
    Create three new files and edit one existing file.

    1) Create `src/components/door/LogoMark.tsx` (new file). No `'use client'` directive (pure presentational). Define `interface LogoMarkProps { size?: number }`. Export a named function `LogoMark` that destructures `{ size = 18 }` and returns a single `<svg>` element with attributes `width={size} height={size} viewBox="0 0 18 18" aria-hidden="true"`, containing a `<path>` with `d="M2 9L6 5L8 7L12 3M12 3H15M12 3V6"`, `stroke="currentColor"`, `strokeWidth={1.8}`, `fill="none"`, `strokeLinecap="round"`, `strokeLinejoin="round"`, followed by a `<circle>` with `cx={14} cy={13} r={2} stroke="currentColor" strokeWidth={1.5} fill="none"`. Match the exact SVG attributes from recruiter.jsx lines 34-44. Named export only (no default export, per project convention).

    2) Create `src/components/door/DoorScreen.tsx` (new file). First line: `'use client';` (per Pattern 6). Import `LogoMark` from `./LogoMark` (relative path, per Pattern Map). Declare `interface DoorScreenProps { onChooseRecruiter: () => void; onChooseIDE: () => void; }`. Export a named function `DoorScreen` that destructures `{ onChooseRecruiter, onChooseIDE }`.

    Return JSX with this exact structure (translate to TSX from resume-app.jsx prototype, adapting copy per D-01..D-04):

    - Outer `<div className="door">` containing two children.
    - First child — recruiter half — `<div className="door-half door-half-recruiter" role="button" tabIndex={0} onClick={onChooseRecruiter} onKeyDown={handleRecruiterKey}>` where `handleRecruiterKey = (e: React.KeyboardEvent<HTMLDivElement>) => { if (e.key === 'Enter') { onChooseRecruiter(); } else if (e.key === ' ') { e.preventDefault(); onChooseRecruiter(); } }`. The handler can be inline or defined just inside the component body. The Space branch MUST call `e.preventDefault()` BEFORE the callback (Pitfall 3).
      Contents of recruiter half in order:
        - `<div className="door-bg-pattern" aria-hidden="true" />`
        - `<div className="door-id"><span className="door-id-mark"><LogoMark size={16} /></span><span className="door-id-wordmark">ruslan.kanatbek</span></div>` (D-04, D-06)
        - `<div className="door-eyebrow"><span className="dot" aria-hidden="true" /><span>For recruiters &amp; hiring partners</span></div>`
        - `<div className="door-body"><h1 className="door-name">Ruslan <em>Kanatbek</em></h1><div className="door-tagline">A readable, single-column résumé. The career, the numbers, how to reach me.</div></div>` (D-01, D-02)
        - `<div className="door-foot"><span className="door-cta"><span>Enter the résumé</span><span className="arrow" aria-hidden="true">→</span></span><span>≈ 90 sec read</span></div>` (D-04)
    - Second child — IDE half — `<div className="door-half door-half-ide" role="button" tabIndex={0} onClick={onChooseIDE} onKeyDown={handleIDEKey}>` with a parallel `handleIDEKey` doing the same Enter/Space branching but invoking `onChooseIDE`.
      Contents of IDE half in order:
        - `<div className="door-id"><span className="door-id-mark"><LogoMark size={16} /></span><span className="door-id-wordmark">ruslan.kanatbek</span></div>` (no bg-pattern on the IDE half per resume.css)
        - `<div className="door-eyebrow"><span className="dot" aria-hidden="true" /><span>For engineering leads &amp; technical interviewers</span></div>`
        - `<div className="door-body"><h1 className="door-name">ruslan<span className="accent">.</span>kanatbek</h1><div className="door-tagline">Open the files, run the smoke test, ask the agent.</div></div>` (D-03, D-04 wordmark pattern)
        - `<div className="door-foot"><span className="door-cta"><span>$ ./open-ide</span><span className="arrow" aria-hidden="true">→</span></span><span>react · ide</span></div>` (D-04)

    Do NOT use inline `style={{ ... }}` for any color or animation value — all styling via classNames already defined in globals.css (Pattern Map convention). No `useState`, no `useEffect`, no `useRef`, no localStorage access inside DoorScreen.

    3) Create `src/components/door/__tests__/DoorScreen.test.tsx` (new file). Start with the vitest globals reference triple-slash, then import `render` + `screen` from `@testing-library/react`, `userEvent` from `@testing-library/user-event`, and `vi` from `vitest`. Import `DoorScreen` from `../DoorScreen`. Define `const defaultProps = { onChooseRecruiter: vi.fn(), onChooseIDE: vi.fn() }`. Add `beforeEach(() => { vi.clearAllMocks(); });`.

    Tests required (cover DOOR-01 + DOOR-02):

    `describe('DoorScreen — rendering')`:
      - `it('renders two role=button halves')`: render `<DoorScreen {...defaultProps} />`, then `expect(screen.getAllByRole('button')).toHaveLength(2)`.
      - `it('recruiter half shows the D-02 tagline and Enter CTA')`: assert `screen.getByText(/A readable, single-column résumé\./i)` is in document and `screen.getByText(/Enter the résumé/i)` is in document.
      - `it('IDE half shows the D-03 tagline and open-ide CTA')`: assert `screen.getByText(/Open the files, run the smoke test, ask the agent\./i)` and `screen.getByText(/\$ \.\/open-ide/i)`.
      - `it('wordmark ruslan.kanatbek appears twice (one per half)')`: `expect(screen.getAllByText('ruslan.kanatbek')).toHaveLength(2)`.

    `describe('DoorScreen — click interactions')`:
      - `it('clicking recruiter half calls onChooseRecruiter')`: render, click first button (`screen.getAllByRole('button')[0]`), assert `defaultProps.onChooseRecruiter` was called once and `onChooseIDE` was NOT called.
      - `it('clicking IDE half calls onChooseIDE')`: parallel for second button.

    `describe('DoorScreen — keyboard interactions')`:
      - `it('Enter on focused recruiter half calls onChooseRecruiter')`: render, `await user.tab()` once, `await user.keyboard('{Enter}')`, assert recruiter callback called once.
      - `it('Space on focused recruiter half calls onChooseRecruiter and prevents default')`: render, `await user.tab()`, `await user.keyboard(' ')`, assert recruiter callback called once. (preventDefault is implicit — RTL fires the synthetic event; the callback being called is the observable behavior we test for.)
      - `it('Enter on focused IDE half calls onChooseIDE')`: render, `await user.tab()` twice, `await user.keyboard('{Enter}')`, assert IDE callback called once.

    Use the `userEvent.setup()` pattern from Sidebar.test.tsx — `const user = userEvent.setup();` inside each interaction `it` block.

    4) Edit `src/app/page.tsx`:
      - Add the import line `import { DoorScreen } from '@/components/door/DoorScreen';` immediately after the existing `import { SiteHeader } from '@/components/layout/SiteHeader';` line (line 6 in the current file).
      - Replace the entire `mode === null` `return (...)` block (currently lines 76-93, starting at the comment `// mode === null: Phase 8 will replace this stub with the full Door component`) with:
        - Remove the placeholder comment.
        - Return `<DoorScreen onChooseRecruiter={() => { try { localStorage.setItem(MODE_KEY, 'recruiter'); } catch {} setMode('recruiter'); }} onChooseIDE={() => { try { localStorage.setItem(MODE_KEY, 'ide'); } catch {} setMode('ide'); }} />`.
      - Do NOT change the early-return for `!mounted`, the `mode === 'ide'` branch (SiteHeader + IDEShell), or the `mode === 'recruiter'` stub (Phase 9 owns that).
      - Do NOT add new top-level helpers (the existing `readStoredMode` / `clearStoredMode` / `MODE_KEY` are already in scope).
  </action>
  <verify>
    <automated>pnpm tsc --noEmit && pnpm vitest run src/components/door/__tests__/DoorScreen.test.tsx && pnpm lint && grep -q "^'use client';" src/components/door/DoorScreen.tsx && ! grep -q "^'use client';" src/components/door/LogoMark.tsx && grep -q "export function DoorScreen" src/components/door/DoorScreen.tsx && grep -q "export function LogoMark" src/components/door/LogoMark.tsx && grep -q "import { DoorScreen } from '@/components/door/DoorScreen';" src/app/page.tsx && grep -q "<DoorScreen" src/app/page.tsx && ! grep -q "Landing door — coming in Phase 8" src/app/page.tsx && grep -q "e.preventDefault()" src/components/door/DoorScreen.tsx</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/door/LogoMark.tsx` exists; first non-comment line is `export function LogoMark` or `interface LogoMarkProps`; no `'use client'` directive (`grep -c "^'use client';" src/components/door/LogoMark.tsx` returns `0`)
    - File `src/components/door/DoorScreen.tsx` exists; first line is `'use client';`; imports `LogoMark` from `./LogoMark`; exports named `DoorScreen` function
    - DoorScreen renders exactly two elements with `role="button"` (assertion in unit test)
    - DoorScreen contains literal strings `ruslan.kanatbek`, `A readable, single-column résumé.`, `Open the files, run the smoke test, ask the agent.`, `Enter the résumé`, `$ ./open-ide`, `≈ 90 sec read`, `For recruiters & hiring partners`, `For engineering leads & technical interviewers` — all verified by `grep` in the file
    - `e.preventDefault()` appears in `onKeyDown` Space branch for both halves: `grep -c "e\.preventDefault()" src/components/door/DoorScreen.tsx` returns at least `2`
    - File `src/components/door/__tests__/DoorScreen.test.tsx` exists; all eight tests above pass
    - `pnpm vitest run src/components/door/__tests__/DoorScreen.test.tsx` exits `0` with 8 passing tests (3 rendering checks may be merged but minimum count is 8 across all describe blocks — fewer is acceptable only if every behavior listed above is covered)
    - `src/app/page.tsx` contains the new import (`grep -c "import { DoorScreen } from '@/components/door/DoorScreen';" src/app/page.tsx` returns `1`) and contains `<DoorScreen` exactly once (`grep -c "<DoorScreen" src/app/page.tsx` returns `1`)
    - The placeholder string `Landing door — coming in Phase 8` no longer appears (`grep -c "Landing door — coming in Phase 8" src/app/page.tsx` returns `0`)
    - The `mode === 'recruiter'` Phase-9 stub is unchanged: `grep -c "Recruiter view — coming in Phase 9" src/app/page.tsx` returns `1`
    - The IDE branch is unchanged: `grep -c "<SiteHeader />" src/app/page.tsx` returns `1` and `grep -c "<IDEShell />" src/app/page.tsx` returns `1`
    - `pnpm tsc --noEmit` exits `0`
    - `pnpm lint` exits `0` (jsx-a11y/interactive-supports-focus and jsx-a11y/click-events-have-key-events both satisfied by the `role="button"` + `tabIndex={0}` + `onKeyDown` triple)
  </acceptance_criteria>
  <done>
    LogoMark and DoorScreen components exist with the required exports and structure. DoorScreen is wired into `ResumeGateInner` so a fresh visitor (no localStorage) at `/` will see the cream/dark split. All eight (or more) unit tests pass. TypeScript and ESLint are clean. DOOR-01 and DOOR-02 are validated by unit tests; DOOR-03 and DOOR-04 are validated end-to-end in Plan 08-02 against the wired component.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser ↔ localStorage | Client-side persistence of mode choice; no server involvement |

Phase 8 introduces no API routes, no user input fields, no LLM calls, no third-party network requests, no file uploads, no eval/HTML injection. The only mutable state crosses the Browser ↔ localStorage boundary via the existing `readStoredMode()` / `setItem(MODE_KEY, ...)` callbacks. DoorScreen itself is purely presentational (no storage access).

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-08-01 | Tampering | localStorage['resume-mode'] (read) | mitigate | `readStoredMode()` in `src/app/page.tsx` already validates against the literal enum `'recruiter' \| 'ide'`; any other value returns `null` and the door is shown. No additional work needed in Plan 08-01 — the gate is already in place from Phase 7. |
| T-08-02 | Tampering | localStorage['resume-mode'] (write) | mitigate | DoorScreen's callbacks in `page.tsx` write only the literal strings `'recruiter'` and `'ide'` — no user-controlled values reach the setItem call. Closed under read-side enum validation (T-08-01). |
| T-08-03 | Information Disclosure | localStorage contents | accept | Mode choice is non-sensitive (audience preference). No PII or secrets stored. Accept. |
| T-08-04 | Denial of Service | flex-grow hover animation | accept | Pure CSS animation gated behind `prefers-reduced-motion: no-preference`; no JS animation loop, no resource accumulation. Worst case on older Safari is an instant jump (cosmetic, not functional). |
| T-08-05 | Spoofing | role="button" elements | accept | Each half has visible text content (CTA + tagline) that satisfies WCAG 4.1.2 Name, Role, Value. Screen readers announce them as buttons with their full text label. |
| T-08-SC | Tampering | npm/pip/cargo installs | mitigate | Phase 8 installs ZERO new packages (verified in 08-RESEARCH.md "Package Legitimacy Audit"). No supply-chain surface introduced. No checkpoint needed. |

**ASVS Level 1 categories applicable:** None require new controls — Phase 8 has no auth, no session management, no protected resources, no input validation surface beyond the already-validated localStorage enum read.
</threat_model>

<verification>
After all three tasks complete, run:

```
pnpm tsc --noEmit
pnpm lint
pnpm vitest run src/components/door
pnpm next build
```

All four commands must exit `0`. The Vitest run must report at least 8 passing tests for DoorScreen. The `next build` confirms the new component renders without SSR errors (DoorScreen is `'use client'` but is imported from the already-client `page.tsx`).

Manual smoke (post-implementation, before Plan 08-02):
1. `pnpm dev`, open `http://localhost:3000` in a fresh-localStorage Chrome window — door should render full-viewport with cream left + dark right.
2. Hover left half — both halves should shrink slightly, hovered half should expand smoothly.
3. Tab once — recruiter half receives focus outline; press Enter — page switches to recruiter stub (cream "Recruiter view — coming in Phase 9").
4. Refresh — recruiter stub stays (localStorage persisted).
5. Visit `/?reset` — door returns.
6. From door, click IDE half — IDEShell renders with SiteHeader.

These manual checks are tracked in Plan 08-02 as automated Playwright tests; they are listed here only as developer-confidence smoke during the build.
</verification>

<success_criteria>
- All three tasks complete; `pnpm vitest run src/components/door` exits `0` with 8+ passing tests covering DOOR-01 (renders two role=button halves, exact copy strings) and DOOR-02 (click + Enter + Space callbacks)
- `src/app/page.tsx` renders `<DoorScreen>` in the `mode === null` branch — placeholder string `Landing door — coming in Phase 8` is gone
- `src/app/globals.css` contains all 13 cream/forest tokens, `.door` rules with `height: 100%`, flex-grow transition, `prefers-reduced-motion` gate, and ≤760px media query
- `src/app/layout.tsx` exposes Newsreader weights `300/400/500/600/700`
- `pnpm tsc --noEmit`, `pnpm lint`, and `pnpm next build` all exit `0`
- LogoMark exports a parameterized SVG; DoorScreen exports the door component; both use named exports per project convention
</success_criteria>

<output>
After implementation, write a brief summary to `.planning/phases/08-landing-door/08-01-SUMMARY.md` capturing: files created/modified, the eight unit tests added, the Newsreader weight expansion, any deviations from the plan (e.g., extra test cases), and the smoke-verified state of the door at `/`. Reference the SUMMARY format at `$HOME/.claude/get-shit-done/templates/summary.md`.
</output>
