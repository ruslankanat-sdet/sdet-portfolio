---
phase: 03-playwright-showcase-hardening
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - src/lib/files-data.ts
  - src/components/ide/Sidebar.tsx
autonomous: true
requirements:
  - SHOW-02

must_haves:
  truths:
    - "IDE sidebar displays a tests/ folder containing 5 entries: 4 spec files plus playwright.config.ts"
    - "Clicking any tests/ entry loads the actual TypeScript source from e2e/ into the editor with TS syntax highlighting"
    - "SAMPLE_LOGS in files-data.ts uses real Playwright test names (no leftover Python pytest names)"
    - "Plan 01 ide-interactions.spec.ts assertions for tests folder + spec file click turn green"
  artifacts:
    - path: "src/lib/files-data.ts"
      provides: "FILES extended with 5 tests/ entries containing real e2e source; SAMPLE_LOGS replaced with Playwright-style entries"
      contains: "tests/landing.spec.ts"
    - path: "src/components/ide/Sidebar.tsx"
      provides: "tests/ folder group rendered with collapsible behavior identical to about/ folder"
      contains: "TEST_FILES"
  key_links:
    - from: "src/components/ide/Sidebar.tsx"
      to: "src/lib/files-data.ts"
      via: "FILES record lookup in fileRow()"
      pattern: "FILES\\[name\\]"
    - from: "src/components/ide/Sidebar.tsx"
      to: "tests/ open state"
      via: "useState open object extension"
      pattern: "tests:"
---

<objective>
Wire the four Playwright spec files plus playwright.config.ts (all created in plan 01) into the IDE sidebar under a new tests/ folder group, and replace the stale Python-style SAMPLE_LOGS with Playwright-format log entries. This satisfies SHOW-02 (test files listed in the IDE sidebar; clicking shows TypeScript source) and turns the deferred ide-interactions.spec.ts assertions from plan 01 green.

Purpose: The headline of the whole portfolio is that visitors see real tests, not pseudo-tests. Plan 01 wrote the specs; this plan makes them visible. Without this plan, the specs exist as repo files invisible to any recruiter who lands on the deployed site. The README.md content already promises "Run Smoke Test in the topbar -> watch this site test itself" — this plan delivers the file-explorer half of that promise (the button half lands in plan 05).

Output: A tests/ folder in the sidebar with five clickable entries, each loading the real spec source into the editor; the terminal pane shows realistic Playwright pass/fail logs on first paint.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-playwright-showcase-hardening/03-CONTEXT.md
@.planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md
@.planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md
@.planning/phases/03-playwright-showcase-hardening/03-01-SUMMARY.md
@CLAUDE.md
@src/lib/files-data.ts
@src/components/ide/Sidebar.tsx
@src/types/ide.ts

<interfaces>
From src/types/ide.ts (no changes needed — both types already support what we need):
- FileEntryLang union includes the string 'typescript'.
- FileEntryIcon union includes the string 'ts'.
- FileEntry shape: lang (FileEntryLang) + path (string) + icon (FileEntryIcon) + content (string).
- LogEntry shape: kind (LogKind) + optional text/test/detail strings.
- LogKind union: info, warn, ok, pass, fail.

From src/components/ide/Sidebar.tsx (existing patterns to mirror exactly):
- ICONS_FOR_FILE record maps icon-name to lucide icon component; 'ts' already maps to FileCode2 (line 35).
- ABOUT_FILES is a string[] array of file names (line 39).
- The open state object (line 52) currently has shape { about: boolean }; extend to { about: boolean, tests: boolean }.
- The about/ folder JSX block (lines 146-165) is the exact template for the new tests/ folder block.
- fileRow(name) helper (lines 55-74) reads FILES[name]; new tests/ entries appear automatically once added to FILES.
- OUTLINE_SYMBOLS record (lines 43-49) provides decorative outline items keyed by file name.

From src/lib/files-data.ts:
- FILES is a Record<string, FileEntry> with sibling entries like bio.json (lines 35-64) — copy this exact shape for each test file entry. The key in FILES is the same string passed to fileRow(name) in Sidebar.tsx.
- SAMPLE_LOGS array (lines 149-164) currently has Python-style test names that must be replaced wholesale per key_technical_facts #5.

From plan 01 deliverables:
- e2e/landing.spec.ts (path on disk: e2e/landing.spec.ts; sidebar key: tests/landing.spec.ts)
- e2e/navigation.spec.ts
- e2e/about.spec.ts
- e2e/ide-interactions.spec.ts
- playwright.config.ts (path on disk: playwright.config.ts at repo root; sidebar key: tests/playwright.config.ts per D-04)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend FILES with 5 tests/ entries and replace SAMPLE_LOGS</name>
  <files>src/lib/files-data.ts</files>
  <read_first>
    - src/lib/files-data.ts (existing bio.json entry lines 35-64 — copy shape exactly; existing SAMPLE_LOGS lines 149-164 — to be replaced wholesale)
    - e2e/landing.spec.ts (read full content — it becomes the inline content field of FILES['tests/landing.spec.ts'])
    - e2e/navigation.spec.ts (read full content)
    - e2e/about.spec.ts (read full content)
    - e2e/ide-interactions.spec.ts (read full content)
    - playwright.config.ts (read full content)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (src/lib/files-data.ts section — exact new-entry shape, exact SAMPLE_LOGS replacement)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 7 — SAMPLE_LOGS replacement content; key_technical_facts #5 — Python-to-Playwright name mapping)
  </read_first>
  <action>
    Two changes in this single file, both following existing patterns.

    Change 1: Add five new entries to the FILES record. Place them after the existing contact.json entry (after line 146, before the SAMPLE_LOGS export). Each entry follows the same shape as bio.json — lang, path, icon, content. Per D-04, all five sidebar keys live under the tests/ prefix even though the source files live at e2e/ and the repo root.

    Required entries:
    1. Key tests/landing.spec.ts — lang typescript, path tilde-slash portfolio/e2e/landing.spec.ts, icon ts, content equal to the verbatim contents of e2e/landing.spec.ts (preserve all imports, describe blocks, axe scan). Use a template literal so the embedded backticks and dollar signs from the spec file render correctly; escape backticks inside the spec with backslash-backtick. If the spec source contains no template literals it can be embedded as a plain backtick-delimited string.
    2. Key tests/navigation.spec.ts — same pattern, content equal to e2e/navigation.spec.ts contents.
    3. Key tests/about.spec.ts — same pattern, content equal to e2e/about.spec.ts contents.
    4. Key tests/ide-interactions.spec.ts — same pattern, content equal to e2e/ide-interactions.spec.ts contents.
    5. Key tests/playwright.config.ts — lang typescript, path tilde-slash portfolio/playwright.config.ts, icon ts, content equal to the verbatim contents of playwright.config.ts at the repo root.

    Per Q3 in RESEARCH.md Open Questions (hardcode vs build-time fetch decision), embed the file contents inline as string literals. Do NOT use Node fs.readFileSync at module evaluation time — files-data.ts is bundled into the client and fs imports fail in the browser. The tradeoff (manual re-paste when specs change) is acceptable for v1; a future phase can introduce a build-time codegen step that reads e2e/ and writes a generated file.

    Change 2: Replace the entire SAMPLE_LOGS array (lines 149-164) with Playwright-style entries per key_technical_facts #5 and RESEARCH.md Pattern 7. Required entries in order:
    1. kind info, text $ pnpm exec playwright test --project=chromium --reporter=line
    2. kind info, text Running 12 tests using 4 workers
    3. kind pass, test landing.spec.ts > has correct page title, detail 312ms
    4. kind pass, test landing.spec.ts > shows IDE chrome on load, detail 188ms
    5. kind pass, test landing.spec.ts > sidebar README.md row loads README content into editor, detail 412ms
    6. kind pass, test navigation.spec.ts > About link navigates to /about, detail 241ms
    7. kind pass, test navigation.spec.ts > logo/home link returns to /, detail 198ms
    8. kind pass, test about.spec.ts > renders work history section, detail 156ms
    9. kind pass, test about.spec.ts > PDF download link is present, detail 124ms
    10. kind pass, test ide-interactions.spec.ts > clicking bio.json loads editor, detail 298ms
    11. kind pass, test landing.spec.ts > has no WCAG AA violations, detail 892ms
    12. kind pass, test about.spec.ts > has no WCAG AA violations, detail 743ms
    13. kind info, text a horizontal rule of em-dashes
    14. kind ok, text checkmark 12 passed (4.2s) dot 0 failed dot 0 flaky

    All test name strings must reference real test cases that exist in the plan-01 spec files (verify by grepping the e2e/*.spec.ts files for the matching test('...') strings). Do NOT keep ANY Python pytest naming (no test_resmed_sdet.py, no test_playwright_expertise.py, etc.).

    Do NOT change the FILES entries for README.md, bio.json, experience.yaml, skills.yaml, contact.json — those are Phase 1 + 2 deliverables and must remain untouched. Do NOT add Vitest log entries. Do NOT add the self-healing selector warn entry from the old SAMPLE_LOGS — it referenced a fictional self-healing feature that the v1 portfolio does not have.
  </action>
  <verify>
    <automated>grep -q "'tests/landing.spec.ts'" src/lib/files-data.ts &amp;&amp; grep -q "'tests/navigation.spec.ts'" src/lib/files-data.ts &amp;&amp; grep -q "'tests/about.spec.ts'" src/lib/files-data.ts &amp;&amp; grep -q "'tests/ide-interactions.spec.ts'" src/lib/files-data.ts &amp;&amp; grep -q "'tests/playwright.config.ts'" src/lib/files-data.ts &amp;&amp; grep -q "lang: 'typescript'" src/lib/files-data.ts &amp;&amp; grep -q "pnpm exec playwright test" src/lib/files-data.ts &amp;&amp; ! grep -q "test_resmed_sdet" src/lib/files-data.ts &amp;&amp; ! grep -q "test_playwright_expertise" src/lib/files-data.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    src/lib/files-data.ts contains exactly 5 new FILES entries keyed tests/landing.spec.ts, tests/navigation.spec.ts, tests/about.spec.ts, tests/ide-interactions.spec.ts, tests/playwright.config.ts; each entry uses lang typescript and icon ts; each content field is the verbatim source of the matching file from e2e/ or repo root; SAMPLE_LOGS contains exactly 14 entries with Playwright-format test names (no Python pytest names remain); pnpm typecheck exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Add tests/ folder group to Sidebar.tsx</name>
  <files>src/components/ide/Sidebar.tsx</files>
  <read_first>
    - src/components/ide/Sidebar.tsx (existing ABOUT_FILES constant line 39, open state line 52, about/ folder JSX block lines 146-165, OUTLINE_SYMBOLS record lines 43-49, fileRow helper lines 55-74 — copy these patterns verbatim)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (src/components/ide/Sidebar.tsx section — exact TEST_FILES + open-state extension + folder JSX + OUTLINE_SYMBOLS additions)
    - src/lib/files-data.ts (after task 1 — confirm the 5 new tests/ keys exist; fileRow(name) reads FILES[name] so the keys must match exactly)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pitfall 4 — new FILES entries do not appear in sidebar unless the corresponding name array exists)
  </read_first>
  <action>
    Three changes in this single file, each mirroring an existing about/ pattern.

    Change 1: Add a TEST_FILES constant immediately after ROOT_FILES (after line 40). It is a string[] array containing the five sidebar keys in display order: tests/landing.spec.ts, tests/navigation.spec.ts, tests/about.spec.ts, tests/ide-interactions.spec.ts, tests/playwright.config.ts. The order matches the order recruiters most likely want to see — landing first, config last.

    Change 2: Extend the open state initial value (line 52) from { about: false } to { about: false, tests: false }. Both folders start collapsed on first load; the user expands them by clicking. Do NOT default tests to true — that would push the about/ folder out of the viewport on narrow widths.

    Change 3: Add a new tests/ folder JSX block immediately after the about/ folder block (after line 165, before the Root-level files block at line 168). Copy the about/ block (lines 146-165) verbatim and substitute four identifiers:
    - The setOpen callback toggles s.tests instead of s.about — open.tests now controls the rotation transform and the children visibility.
    - The ChevronRight transform reads open.tests instead of open.about.
    - The treeLabel span text is the literal string tests instead of about.
    - The children render TEST_FILES.map(name => fileRow(name)) instead of ABOUT_FILES.map.

    Per Pitfall 4 in RESEARCH.md, this triplet (TEST_FILES + open.tests + tests folder JSX) is the minimum required for new FILES entries to appear in the sidebar. fileRow already exists; ICONS_FOR_FILE already maps ts to FileCode2; no other changes needed.

    Change 4 (decorative): Add five entries to OUTLINE_SYMBOLS record (after line 48, before the closing brace on line 49). Per RESEARCH.md Pattern 7 and PATTERNS.md, these outline labels surface the test names a recruiter would see by opening each file. Required entries:
    - Key tests/landing.spec.ts -> array with two items: label has correct page title kind key, label has no WCAG AA violations kind key.
    - Key tests/navigation.spec.ts -> array with one item: label About link navigates to /about kind key.
    - Key tests/about.spec.ts -> array with two items: label renders work history section kind key, label has no WCAG AA violations kind key.
    - Key tests/ide-interactions.spec.ts -> array with two items: label clicking bio.json loads editor kind key, label test files appear in sidebar kind key.
    - Key tests/playwright.config.ts -> array with three items: label testDir kind key, label projects kind key, label webServer kind key.

    Do NOT change the activityBar buttons, the explorerHeader, the outline rendering block, fileRow, or any styles. Do NOT add a new lucide icon — Folder + FileCode2 already cover everything.
  </action>
  <verify>
    <automated>grep -q "TEST_FILES" src/components/ide/Sidebar.tsx &amp;&amp; grep -q "'tests/landing.spec.ts'" src/components/ide/Sidebar.tsx &amp;&amp; grep -q "'tests/playwright.config.ts'" src/components/ide/Sidebar.tsx &amp;&amp; grep -q "tests: false" src/components/ide/Sidebar.tsx &amp;&amp; grep -q "open.tests" src/components/ide/Sidebar.tsx &amp;&amp; grep -q "tests/landing.spec.ts" src/components/ide/Sidebar.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <done>
    src/components/ide/Sidebar.tsx contains TEST_FILES array with the 5 keys in display order; the open useState initial value is { about: false, tests: false }; a tests/ folder JSX block sits between the about/ block and the Root-level files block, mirroring the about/ pattern exactly; OUTLINE_SYMBOLS has 5 new entries keyed under tests/; pnpm typecheck and pnpm lint both exit 0; pnpm dev followed by visiting / shows the tests folder in the sidebar; clicking the tests label expands it; clicking landing.spec.ts loads the spec source into the editor with TypeScript syntax highlighting; plan 01 ide-interactions.spec.ts assertions for tests folder visibility and spec file click pass when re-run.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Sidebar component -> files-data module | All file content is statically bundled at build time; no runtime fetch |
| Browser -> editor pane | Spec source displayed via existing Shiki renderer (Phase 1); no eval or HTML injection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-XSS | Tampering | Spec source rendered in editor | accept | Existing EditorArea.tsx (Phase 1) renders content via Shiki which emits safe HTML; no dangerouslySetInnerHTML on user-controlled strings. Per CLAUDE.md anti-pattern rule. |
| T-03-Bundle | Information Disclosure | files-data.ts client bundle size | accept | Five new TypeScript spec strings add roughly 4-6 KB to the client bundle (specs are small, no secrets). Acceptable for a portfolio whose entire value prop is "see the code." Future optimization (lazy-load per-file via dynamic import) deferred. |
</threat_model>

<verification>
- pnpm typecheck exits 0.
- pnpm lint exits 0.
- pnpm dev followed by visiting / shows the tests folder in the sidebar (collapsed by default).
- Clicking the tests folder header expands it to reveal 5 entries.
- Clicking each entry loads its real source into the editor with TypeScript syntax highlighting.
- The Terminal pane on first paint shows the new Playwright-format SAMPLE_LOGS entries (12 passing tests in 4.2 seconds).
- Re-running plan 01 ide-interactions.spec.ts assertions for tests folder + spec file click turns them green.
</verification>

<success_criteria>
- src/lib/files-data.ts has 5 new tests/ FILES entries with verbatim source from e2e/ + repo root, and SAMPLE_LOGS replaced with 14 Playwright-format entries.
- src/components/ide/Sidebar.tsx renders a tests/ folder group with collapsible behavior identical to about/.
- All Python pytest names removed from SAMPLE_LOGS.
- All 14 SAMPLE_LOGS test names reference real test cases that exist in plan-01 spec files.
- Plan 01 ide-interactions.spec.ts deferred assertions turn green.
</success_criteria>

<output>
Create .planning/phases/03-playwright-showcase-hardening/03-03-SUMMARY.md documenting:
- Files modified (2: files-data.ts + Sidebar.tsx)
- Number of FILES entries added (5)
- Sidebar groups now present (root-level, about/, tests/)
- Plan 01 assertions newly green after this plan ships
</output>
