---
phase: 03-playwright-showcase-hardening
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/og/route.tsx
  - src/app/layout.tsx
  - src/app/about/page.tsx
  - public/robots.txt
  - public/sitemap.xml
autonomous: true
requirements:
  - HARD-03
  - HARD-04

must_haves:
  truths:
    - "GET /og returns 200 with a 1200x630 PNG using design-token hex colors"
    - "Landing and /about HTML each include og:image and twitter:card meta tags"
    - "GET /robots.txt returns 200 body containing User-agent and a Sitemap line"
    - "GET /sitemap.xml returns 200 body containing both / and /about urls"
    - "pnpm build emits no metadataBase warnings or errors"
  artifacts:
    - path: "src/app/og/route.tsx"
      provides: "Dynamic OG image generation at /og via next/og ImageResponse"
      contains: "ImageResponse"
    - path: "public/robots.txt"
      provides: "Crawler directives + sitemap pointer"
      contains: "Sitemap:"
    - path: "public/sitemap.xml"
      provides: "Two-URL sitemap (landing + about)"
      contains: "urlset"
  key_links:
    - from: "src/app/layout.tsx"
      to: "src/app/og/route.tsx"
      via: "openGraph.images entry resolved via metadataBase"
      pattern: "images.*url.*'/og'"
    - from: "src/app/about/page.tsx"
      to: "src/app/og/route.tsx"
      via: "page-level metadata override referencing /og"
      pattern: "openGraph"
---

<objective>
Add Open Graph + Twitter Card meta to the landing and /about routes (HARD-03), publish a dynamic OG image at /og using next/og ImageResponse, and ship static robots.txt + sitemap.xml in public/ (HARD-04). These are pure-additive, file-isolated changes — no shared files with plan 01, so this plan runs in wave 1 alongside it.

Purpose: Without OG meta, sharing the portfolio URL in Slack/LinkedIn produces a bare-link card — a recruiter scanning a feed scrolls past it. Without robots.txt + sitemap.xml, the site is harder to index. Both are launch-blocking items per ROADMAP Phase 3 success criteria 4 and 5.

Output: A live /og route returning a branded 1200x630 PNG, full OG + Twitter Card tags on both pages, and two static SEO files in public/.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-playwright-showcase-hardening/03-CONTEXT.md
@.planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md
@.planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md
@CLAUDE.md
@src/app/layout.tsx
@src/app/about/page.tsx
@.planning/design/design_handoff_ide_portfolio/styles.css

<interfaces>
Key contracts the executor needs.

From next/og (bundled with next@15.5.18) — class ImageResponse extends Response, constructor takes a ReactElement and an options object with optional width and height numbers.

From next (already imported on layout.tsx line 2) — the Metadata type supports metadataBase (URL), title, description, openGraph (title, description, url, siteName, images array, locale, type), twitter (card, title, description, images).

Design token hex values (resolved from .planning/design/design_handoff_ide_portfolio/styles.css per key_technical_facts #3 — CSS vars do NOT work inside ImageResponse):
- Background hex: 0b1117 (token --bg-deep)
- Accent green hex: 3ddc84 (token --green)
- Muted text hex: 8b96a8 (token --text-muted)
- Link blue hex: 79b8ff (token --blue)
- Primary text hex: e2e8f0 (token --text-primary per key_technical_facts #3; RESEARCH.md cites e6edf3 for --text — use e2e8f0 per key_technical_facts as the authoritative override)

Site canonical URL: env var NEXT_PUBLIC_SITE_URL with fallback string https://ruslankanat.dev per key_technical_facts #4. (RESEARCH.md suggests ruslankanat.vercel.app as fallback; key_technical_facts #4 overrides with ruslankanat.dev — use ruslankanat.dev as the literal fallback string in code AND in the static SEO files.)
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /og route handler with ImageResponse</name>
  <files>src/app/og/route.tsx</files>
  <read_first>
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (app/og/route.tsx section — full ImageResponse JSX template)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 5; Pitfall 1 on CSS vars; Anti-Patterns on force-static)
    - .planning/design/design_handoff_ide_portfolio/styles.css (confirm hex values match the interfaces table above)
    - src/app/layout.tsx (confirm there is no existing app/og/ directory)
  </read_first>
  <action>
    Create new file src/app/og/route.tsx. This is a Route Handler (not a Page) under the App Router; it must export a GET function and may export runtime / revalidate constants.

    Required exports (top of file):
    - Import ImageResponse from next/og.
    - Export const runtime equal to the string nodejs — per CLAUDE.md (Node, not Edge, for tool routes) and RESEARCH.md shared pattern. RESEARCH.md Pattern 5 notes Edge also works for ImageResponse, but the project convention is Node.
    - Export const revalidate equal to 86400 — 24-hour cache per RESEARCH.md anti-pattern note (force-static conflicts with ImageResponse — use revalidate instead).

    GET handler returns a new ImageResponse(jsx, options) where options contains width 1200 and height 630. JSX content per RESEARCH.md Pattern 5 and key_technical_facts #3:
    - Root div with inline style — width 100%, height 100%, display flex, flexDirection column, justifyContent center, padding 60px, backgroundColor hex 0b1117, color hex e2e8f0, fontFamily monospace — inline hex values ONLY; per Pitfall 1, CSS variables silently render wrong inside ImageResponse.
    - Child 1: text Ruslan Kanatbek at fontSize 72, fontWeight 700, color hex 3ddc84. Add a comment near the line naming the token (--green) so a future token update is traceable.
    - Child 2: text Senior SDET dot Playwright dot TypeScript at fontSize 32, marginTop 24, color hex 8b96a8, with comment naming token --text-muted.
    - Child 3: text ruslankanat.dev at fontSize 24, marginTop 40, color hex 79b8ff, with comment naming token --blue.

    Per T-03-02 (LOW): renders static hardcoded strings only, no user input — no request param parsing, no searchParams reads. The handler signature is async function GET with no request argument.

    Do NOT use Twemoji or external fonts in v1 (Satori only loads bundled defaults; adding a Google Font fetch in this route adds a network call to the build/runtime image generation). The monospace stack inside Satori falls back to a system mono; the design tradeoff is acceptable for the v1 OG card.

    Do NOT call the Image component from next/image inside ImageResponse — only plain JSX with inline styles is supported by Satori.
  </action>
  <verify>
    <automated>test -f src/app/og/route.tsx &amp;&amp; grep -q "from 'next/og'" src/app/og/route.tsx &amp;&amp; grep -q "ImageResponse" src/app/og/route.tsx &amp;&amp; grep -q "1200" src/app/og/route.tsx &amp;&amp; grep -q "630" src/app/og/route.tsx &amp;&amp; grep -q "0b1117" src/app/og/route.tsx &amp;&amp; grep -q "3ddc84" src/app/og/route.tsx &amp;&amp; grep -q "Ruslan Kanatbek" src/app/og/route.tsx &amp;&amp; grep -q "nodejs" src/app/og/route.tsx &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    src/app/og/route.tsx exists, exports GET returning an ImageResponse sized 1200x630 with the three text layers (name + title + URL) using the hex literals 0b1117, 3ddc84, 8b96a8, 79b8ff, e2e8f0; runtime equals nodejs and revalidate equals 86400 are exported; pnpm typecheck exits 0; running pnpm dev then visiting http://localhost:3000/og returns a 1200x630 PNG (verifiable via curl with -sI showing Content-Type image/png).
  </done>
</task>

<task type="auto">
  <name>Task 2: Wire metadataBase + OG + Twitter tags into layout.tsx and about/page.tsx</name>
  <files>src/app/layout.tsx, src/app/about/page.tsx</files>
  <read_first>
    - src/app/layout.tsx (current metadata export — lines 16-20; the Metadata type is already imported on line 2)
    - src/app/about/page.tsx (current metadata export — lines 9-13; Metadata already imported on line 1)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (src/app/layout.tsx, src/app/about/page.tsx sections — exact metadata object shape)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 6; Pitfall 5 on metadataBase requirement)
  </read_first>
  <action>
    Extend the existing metadata exports in both files. Do NOT delete the existing title/description fields — they remain unchanged. Add the new fields alongside.

    src/app/layout.tsx — replace the existing metadata export block (currently 5 lines) with the extended version. Required additions:
    - metadataBase: new URL(process.env.NEXT_PUBLIC_SITE_URL ?? 'https://ruslankanat.dev') — per key_technical_facts #4. This is the literal fallback string; RESEARCH.md's https://ruslankanat.vercel.app fallback is superseded by key_technical_facts.
    - openGraph object with: title equal to Ruslan Kanatbek SDET Portfolio, description equal to the existing description text from line 19, url equal to /, siteName equal to the same Ruslan Kanatbek SDET Portfolio title, images array with one entry containing url /og + width 1200 + height 630, locale en_US, type website. The relative /og path requires metadataBase to be set (Pitfall 5).
    - twitter object with: card equal to summary_large_image, title equal to Ruslan Kanatbek SDET Portfolio, description equal to the same description, images array containing the single string /og.

    Keep the existing title and description fields unchanged. Keep all other imports, the font setup, the RootLayout function body, and the Analytics + SpeedInsights placements untouched.

    src/app/about/page.tsx — replace the existing metadata block (lines 9-13) with an extended version. Required additions (page-level metadata inherits and overrides layout-level):
    - Keep title About Ruslan Kanatbek and the existing description unchanged.
    - Add openGraph with: title About Ruslan Kanatbek, description Senior SDET QA Automation Engineer Work history projects and skills, url /about, images array with one entry url /og + width 1200 + height 630, type website.
    - Add twitter with: card summary_large_image, title About Ruslan Kanatbek, images array containing /og.

    Do NOT add metadataBase to the about page — it inherits from layout (the openGraph.images relative /og resolves via the parent metadataBase). Adding it to the page would be redundant.

    Do NOT add Twitter creator handle — none specified in CONTEXT.md (Claude's Discretion item); leave off in v1.
  </action>
  <verify>
    <automated>grep -q "metadataBase" src/app/layout.tsx &amp;&amp; grep -q "NEXT_PUBLIC_SITE_URL" src/app/layout.tsx &amp;&amp; grep -q "ruslankanat.dev" src/app/layout.tsx &amp;&amp; grep -q "openGraph" src/app/layout.tsx &amp;&amp; grep -q "summary_large_image" src/app/layout.tsx &amp;&amp; grep -q "openGraph" src/app/about/page.tsx &amp;&amp; grep -q "/about" src/app/about/page.tsx &amp;&amp; grep -q "summary_large_image" src/app/about/page.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm build 2>&amp;1 | grep -i "metadataBase.*error" &amp;&amp; exit 1 || true</automated>
  </verify>
  <done>
    src/app/layout.tsx exports metadata with metadataBase (URL constructor wrapping NEXT_PUBLIC_SITE_URL env var with https://ruslankanat.dev fallback) plus full openGraph and twitter blocks pointing to /og; src/app/about/page.tsx adds page-level openGraph (with url /about) and twitter overrides; pnpm typecheck exits 0; pnpm build completes with no metadataBase warnings or errors; visiting view-source for http://localhost:3000/ after pnpm dev shows meta property og:image with content https://ruslankanat.dev/og (or the env-var URL if set) and meta name twitter:card with content summary_large_image.
  </done>
</task>

<task type="auto">
  <name>Task 3: Add public/robots.txt and public/sitemap.xml</name>
  <files>public/robots.txt, public/sitemap.xml</files>
  <read_first>
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (public/robots.txt, public/sitemap.xml sections — exact file contents)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (D-20 confirms static files, no next-sitemap library)
    - public/ directory listing (verify no existing robots.txt or sitemap.xml; both are new)
  </read_first>
  <action>
    Create two static text files inside public/. Per D-20 these are hand-written — no next-sitemap dependency. Per the user constraint of two pages in v1 (landing + about), the sitemap has exactly two url entries.

    public/robots.txt — three logical lines separated by blank lines:
    Line 1 — User-agent: asterisk
    Line 2 — Allow: /
    Blank line
    Line 4 — Sitemap: https://ruslankanat.dev/sitemap.xml

    Use the literal canonical URL https://ruslankanat.dev — robots.txt is a static file with no runtime env-var expansion. If the canonical domain changes later, this file is hand-updated.

    public/sitemap.xml — standard XML sitemap shape:
    - XML prolog with version 1.0 and encoding UTF-8.
    - Root element urlset with xmlns http://www.sitemaps.org/schemas/sitemap/0.9.
    - Child url element containing loc https://ruslankanat.dev/ + changefreq weekly + priority 1.0.
    - Child url element containing loc https://ruslankanat.dev/about + changefreq monthly + priority 0.8.
    - Close urlset.

    Per Claude's Discretion in CONTEXT.md, omit lastmod tags — hand-maintained dates go stale; absence is preferred to lying. Use weekly and monthly changefreq to match the rhythm of likely edits (landing more dynamic than resume).

    Both files: no trailing whitespace, single newline at EOF. No Disallow line in robots.txt (we want the whole site indexed; Allow / is sufficient). No image-extension or video-extension entries in sitemap.xml (deferred).

    Do NOT install next-sitemap — per RESEARCH.md Alternatives Considered, it adds a dev dependency for zero benefit on a 2-page site.
  </action>
  <verify>
    <automated>test -f public/robots.txt &amp;&amp; test -f public/sitemap.xml &amp;&amp; grep -q "User-agent" public/robots.txt &amp;&amp; grep -q "Sitemap:" public/robots.txt &amp;&amp; grep -q "ruslankanat.dev/sitemap.xml" public/robots.txt &amp;&amp; grep -q "urlset" public/sitemap.xml &amp;&amp; grep -q "ruslankanat.dev/about" public/sitemap.xml &amp;&amp; grep -q "ruslankanat.dev/" public/sitemap.xml</automated>
  </verify>
  <done>
    public/robots.txt contains a User-agent line, Allow: / line, and a Sitemap line pointing to https://ruslankanat.dev/sitemap.xml; public/sitemap.xml is a well-formed XML document with two url entries (landing + about) using the same canonical domain; after pnpm dev, curl http://localhost:3000/robots.txt returns 200 with the user-agent and sitemap lines, and curl http://localhost:3000/sitemap.xml returns 200 with both URLs; the new files allow plan 01 landing.spec.ts assertions for robots.txt + sitemap.xml accessibility to turn green.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public web -> /og route | Anonymous HTTP GETs reach the ImageResponse handler |
| Public web -> /robots.txt and /sitemap.xml | Anonymous GETs of static files served by Next.js/Vercel |
| Layout/page metadata -> social platform crawlers | OG/Twitter tags consumed by Slack/LinkedIn/Twitter to render preview cards |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-02 | Information Disclosure | /og route content | accept | Renders static hardcoded strings only (name + title + URL) — no user input parsed from query string or headers, so no injection surface. Documented in plan action notes. |
| T-03-02b | Denial of Service | /og route compute cost | mitigate | revalidate = 86400 (24h cache) per RESEARCH.md anti-pattern note; ImageResponse output is cached by Vercel's edge so repeat hits do not recompute the PNG. |
| T-03-02c | Tampering | metadataBase env var | mitigate | Fallback to literal string https://ruslankanat.dev if NEXT_PUBLIC_SITE_URL is unset — prevents accidental relative-URL deploys and protects OG image links from breaking on preview deploys. |
</threat_model>

<verification>
- pnpm typecheck exits 0.
- pnpm build completes with no metadataBase warnings.
- pnpm dev followed by GET /og returns Content-Type image/png with a non-empty body.
- view-source on / and /about both contain the og:image, og:title, og:description, twitter:card meta tags.
- GET /robots.txt returns 200 with the Sitemap line.
- GET /sitemap.xml returns 200 with both / and /about loc entries.
- After this plan lands, the four currently-red assertions in plan 01 landing.spec.ts (robots.txt, sitemap.xml, OG meta presence) turn green automatically.
</verification>

<success_criteria>
- src/app/og/route.tsx exports GET returning ImageResponse(1200, 630) with design-token hex colors inlined.
- src/app/layout.tsx exports metadata with metadataBase, openGraph, twitter blocks.
- src/app/about/page.tsx exports page-level metadata override with openGraph and twitter blocks.
- public/robots.txt and public/sitemap.xml exist and serve at /robots.txt and /sitemap.xml.
- Plan 01 robots/sitemap/OG meta tests turn green when run after this plan ships.
</success_criteria>

<output>
Create .planning/phases/03-playwright-showcase-hardening/03-02-SUMMARY.md documenting:
- Files created (1 route + 2 static + 2 modified)
- Hex values used (cross-referenced to design tokens)
- Canonical URL decision (ruslankanat.dev as fallback)
- Sample OG image preview path (http://localhost:3000/og)
</output>