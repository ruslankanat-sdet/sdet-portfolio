---
phase: 03-playwright-showcase-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - playwright.config.ts
  - e2e/landing.spec.ts
  - e2e/navigation.spec.ts
  - e2e/about.spec.ts
  - e2e/ide-interactions.spec.ts
  - package.json
  - pnpm-lock.yaml
  - .gitignore
autonomous: true
requirements:
  - SHOW-01
  - HARD-01
  - HARD-02

must_haves:
  truths:
    - "pnpm exec playwright test --project=chromium passes locally with zero failures"
    - "axe-core scan returns zero violations on landing and /about pages"
    - "Each Playwright spec covers a real user-facing flow (landing render, nav, about render, file-click-to-editor)"
  artifacts:
    - path: "playwright.config.ts"
      provides: "Single-chromium Playwright config with webServer auto-start"
      contains: "testDir: './e2e'"
    - path: "e2e/landing.spec.ts"
      provides: "Landing render, OG meta, robots/sitemap reachability, axe WCAG AA"
    - path: "e2e/navigation.spec.ts"
      provides: "Cross-page navigation flows (landing → about, about → landing)"
    - path: "e2e/about.spec.ts"
      provides: "About page render, PDF link present, axe WCAG AA"
    - path: "e2e/ide-interactions.spec.ts"
      provides: "Sidebar file click loads editor content (covers SHOW-02 happy path)"
  key_links:
    - from: "playwright.config.ts"
      to: "e2e/*.spec.ts"
      via: "testDir resolution"
      pattern: "testDir:\\s*'./e2e'"
    - from: "e2e/landing.spec.ts"
      to: "@axe-core/playwright"
      via: "default import"
      pattern: "import AxeBuilder from '@axe-core/playwright'"
---

<objective>
Install Playwright + @axe-core/playwright, scaffold playwright.config.ts at the repo root, and write the four E2E spec files that cover Phase 3 success criteria 1, 3, and the SHOW-01 functional flows.

Purpose: Real TypeScript Playwright tests of this site are the headline artifact of Phase 3. They are simultaneously (a) proof the site works, (b) the source files later displayed in the IDE sidebar (SHOW-02 — plan 03), and (c) the workload the CI job runs (HARD-05 — plan 04). Every later plan in Phase 3 depends on these specs existing.

Output: Working Playwright suite, configuration, browser binary installed via system package manager, four spec files exercising the public site, all axe-core WCAG AA scans passing.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-playwright-showcase-hardening/03-CONTEXT.md
@.planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md
@.planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md
@CLAUDE.md
@src/app/about/page.tsx
@src/components/ide/Sidebar.tsx
@src/types/ide.ts
@package.json

<interfaces>
<!-- Key contracts the executor needs. -->

From @axe-core/playwright (default export, version 4.11.3):
```typescript
export default class AxeBuilder {
  constructor(options: { page: Page });
  withTags(tags: string[]): this;
  analyze(): Promise<{ violations: Array<{ id: string; impact: string; nodes: unknown[] }> }>;
}
```

From @playwright/test (version 1.60.0):
```typescript
export const test: TestType;
export const expect: Expect;
export function defineConfig(config: PlaywrightTestConfig): PlaywrightTestConfig;
export const devices: Record<string, BrowserContextOptions>;
```

Site URLs the specs interact with:
- `/`             → IDE landing (title contains "Ruslan Kanatbek"; <main> visible; sidebar buttons present)
- `/about`        → resume page (download PDF link with text /Download PDF Resume/i, has `download` attribute)
- `/robots.txt`   → must be 200 once plan 02 ships (this plan only asserts shape, not 200; see action note)
- `/sitemap.xml`  → must be 200 once plan 02 ships

Existing aria-labels / accessible names already in DOM (do not invent new ones):
- `getByRole('button', { name: /README\.md/ })` — root README file row
- `getByRole('button', { name: /bio\.json/ })` — inside about/ folder (must expand folder first)
- `getByRole('link', { name: /About/i })` — SiteHeader nav link
- `getByRole('link', { name: /Download PDF Resume/i })` — about page download CTA
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Install Playwright tooling and scaffold playwright.config.ts</name>
  <files>package.json, pnpm-lock.yaml, playwright.config.ts, .gitignore</files>
  <read_first>
    - package.json (verify devDependencies block — confirm @playwright/test and @axe-core/playwright are NOT already present)
    - .gitignore (verify current ignore entries; we need to add Playwright artifacts)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 1 — playwright.config.ts canonical shape)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md ("playwright.config.ts" section)
  </read_first>
  <action>
    Install Playwright dev dependencies via pnpm and write the config file.

    Install commands (run in order, in repo root):
    1. `pnpm add -D @playwright/test@1.60.0 @axe-core/playwright@4.11.3` — pin exact versions confirmed by RESEARCH.md Package Legitimacy Audit (both [OK] disposition).
    2. `pnpm exec playwright install --with-deps chromium` — installs Chromium browser binary + OS deps. Skip if running on a sandbox that blocks apt-get; in that case run just `pnpm exec playwright install chromium`.

    Create `playwright.config.ts` at the repo root (NOT inside src/, NOT inside e2e/) per D-03. Use `defineConfig` from `@playwright/test`. Required field values per key_technical_facts #9 and RESEARCH.md Pattern 1:
    - `testDir: './e2e'`
    - `fullyParallel: true`
    - `forbidOnly: !!process.env.CI`
    - `retries: process.env.CI ? 1 : 0`
    - `workers: process.env.CI ? 1 : undefined`
    - `reporter: process.env.CI ? 'github' : [['html'], ['line']]` — list reporter format per key_technical_facts #9; in CI use 'github' for inline annotations
    - `use.baseURL: process.env.PLAYWRIGHT_TEST_BASE_URL ?? 'http://localhost:3000'` — env var name per key_technical_facts #9 (note: keep `PLAYWRIGHT_TEST_BASE_URL` not `PLAYWRIGHT_BASE_URL`)
    - `use.trace: 'on-first-retry'`
    - `projects: [{ name: 'chromium', use: { ...devices['Desktop Chrome'] } }]` — Chromium only per D-13
    - `webServer.command: 'pnpm build && pnpm start'` — production build so the about page (`force-static`) renders identically to deployed Vercel (avoids dev-only HMR overlays interfering with axe scans)
    - `webServer.url: 'http://localhost:3000'`
    - `webServer.reuseExistingServer: !process.env.CI`
    - `webServer.timeout: 120_000`

    Append to `.gitignore` (add only entries not already present):
    - `/test-results/`
    - `/playwright-report/`
    - `/blob-report/`
    - `/playwright/.cache/`

    Do NOT scaffold the default example tests from `playwright install` — the four spec files in tasks 2–3 are the only specs we want.
  </action>
  <verify>
    <automated>test -f playwright.config.ts && grep -q "testDir: './e2e'" playwright.config.ts && grep -q "chromium" playwright.config.ts && pnpm exec playwright --version | grep -q "1.60" && grep -q "@playwright/test" package.json && grep -q "@axe-core/playwright" package.json</automated>
  </verify>
  <done>
    `pnpm exec playwright --version` reports 1.60.x; `playwright.config.ts` exists at repo root with `testDir: './e2e'`, `projects` containing chromium only, and the `webServer` block; `package.json` devDependencies contains `@playwright/test` and `@axe-core/playwright` at the pinned versions; `.gitignore` excludes `test-results/`, `playwright-report/`, `blob-report/`, `playwright/.cache/`; Chromium browser binary is installed locally (verifiable via `pnpm exec playwright install --dry-run chromium` reporting no work to do).
  </done>
</task>

<task type="auto">
  <name>Task 2: Write landing.spec.ts and navigation.spec.ts</name>
  <files>e2e/landing.spec.ts, e2e/navigation.spec.ts</files>
  <read_first>
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (`e2e/landing.spec.ts`, `e2e/navigation.spec.ts` sections — copy the role-based selectors verbatim)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 2 AxeBuilder API; Code Examples section)
    - src/app/layout.tsx (confirm document `<title>` will be set from `metadata.title` — current value contains "Ruslan Kanatbek")
    - src/components/ide/Sidebar.tsx (confirm role="button" + tabIndex={0} are set on file rows — they are, line 60–67 — so `getByRole('button', { name: /README\.md/ })` works)
    - src/components/layout/SiteHeader.tsx (confirm the About nav link is an anchor with accessible name matching /About/i)
  </read_first>
  <action>
    Create `e2e/landing.spec.ts` covering SHOW-01 landing render, HARD-01 axe scan, and the public-asset reachability checks for HARD-04 (robots/sitemap — plan 02 ships the files; these assertions will fail until plan 02 lands, which is the correct gating signal). Per D-14 + D-15 keep the axe scan inline at the bottom of the spec (anti-pattern note in RESEARCH.md: a standalone accessibility.spec.ts hides per-page coverage from the sidebar).

    `e2e/landing.spec.ts` test cases (use `test.describe('Landing page', ...)` wrapper):
    1. `'has correct page title'` — `await page.goto('/'); await expect(page).toHaveTitle(/Ruslan Kanatbek/);`
    2. `'shows IDE chrome on load'` — assert `page.getByRole('main')` visible AND `page.getByRole('button', { name: /README\.md/ })` visible (proves Sidebar mounted and a known file row rendered).
    3. `'sidebar README.md row loads README content into editor'` — click the README.md row, assert `page.locator('pre code')` contains text "Hi, I'm Ruslan" (the literal opening of README.md from src/lib/files-data.ts line 17).
    4. `'robots.txt is accessible'` — `const r = await page.request.get('/robots.txt'); expect(r.status()).toBe(200); expect(await r.text()).toContain('User-agent');`
    5. `'sitemap.xml is accessible'` — `const r = await page.request.get('/sitemap.xml'); expect(r.status()).toBe(200); expect(await r.text()).toContain('<urlset');`
    6. `'OG meta tag is present'` — `await page.goto('/'); const og = await page.locator('meta[property="og:image"]').getAttribute('content'); expect(og).toBeTruthy(); expect(og).toContain('/og');` (passes once plan 02 wires metadataBase + openGraph.images).
    7. `'has no WCAG AA violations'` — instantiate `new AxeBuilder({ page }).withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa']).analyze()`; assert `results.violations` equals `[]`. Import: `import AxeBuilder from '@axe-core/playwright'` (default import — NOT named import; key_technical_facts #1).

    `e2e/navigation.spec.ts` test cases (use `test.describe('Navigation', ...)` wrapper):
    1. `'About link navigates to /about'` — `await page.goto('/'); await page.getByRole('link', { name: /About/i }).first().click(); await expect(page).toHaveURL(/\/about$/); await expect(page.getByRole('main')).toBeVisible();` (use `.first()` to disambiguate SiteHeader vs Footer About links).
    2. `'logo/home link returns to /'` — `await page.goto('/about'); await page.getByRole('link', { name: /Ruslan|portfolio/i }).first().click(); await expect(page).toHaveURL('http://localhost:3000/');` (the SiteHeader brand link returns to /).

    Both specs: `import { test, expect } from '@playwright/test';` at top. No fixture extension, no shared helpers, no separate accessibility.spec.ts file.

    Do NOT add Firefox/WebKit cases (Chromium only per D-13). Do NOT add visual regression (deferred). Do NOT add `injectAxe()` calls (auto-injected by AxeBuilder per anti-pattern note).
  </action>
  <verify>
    <automated>test -f e2e/landing.spec.ts && test -f e2e/navigation.spec.ts && grep -q "import AxeBuilder from '@axe-core/playwright'" e2e/landing.spec.ts && grep -q "withTags" e2e/landing.spec.ts && grep -c "test(" e2e/landing.spec.ts | awk '$1 >= 7 {exit 0} {exit 1}' && grep -c "test(" e2e/navigation.spec.ts | awk '$1 >= 2 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    `e2e/landing.spec.ts` contains 7 test cases including the AxeBuilder scan with WCAG 2.1 AA tags; `e2e/navigation.spec.ts` contains 2 navigation test cases; both files compile cleanly under `pnpm typecheck`; running `pnpm exec playwright test e2e/landing.spec.ts e2e/navigation.spec.ts --project=chromium` produces results (robots/sitemap/OG cases may fail — that is expected until plan 02 ships, and the failures will turn green automatically once plan 02 merges).
  </done>
</task>

<task type="auto">
  <name>Task 3: Write about.spec.ts and ide-interactions.spec.ts</name>
  <files>e2e/about.spec.ts, e2e/ide-interactions.spec.ts</files>
  <read_first>
    - e2e/landing.spec.ts (just written — copy the import block and AxeBuilder usage verbatim)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (`e2e/about.spec.ts`, `e2e/ide-interactions.spec.ts` sections)
    - src/app/about/page.tsx (confirm `<a href="/resume.pdf" download="...">` exists with accessible name including "Download PDF Resume" — line 19–27)
    - src/components/ide/Sidebar.tsx (confirm bio.json lives inside the about/ folder which starts collapsed — `useState({ about: false })` line 52 — so a click on the about folder header is required before bio.json is visible)
  </read_first>
  <action>
    Create `e2e/about.spec.ts` covering SHOW-01 about render and HARD-01 axe scan, and `e2e/ide-interactions.spec.ts` covering SHOW-01 file-click-to-editor (the SHOW-02 sidebar-display assertions live here too — they will go red until plan 03 ships the tests/ folder, which is correct gating).

    `e2e/about.spec.ts` test cases (use `test.describe('About page', ...)` wrapper):
    1. `'renders work history section'` — `await page.goto('/about'); await expect(page.getByRole('main')).toBeVisible();` Additionally assert at least one heading containing "Experience" or "Work" or a company name from resume.mdx is visible: `await expect(page.getByRole('heading').first()).toBeVisible();`
    2. `'PDF download link is present'` — `const link = page.getByRole('link', { name: /Download PDF Resume/i }); await expect(link).toBeVisible(); await expect(link).toHaveAttribute('download'); await expect(link).toHaveAttribute('href', /resume\.pdf$/);`
    3. `'has no WCAG AA violations'` — same AxeBuilder pattern as landing, but `page.goto('/about')`.

    `e2e/ide-interactions.spec.ts` test cases (use `test.describe('IDE interactions', ...)` wrapper):
    1. `'clicking bio.json loads editor with bio content'` — `await page.goto('/'); await page.getByRole('button', { name: /^about$/i }).first().click();` (expands the about folder — Sidebar.tsx line 146–158 uses a div with text "about" inside a treeFolder div; if `getByRole('button')` does not match the folder header, fall back to `page.getByText('about', { exact: true })`). Then `await page.getByRole('button', { name: /bio\.json/ }).click(); await expect(page.locator('pre code')).toContainText('Ruslan Kanatbek');`
    2. `'test files appear in sidebar under tests/ folder'` — Locator: `page.getByText('tests', { exact: true })` and `page.getByRole('button', { name: /landing\.spec\.ts/ })`. This test will FAIL until plan 03 ships the tests/ folder; that failure is the gating signal proving plan 03 has not yet landed.
    3. `'clicking landing.spec.ts loads its TypeScript source in editor'` — expand tests folder, click landing.spec.ts row, assert `page.locator('pre code')` contains the literal string `'@playwright/test'` (the import line of the spec file). Also will fail until plan 03 lands.

    All four specs share the same `import { test, expect } from '@playwright/test';` header. Specs that use axe also add `import AxeBuilder from '@axe-core/playwright';` (default import).

    Do NOT create a shared helpers file. Per D-04 and RESEARCH.md Pattern 7, every file shown in the sidebar must be self-contained TypeScript so a recruiter clicking the spec sees the entire test logic on one screen.
  </action>
  <verify>
    <automated>test -f e2e/about.spec.ts && test -f e2e/ide-interactions.spec.ts && grep -q "Download PDF Resume" e2e/about.spec.ts && grep -q "bio.json" e2e/ide-interactions.spec.ts && grep -q "tests" e2e/ide-interactions.spec.ts && grep -q "landing.spec.ts" e2e/ide-interactions.spec.ts && grep -c "test(" e2e/about.spec.ts | awk '$1 >= 3 {exit 0} {exit 1}' && grep -c "test(" e2e/ide-interactions.spec.ts | awk '$1 >= 3 {exit 0} {exit 1}'</automated>
  </verify>
  <done>
    `e2e/about.spec.ts` contains 3 tests (render, PDF link, axe); `e2e/ide-interactions.spec.ts` contains 3 tests (bio.json click, tests folder visible, spec file click); `pnpm typecheck` exits 0; running `pnpm exec playwright test --project=chromium` against a local `pnpm dev` server runs all 4 spec files — the `bio.json` interaction test and `about.spec.ts` tests pass; the sidebar-tests-folder + spec-file-click tests fail with a clear locator-not-found message until plan 03 ships (that failure is the correct gating signal).
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Playwright runner → site under test | Specs issue HTTP GETs against the local Next.js server; no state mutation occurs |
| Test process → axe-core | Auto-injected script runs in the page context to scan rendered DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-04 | Tampering | E2E test traffic against the deployed site | accept | Specs are GET-only navigation flows; no POST/PUT against production. Per `webServer.url` config, tests run against `localhost:3000`, never against the production Vercel URL. Documented in RESEARCH.md security domain. |
| T-03-03 | Information Disclosure | axe-core scan coverage | accept | axe-core covers ~30–40% of WCAG violations; manual keyboard nav audit performed in plan 06. Documented in plan 06 acceptance criteria. |
| T-03-SC | Tampering | npm installs (`@playwright/test`, `@axe-core/playwright`) | mitigate | Both packages have `[OK]` disposition in RESEARCH.md Package Legitimacy Audit (verified via npm registry, mature publishers Microsoft + Deque, no postinstall scripts). Pinned to exact versions `1.60.0` and `4.11.3` to lock the audited supply chain. No blocking checkpoint needed since both are `[OK]`. |
</threat_model>

<verification>
- `pnpm typecheck` exits 0 (Playwright + axe types resolve cleanly).
- `pnpm exec playwright test --project=chromium --list` lists 15 tests across 4 spec files (7 landing + 2 nav + 3 about + 3 ide).
- `pnpm exec playwright test --project=chromium` produces a results report (some failures expected until plans 02 + 03 ship — see done criteria of tasks 2 + 3).
- `pnpm lint` exits 0 (no new ESLint errors in e2e/).
</verification>

<success_criteria>
- Four spec files exist under `e2e/` with the test counts noted above.
- `playwright.config.ts` at repo root with `testDir: './e2e'`, chromium-only project, webServer auto-start.
- `@playwright/test@1.60.0` and `@axe-core/playwright@4.11.3` pinned in `package.json` devDependencies.
- Chromium browser binary installed; `pnpm exec playwright install --dry-run chromium` reports no pending work.
- All landing-render, about-render, navigation, and bio.json-click tests pass against `pnpm dev`.
- axe-core WCAG AA scans on `/` and `/about` return zero violations (any violations surfaced here are real a11y bugs to fix in plan 06).
</success_criteria>

<output>
Create `.planning/phases/03-playwright-showcase-hardening/03-01-SUMMARY.md` documenting:
- Files created (4 specs + 1 config + package.json delta)
- Installed package versions
- Pre-existing axe violations found (these become inputs to plan 06)
- Which test assertions are intentionally red until plans 02/03 ship (robots/sitemap/OG/tests-folder)
</output>