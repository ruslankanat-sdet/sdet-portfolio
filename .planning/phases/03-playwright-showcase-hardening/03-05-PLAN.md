---
phase: 03-playwright-showcase-hardening
plan: 05
type: execute
wave: 3
depends_on:
  - 03-04
files_modified:
  - src/app/api/run-tests/route.ts
  - src/app/api/run-status/[run_id]/route.ts
  - src/components/ide/IDEShell.tsx
autonomous: true
requirements:
  - SHOW-03
user_setup:
  - service: github
    why: "Workflow_dispatch trigger + run polling for the Run Smoke Test button"
    env_vars:
      - name: GITHUB_TOKEN
        source: "GitHub -> Settings -> Developer Settings -> Fine-grained tokens; scope Actions Read and Write on the sdet-portfolio repository only"
      - name: GH_OWNER
        source: "Literal string ruslankanat-sdet (the GitHub org/user that owns the repo)"
      - name: GH_REPO
        source: "Literal string sdet-portfolio"
    dashboard_config:
      - task: "Add all 3 env vars to Vercel project. Mark GITHUB_TOKEN as Sensitive. Apply to Production scope"
        location: "Vercel project dashboard -> Settings -> Environment Variables"

must_haves:
  truths:
    - "Clicking Run Smoke Test in the TopBar triggers a real GitHub Actions run on the ci.yml workflow"
    - "Terminal pane polls the run every 5 seconds and renders LogEntry rows reflecting job status"
    - "GITHUB_TOKEN never appears in any client-side bundle, response body, or NEXT_PUBLIC env var"
    - "When GITHUB_TOKEN is unset, the run-tests endpoint returns HTTP 503 with a clear not-configured message"
    - "run_id path parameter on run-status is validated as a positive integer before being embedded in the GitHub API URL"
  artifacts:
    - path: "src/app/api/run-tests/route.ts"
      provides: "POST handler that triggers workflow_dispatch with return_run_details true and returns the new runId"
      contains: "workflow_dispatch"
    - path: "src/app/api/run-status/[run_id]/route.ts"
      provides: "GET handler that proxies GitHub API job-status calls keyed by run_id"
      contains: "jobs"
    - path: "src/components/ide/IDEShell.tsx"
      provides: "runSmoke replaced with real API call + 5s polling loop that updates logs state"
      contains: "/api/run-tests"
  key_links:
    - from: "src/components/ide/IDEShell.tsx runSmoke"
      to: "src/app/api/run-tests/route.ts"
      via: "POST fetch"
      pattern: "fetch.*api/run-tests"
    - from: "src/components/ide/IDEShell.tsx poll function"
      to: "src/app/api/run-status/[run_id]/route.ts"
      via: "GET fetch with runId in path"
      pattern: "fetch.*api/run-status"
    - from: "src/app/api/run-tests/route.ts"
      to: "GitHub Actions API"
      via: "POST to workflows/ci.yml/dispatches"
      pattern: "workflows/ci.yml/dispatches"
---

<objective>
Replace the existing animated-fake runSmoke in IDEShell.tsx with a real round-trip: button click POSTs to /api/run-tests which triggers a GitHub Actions workflow_dispatch on ci.yml, the client polls /api/run-status/[run_id] every 5 seconds, and the Terminal pane renders LogEntry rows reflecting actual job step status until the run completes. GITHUB_TOKEN stays server-side per T-03-01.

Purpose: The README.md file already promises "Run Smoke Test in the topbar -> watch this site test itself." Phase 1 wired a fake animation. This plan delivers the real thing — a recruiter clicking the button watches a real CI run kick off and stream status into the terminal pane in real time. Combined with the static SAMPLE_LOGS (D-10) showing the last successful run on initial paint, this satisfies SHOW-03 in both static and interactive form.

Output: Two new Node-runtime API routes proxying GitHub Actions, a refactored runSmoke that uses recursive setTimeout polling, and a Terminal pane that reflects real job state. The Run Smoke Test button works against the production GitHub repo once env vars are set in Vercel.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/03-playwright-showcase-hardening/03-CONTEXT.md
@.planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md
@.planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md
@.planning/phases/03-playwright-showcase-hardening/03-04-SUMMARY.md
@CLAUDE.md
@src/components/ide/IDEShell.tsx
@src/components/ide/TopBar.tsx
@src/components/ide/Terminal.tsx
@src/types/ide.ts
@src/lib/files-data.ts

<interfaces>
From src/types/ide.ts:
- LogEntry shape: kind (LogKind) + optional text/test/detail strings.
- LogKind union: info, warn, ok, pass, fail. No new kinds added — per Pitfall 6 in RESEARCH.md, map in_progress GitHub job status to kind info with spinner prefix character.

From src/components/ide/IDEShell.tsx (existing wiring to preserve):
- State: running boolean, logs LogEntry array, lastRun string, termTab TerminalTab.
- runSmoke useCallback at lines 53-70 currently animates SAMPLE_LOGS via recursive setTimeout. Replace body with real fetch + polling.
- TopBar prop wiring at line 86 already passes onRun runSmoke and running running — no TopBar signature changes needed.
- Keyboard shortcut effect at lines 73-82 triggers runSmoke on cmd+enter / ctrl+enter. Preserve untouched.

From GitHub Actions API (verified in RESEARCH.md Pattern 3 and Pattern 4):
- POST https colon slash slash api.github.com/repos/owner/repo/actions/workflows/ci.yml/dispatches with JSON body containing ref main and return_run_details true returns HTTP 200 with JSON containing workflow_run_id, run_url, html_url. Authorization header is Bearer GITHUB_TOKEN.
- GET https colon slash slash api.github.com/repos/owner/repo/actions/runs/run_id/jobs returns JSON with a jobs array; each job has name, status (queued or in_progress or completed), conclusion (success or failure or cancelled or null), and steps array with similar status and conclusion fields.

Per key_technical_facts #2: With return_run_details true, the dispatch endpoint returns HTTP 200 plus workflow_run_id directly in the response body — no race-condition polling to find the run ID. GA since 2026-02-19.

Per key_technical_facts #7: GITHUB_TOKEN must have NO NEXT_PUBLIC prefix. Read only in route handlers. Also need GH_OWNER and GH_REPO. Missing token returns error not-configured with HTTP 503.

Route handler convention from CLAUDE.md and RESEARCH.md shared pattern:
- export const runtime equal to nodejs.
- Use native Response.json (no NextResponse import).
- Guard env vars before any fetch; return 503 if missing.

Route file convention: per planning_context wave_structure, run-status uses a dynamic segment named run_id in the path (Next.js App Router dynamic route). The handler signature is GET(request, context) where context.params is a Promise of an object containing run_id — note that in Next 15 the params object is Promise-wrapped and must be awaited.
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create /api/run-tests route handler</name>
  <files>src/app/api/run-tests/route.ts</files>
  <read_first>
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (app/api/run-tests/route.ts section — exact code)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 3 + Code Examples; Security Domain GITHUB_TOKEN exposure threat; Pitfall 2 on token scope)
    - CLAUDE.md (Route Handler conventions, Node runtime requirement)
    - .github/workflows/ci.yml (confirm the workflow filename is literally ci.yml — the API call URL embeds this string)
  </read_first>
  <action>
    Create new file src/app/api/run-tests/route.ts. App Router will auto-route this as POST /api/run-tests.

    Required exports:
    - export const runtime equal to the string nodejs — per CLAUDE.md (Node, not Edge, for tool routes).
    - export async function POST that takes no arguments (no request body parsing — the button click sends no payload).

    Handler logic in order:
    1. Read token from process.env.GITHUB_TOKEN. If absent, return Response.json with body containing error CI trigger not configured and status 503. Per key_technical_facts #7 — missing token must return 503 with a clear message; do NOT fall back to a fake animation.
    2. Read owner from process.env.GH_OWNER and repo from process.env.GH_REPO. If either absent, return Response.json with error CI trigger not configured and status 503 (same generic message; avoid leaking which env var is missing).
    3. Call fetch on the URL https colon slash slash api.github.com/repos/owner/repo/actions/workflows/ci.yml/dispatches with:
       - method POST
       - headers: Authorization Bearer plus token, Accept application/vnd.github+json, X-GitHub-Api-Version 2022-11-28, Content-Type application/json
       - body JSON.stringify of an object containing ref main and return_run_details true (per key_technical_facts #2, returns HTTP 200 plus run ID directly)
    4. If response.ok is false, return Response.json with error Dispatch failed and status equal to response.status (pass through 401/403/404 as-is so the client can distinguish auth errors from other failures).
    5. Parse response JSON as containing workflow_run_id, run_url, html_url. Return Response.json with runId equal to workflow_run_id and url equal to html_url.

    Per T-03-01 (MEDIUM): No part of the GITHUB_TOKEN value may appear in the response body. Only runId (numeric) and url (the public html_url) are returned. Do NOT echo back any header values, do NOT return the request body, do NOT log the token.

    Per RESEARCH.md security gap note: no rate limiting in v1 (Upstash Redis cut from scope). The button can be spammed. Add a single-line comment near the top of the file documenting this as a known gap referencing the CONTEXT.md decision to defer rate limiting.

    Do NOT use NextResponse from next/server — Response.json is sufficient per the shared route handler pattern. Do NOT parse request.body — there is nothing to parse.
  </action>
  <verify>
    <automated>test -f src/app/api/run-tests/route.ts &amp;&amp; grep -q "runtime" src/app/api/run-tests/route.ts &amp;&amp; grep -q "GITHUB_TOKEN" src/app/api/run-tests/route.ts &amp;&amp; grep -q "GH_OWNER" src/app/api/run-tests/route.ts &amp;&amp; grep -q "workflows/ci.yml/dispatches" src/app/api/run-tests/route.ts &amp;&amp; grep -q "return_run_details" src/app/api/run-tests/route.ts &amp;&amp; grep -q "503" src/app/api/run-tests/route.ts &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    src/app/api/run-tests/route.ts exports runtime nodejs and async POST; with GITHUB_TOKEN unset, calling POST /api/run-tests via curl returns 503 with the not-configured error message; with all 3 env vars set and a valid PAT, the same call returns 200 with a JSON body containing runId (numeric) and url (the html_url of the dispatched run); the GITHUB_TOKEN string never appears in any response body or log line; pnpm typecheck exits 0.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create /api/run-status/[run_id] dynamic route handler</name>
  <files>src/app/api/run-status/[run_id]/route.ts</files>
  <read_first>
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (app/api/run-status/route.ts section — exact code; note PATTERNS.md uses a query param while this plan uses a dynamic segment per planning_context wave_structure)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 4 + Code Examples; Security Domain run_id parameter injection threat)
    - src/app/api/run-tests/route.ts (just written — copy the Node runtime + env var guard pattern verbatim)
    - CLAUDE.md (dynamic route params are Promise-wrapped in Next 15)
  </read_first>
  <action>
    Create new file at src/app/api/run-status/[run_id]/route.ts. The bracketed segment makes run_id a dynamic path parameter — App Router will route GET /api/run-status/123 to this handler with params.run_id equal to the string 123.

    Required exports:
    - export const runtime equal to the string nodejs.
    - export async function GET that takes two arguments: request typed as Request, and context typed as an object whose params field is a Promise of an object containing run_id string. Per Next 15 the params object is a Promise that must be awaited before access (note in CLAUDE.md Version Compatibility table).

    Handler logic in order:
    1. Await context.params to extract run_id.
    2. Validate run_id as a positive integer using a regex matching only one or more digits. If invalid, return Response.json with error run_id must be a positive integer and status 400. Per RESEARCH.md security domain ASVS V5: this prevents tampering attacks where a malicious run_id (path traversal segments) would inject into the GitHub URL.
    3. Read GITHUB_TOKEN, GH_OWNER, GH_REPO env vars exactly as in /api/run-tests. If GITHUB_TOKEN missing return 503 with not-configured message. If GH_OWNER or GH_REPO missing return 503.
    4. Call fetch on https colon slash slash api.github.com/repos/owner/repo/actions/runs/run_id/jobs with:
       - method GET
       - headers: Authorization Bearer plus token, Accept application/vnd.github+json, X-GitHub-Api-Version 2022-11-28
    5. If response.ok is false, return Response.json with error Status fetch failed and status equal to response.status.
    6. Parse response JSON. Return Response.json with the parsed data passed through. The client maps jobs/steps to LogEntry rows; the route is a pure proxy so the client never sees the GITHUB_TOKEN.

    Per T-03-01: GITHUB_TOKEN never appears in response body. The response body is whatever the GitHub jobs API returned (jobs array with names, statuses, conclusions, steps) — no token, no Authorization header echoed back.

    Per Pitfall 6 (RESEARCH.md): the LogKind type has no running or pending kind; client-side mapping (in task 3) translates GitHub status in_progress to kind info with a spinner character prefix. The route handler returns raw GitHub data; mapping is the client's job.
  </action>
  <verify>
    <automated>test -f "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "runtime" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "run_id" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "actions/runs" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "jobs" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "GITHUB_TOKEN" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; grep -q "params" "src/app/api/run-status/[run_id]/route.ts" &amp;&amp; pnpm typecheck</automated>
  </verify>
  <done>
    src/app/api/run-status/[run_id]/route.ts exists at the dynamic-segment path; GET /api/run-status/abc returns 400 with the positive-integer validation message; GET /api/run-status/123 (with valid env + token) returns the GitHub jobs payload for run 123; GET /api/run-status/123 with GITHUB_TOKEN unset returns 503; pnpm typecheck exits 0; the GITHUB_TOKEN never appears in the response body.
  </done>
</task>

<task type="auto">
  <name>Task 3: Refactor runSmoke in IDEShell.tsx to real fetch + polling</name>
  <files>src/components/ide/IDEShell.tsx</files>
  <read_first>
    - src/components/ide/IDEShell.tsx (existing runSmoke lines 53-70 — to be replaced; preserve all surrounding state and other callbacks)
    - .planning/phases/03-playwright-showcase-hardening/03-PATTERNS.md (src/components/ide/IDEShell.tsx section — runSmoke replacement pattern + buildLogEntries helper concept)
    - .planning/phases/03-playwright-showcase-hardening/03-RESEARCH.md (Pattern 4 — polling with recursive setTimeout; Pitfall 6 — kind info with spinner for in_progress)
    - src/types/ide.ts (LogKind union and LogEntry shape — confirm only the existing kinds are available)
    - src/lib/files-data.ts (SAMPLE_LOGS — the initial logs state continues to slice from this for the static last-run view per D-10)
  </read_first>
  <action>
    Two changes within IDEShell.tsx, both confined to the runSmoke callback and a new helper function. Do NOT touch other state, useEffect blocks, or component props.

    Change 1: Add a module-scoped (file-top, before the IDEShell function) helper named buildLogEntries that takes the GitHub jobs payload and returns a LogEntry array. The payload shape is an object with optional jobs array; each job has name string, status string, conclusion string-or-null, and optional steps array; each step has name, status, conclusion, number. Mapping rules per Pitfall 6:
    - Top entry: kind info, text Run dispatched - polling status.
    - For each job in data.jobs:
      - If job.status equal to completed and job.conclusion equal to success: kind pass, test job.name, detail completed
      - Else if job.status equal to completed and job.conclusion equal to failure: kind fail, test job.name, detail failed
      - Else if job.status equal to completed and job.conclusion equal to cancelled: kind warn, text Job cancelled - job.name
      - Else if job.status equal to in_progress: kind info, text spinner-character + space + Running: + job.name (use a circular-arrow or similar non-emoji unicode glyph that the existing terminal font can render; if unsure, use a literal greater-than or asterisk)
      - Else if job.status equal to queued: kind info, text [queued] + space + job.name
    - For each completed job's steps array, append one row per FAILED step (skip passing steps to keep the log readable): kind fail, test step.name, detail failed.
    - When all jobs reach status completed: append a summary row. kind ok if every conclusion equals success, otherwise kind fail. Text summary: pass count, fail count, total.

    Change 2: Replace the body of the existing runSmoke useCallback (lines 53-70). New body:
    1. If running is true, return immediately (debounce).
    2. setRunning to true. setLogs to a single-entry array containing kind info and text Triggering CI run. setTermTab to TERMINAL.
    3. Wrap the rest in a try block (catch errors and surface them as LogEntry kind fail).
    4. Call fetch on /api/run-tests with method POST. Await response. Await response.json into an object that may contain runId, url, error.
    5. If response.ok is false or error is present:
       - If response.status equal to 503: setLogs append kind warn, text CI trigger not configured - set GITHUB_TOKEN in Vercel env vars. Then setRunning false and return. (Graceful fallback for the developer-only local dev path where env vars are not set.)
       - Else: setLogs append kind fail, text Dispatch failed plus the status. Then setRunning false and return.
    6. Append two info entries: text Run dispatched - GitHub Actions run runId queued, and text View run: url. Now begin polling.
    7. Define an inner async poll function that:
       - Calls fetch on /api/run-status/ concatenated with runId (template literal that interpolates runId into the path).
       - Awaits response.json.
       - Calls buildLogEntries on the parsed payload and setLogs to the result (replace entire log list each poll — keeps the display in sync with current state, prevents append-runaway).
       - Determines completion: every job's status equals completed. If complete, setRunning false, setLastRun to just now, and stop polling. Else setTimeout recursively with poll and 5000ms (per Pattern 4 — recursive setTimeout, NOT setInterval, to avoid overlap).
    8. Kick off polling with setTimeout(poll, 5000) (first poll happens 5 seconds after dispatch).
    9. Wrap polling errors in try/catch: any thrown fetch error becomes setLogs append kind fail, text Polling failed plus err.message; setRunning false; do not reschedule.

    Per D-09: polling interval is exactly 5 seconds. Per D-10: the initial logs state on first paint of the page (already set on line 19 from SAMPLE_LOGS.slice(0, 8)) shows the last successful run statically — this plan does not change that line; it only changes what happens after the button is clicked.

    Preserve all other state, useEffect blocks (theme sync, keyboard shortcut), and the TopBar/Sidebar/EditorArea/Terminal/StatusBar JSX exactly as they are. The TopBar already wires onRun and running — those props need no changes.

    Do NOT add a new file for buildLogEntries — keep it module-scoped inside IDEShell.tsx (one helper, used once, splitting it adds an import without benefit). Do NOT introduce a custom React hook (useRunStatus, etc.) — the polling logic is plan-local complexity not worth abstracting.

    Do NOT remove the keyboard shortcut effect that triggers runSmoke on cmd+enter / ctrl+enter — that wiring works identically with the new async runSmoke (async callbacks fire fine inside event handlers).

    Do NOT call /api/run-status directly from useEffect — only inside the poll function spawned by runSmoke. The terminal pane should be quiet on first paint, animating only when the user clicks the button.
  </action>
  <verify>
    <automated>grep -q "buildLogEntries" src/components/ide/IDEShell.tsx &amp;&amp; grep -q "api/run-tests" src/components/ide/IDEShell.tsx &amp;&amp; grep -q "api/run-status" src/components/ide/IDEShell.tsx &amp;&amp; grep -q "5000" src/components/ide/IDEShell.tsx &amp;&amp; grep -q "in_progress" src/components/ide/IDEShell.tsx &amp;&amp; grep -q "Triggering CI run" src/components/ide/IDEShell.tsx &amp;&amp; pnpm typecheck &amp;&amp; pnpm lint</automated>
  </verify>
  <done>
    src/components/ide/IDEShell.tsx has a module-scoped buildLogEntries helper and a refactored runSmoke that calls /api/run-tests then polls /api/run-status/runId every 5 seconds; clicking the Run Smoke Test button (after GITHUB_TOKEN, GH_OWNER, GH_REPO are set in Vercel) triggers a real workflow_dispatch on the ci.yml workflow visible in the GitHub Actions UI; with env vars missing, the terminal pane shows a warn-kind row explaining the configuration is missing rather than crashing; pnpm typecheck and pnpm lint both exit 0.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Public browser -> /api/run-tests (POST) | Anonymous public endpoint; no auth on the Next.js route; rate limiting deferred (acceptable risk per CONTEXT.md and RESEARCH.md security gap note) |
| Public browser -> /api/run-status/run_id (GET) | Anonymous public endpoint; only the runId path param is user-controlled; validated as positive integer before being embedded in the GitHub URL |
| Route handler -> GitHub Actions API | Authorization Bearer GITHUB_TOKEN sent server-side only; never echoed to client |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-01 | Information Disclosure | GITHUB_TOKEN exposure | mitigate | Env var name has no NEXT_PUBLIC prefix per key_technical_facts #7; read only inside Node-runtime route handlers; never returned in response body; route returns only runId (numeric) and html_url (public link). Documented in T-03-01 of security_requirements. |
| T-03-INJECT | Tampering | run_id path param injection | mitigate | Regex validation rejecting anything except one or more digits before the value is concatenated into the GitHub API URL; per RESEARCH.md ASVS V5 control. |
| T-03-DOS | Denial of Service | Run-tests button abuse | accept | No rate limiting in v1 (Upstash Redis cut from scope). Acceptable for a portfolio with low expected traffic. Documented as a known gap; future plan can add per-IP throttle if traffic warrants. |
</threat_model>

<verification>
- pnpm typecheck exits 0 across all 3 modified files.
- pnpm lint exits 0.
- curl POST /api/run-tests with no env vars returns HTTP 503 with not-configured body.
- curl GET /api/run-status/notanumber returns HTTP 400 with positive-integer validation message.
- With env vars set in Vercel preview, clicking Run Smoke Test in the deployed app triggers a real GitHub Actions run visible in the Actions tab of the repo.
- The terminal pane updates every 5 seconds with job progress, then shows a final summary row.
- The GITHUB_TOKEN value cannot be found in any client-bundled JavaScript (verifiable via grep over the production build output).
</verification>

<success_criteria>
- src/app/api/run-tests/route.ts and src/app/api/run-status/[run_id]/route.ts both exist with Node runtime and the env-var guard pattern.
- src/components/ide/IDEShell.tsx uses real fetch + recursive setTimeout polling instead of the SAMPLE_LOGS animation.
- GITHUB_TOKEN, GH_OWNER, GH_REPO documented in user_setup and must be added by the developer to Vercel before this plan's outcome works in production.
- Clicking the button triggers a real CI run; the terminal pane reflects job status in real time.
</success_criteria>

<output>
Create .planning/phases/03-playwright-showcase-hardening/03-05-SUMMARY.md documenting:
- Files created (2 new route handlers) and modified (IDEShell.tsx)
- Env vars added to Vercel project (which scopes)
- First end-to-end smoke test run after deployment (run URL, duration, final terminal state)
- Any GitHub API surprises encountered (rate limits, response shape variants)
</output>
