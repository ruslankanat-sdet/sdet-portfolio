---
phase: 04-tech-debt-sweep
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/layout/SiteHeader.tsx
  - src/components/layout/Footer.tsx
  - src/lib/files-data.ts
autonomous: true
requirements:
  - DEBT-01
  - DEBT-02
  - DEBT-04
tags:
  - content
  - sidebar
  - footer
  - header
user_setup: []

must_haves:
  truths:
    - "Every page renders the SiteHeader title text 'Senior SDET / QA Automation Engineer' (full title, not just 'Senior SDET')"
    - "The full-variant Footer renders the copyright string '© 2026 Ruslan Kanatbek'"
    - "The IDE sidebar entry for tests/landing.spec.ts displays source byte-equivalent to e2e/landing.spec.ts (including the .first() chain and the locator('aside') scoping)"
    - "The IDE sidebar entry for tests/ide-interactions.spec.ts displays source byte-equivalent to e2e/ide-interactions.spec.ts (including the 'This test will FAIL until plan 03 ships' gating comments)"
    - "The IDE sidebar entries for tests/about.spec.ts, tests/navigation.spec.ts, and tests/playwright.config.ts display source byte-equivalent to their actual on-disk files"
  artifacts:
    - path: "src/components/layout/SiteHeader.tsx"
      provides: "SiteHeader component with full candidate title"
      contains: "Senior SDET / QA Automation Engineer"
    - path: "src/components/layout/Footer.tsx"
      provides: "Footer component with current copyright year (no code change expected — verification only)"
      contains: "© 2026 Ruslan Kanatbek"
    - path: "src/lib/files-data.ts"
      provides: "Sidebar file data with content strings matching the real e2e/ specs and root playwright.config.ts"
      contains: "page.locator('aside').getByRole"
  key_links:
    - from: "src/components/layout/SiteHeader.tsx"
      to: "rendered DOM .title span"
      via: "static JSX text node"
      pattern: "Senior SDET / QA Automation Engineer"
    - from: "src/lib/files-data.ts FILES['tests/landing.spec.ts'].content"
      to: "e2e/landing.spec.ts on-disk source"
      via: "verbatim string-literal copy (with double-escaped backslashes for regex dots)"
      pattern: "page\\.locator\\('aside'\\)\\.getByRole"
    - from: "src/lib/files-data.ts FILES['tests/ide-interactions.spec.ts'].content"
      to: "e2e/ide-interactions.spec.ts on-disk source"
      via: "verbatim string-literal copy preserving FAIL-gating comments"
      pattern: "This test will FAIL until plan 03 ships"
---

<objective>
Fix three independent content-accuracy debts surfaced at end of v1.0:
- DEBT-01: SiteHeader currently shows the truncated title "Senior SDET" — visitors do not see the full role string. Update to "Senior SDET / QA Automation Engineer".
- DEBT-02: Footer copyright was previously © 2024; was already corrected in a v1.0 quick task. Verify the line is still present and close.
- DEBT-04: Five sidebar file entries in files-data.ts have drifted from the actual e2e/ specs and root playwright.config.ts. Re-sync the displayed source so recruiters see the real test code, not a stale copy.

Purpose: Visitors scanning the IDE must see correct identity ("Senior SDET / QA Automation Engineer"), correct copyright (2026), and a sidebar whose displayed test code matches the real repository. Stale sidebar source code in a portfolio whose value proposition is "this is my actual test code" is the worst possible failure mode — it makes the rest of the showcase look fake.

Output: Updated SiteHeader.tsx, verified Footer.tsx (no edit), and resynced files-data.ts entries for tests/landing.spec.ts, tests/navigation.spec.ts, tests/about.spec.ts, tests/ide-interactions.spec.ts, tests/playwright.config.ts.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/04-tech-debt-sweep/04-CONTEXT.md
@.planning/phases/04-tech-debt-sweep/04-PATTERNS.md
@src/components/layout/SiteHeader.tsx
@src/components/layout/Footer.tsx
@src/lib/files-data.ts
@e2e/landing.spec.ts
@e2e/navigation.spec.ts
@e2e/about.spec.ts
@e2e/ide-interactions.spec.ts
@playwright.config.ts

<interfaces>
<!-- FileEntry shape used by files-data.ts FILES record (from src/types/ide.ts) -->
<!-- Each entry: { lang, path, icon, content } where content is a string literal -->
<!-- IMPORTANT: content strings inside files-data.ts template literals use DOUBLE-backslash for regex dot escapes (\\.) because the source files use \. and we are embedding them in a JS template literal that the editor pane renders verbatim. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update SiteHeader title to full role string (DEBT-01)</name>
  <files>src/components/layout/SiteHeader.tsx</files>
  <read_first>
    - src/components/layout/SiteHeader.tsx (the file being modified — confirm line 10 still reads `<span className={styles.title}>Senior SDET</span>`)
    - src/components/layout/SiteHeader.module.css (confirm no responsive treatment is required to land this string; if `.title` lacks any size/wrap rule that breaks at desktop widths, leave it alone — responsive handling is deferred to Phase 5 per CONTEXT.md D-05)
    - .planning/phases/04-tech-debt-sweep/04-CONTEXT.md (decision D-05 — text-only change, no responsive logic)
  </read_first>
  <action>Edit src/components/layout/SiteHeader.tsx and replace the single text node `Senior SDET` (inside the `<span className={styles.title}>...</span>` element on line 10) with the exact string `Senior SDET / QA Automation Engineer`. Implements D-05. Do NOT add any conditional render, breakpoint helper, media query, or `white-space` rule — responsive handling is deferred to Phase 5. Do NOT modify SiteHeader.module.css. Do NOT introduce any new imports or props.</action>
  <verify>
    <automated>grep -F "Senior SDET / QA Automation Engineer" src/components/layout/SiteHeader.tsx &amp;&amp; ! grep -E "&gt;Senior SDET&lt;" src/components/layout/SiteHeader.tsx &amp;&amp; pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/SiteHeader.tsx contains the exact literal `Senior SDET / QA Automation Engineer` inside a `<span className={styles.title}>` element.
    - src/components/layout/SiteHeader.tsx does NOT contain the bare literal `>Senior SDET<` (the truncated form must be gone).
    - src/components/layout/SiteHeader.module.css is unchanged (no diff against pre-task state).
    - `pnpm typecheck` exits 0.
    - No new imports added to SiteHeader.tsx (file still imports only `NavLink` and the CSS module).
  </acceptance_criteria>
  <done>SiteHeader renders `Senior SDET / QA Automation Engineer` in the `.title` span on every page. CSS module untouched.</done>
</task>

<task type="auto">
  <name>Task 2: Verify Footer copyright already shows © 2026 (DEBT-02)</name>
  <files>src/components/layout/Footer.tsx</files>
  <read_first>
    - src/components/layout/Footer.tsx (verify-only — confirm the full-variant footer at line 84 contains `© 2026 Ruslan Kanatbek`)
    - .planning/phases/04-tech-debt-sweep/04-CONTEXT.md (DEBT-02 specifics — explicitly notes this is a verification pass with no code change expected)
    - .planning/phases/04-tech-debt-sweep/04-PATTERNS.md (DEBT-02 Pre-Closure Note section)
    - .planning/STATE.md (quick task 260523-q02 already corrected the related double-footer issue)
  </read_first>
  <action>This is a verify-only task — DO NOT edit Footer.tsx. Confirm two things by reading the file: (1) the full-variant footer block contains the exact string `© 2026 Ruslan Kanatbek` inside the `<p className={styles.copyright}>` element; (2) the compact-variant footer block does NOT render any copyright string (compact only shows email + social + "No cookies. No tracking."). If either assertion fails, STOP and surface the mismatch to the user — do not auto-edit. If both assertions hold, mark DEBT-02 closed in the summary with no code change.</action>
  <verify>
    <automated>grep -F "© 2026 Ruslan Kanatbek" src/components/layout/Footer.tsx &amp;&amp; ! grep -F "© 2024" src/components/layout/Footer.tsx</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/Footer.tsx contains the exact literal `© 2026 Ruslan Kanatbek`.
    - src/components/layout/Footer.tsx does NOT contain any `© 2024` substring.
    - src/components/layout/Footer.tsx has zero diff lines against the pre-task state (verification only).
    - The summary file records DEBT-02 as "closed — already correct, no code change".
  </acceptance_criteria>
  <done>DEBT-02 confirmed closed; Footer.tsx unchanged; summary documents the verify-only outcome.</done>
</task>

<task type="auto">
  <name>Task 3: Resync files-data.ts test-file entries against actual e2e/ specs (DEBT-04)</name>
  <files>src/lib/files-data.ts</files>
  <read_first>
    - src/lib/files-data.ts (the file being modified — note the existing FileEntry shape and the double-backslash regex-escape convention used inside the template-literal `content` fields)
    - e2e/landing.spec.ts (source of truth for `FILES['tests/landing.spec.ts'].content`; the actual file uses `.first()` on the `getByRole` matcher and `page.locator('aside').getByRole(...)` for the click target)
    - e2e/navigation.spec.ts (source of truth for `FILES['tests/navigation.spec.ts'].content`)
    - e2e/about.spec.ts (source of truth for `FILES['tests/about.spec.ts'].content`)
    - e2e/ide-interactions.spec.ts (source of truth for `FILES['tests/ide-interactions.spec.ts'].content` — currently has gating comments "This test will FAIL until plan 03 ships..." on tests 2 and 3 that the sidebar entry is missing)
    - playwright.config.ts (root, NOT inside e2e/ — source of truth for `FILES['tests/playwright.config.ts'].content`; the sidebar `path` field intentionally renders this under `~/portfolio/playwright.config.ts` per D-06)
    - .planning/phases/04-tech-debt-sweep/04-CONTEXT.md (decision D-06 — one-time sync, no automated drift detection)
    - .planning/phases/04-tech-debt-sweep/04-PATTERNS.md (Stale vs actual comparison table; escape-convention note)
  </read_first>
  <action>For each of the five FILES entries listed below, replace the existing `content: \`...\`` template literal with the verbatim current source of the corresponding on-disk file. Implements D-06. Escape conversion rule: every backslash in the source file becomes a DOUBLE-backslash inside the template literal (`\.` in the .spec.ts becomes `\\.` inside the JS template-literal content string) so the editor pane renders the regex dot correctly. Preserve any backtick characters by escaping them with a backslash (none expected in these specs but verify on copy). Do NOT change the `lang`, `path`, or `icon` fields on any entry. Do NOT add or remove entries. Do NOT change non-test entries (README.md, bio.json, experience.yaml, skills.yaml, contact.json). Entries to resync: (1) `FILES['tests/landing.spec.ts'].content` ← `e2e/landing.spec.ts` (must contain the `.first()` chain and the `page.locator('aside').getByRole(...)` invocation); (2) `FILES['tests/navigation.spec.ts'].content` ← `e2e/navigation.spec.ts`; (3) `FILES['tests/about.spec.ts'].content` ← `e2e/about.spec.ts`; (4) `FILES['tests/ide-interactions.spec.ts'].content` ← `e2e/ide-interactions.spec.ts` (must include the two "This test will FAIL until plan 03 ships the tests/ folder entries in files-data.ts — that failure is the correct gating signal proving plan 03 has not yet landed." inline comments on tests 2 and 3); (5) `FILES['tests/playwright.config.ts'].content` ← `playwright.config.ts` at the repo root. After editing, run `pnpm exec playwright test e2e/ide-interactions.spec.ts e2e/landing.spec.ts --reporter=line` to confirm the sidebar still renders the synced source as expected by the E2E tests (the editor click-then-`pre code` assertions exercise the content strings end-to-end).</action>
  <verify>
    <automated>grep -F "page.locator('aside').getByRole" src/lib/files-data.ts &amp;&amp; grep -F "This test will FAIL until plan 03 ships" src/lib/files-data.ts &amp;&amp; grep -F "page.getByRole('button', { name: /README\\.md/ }).first()" src/lib/files-data.ts &amp;&amp; pnpm typecheck &amp;&amp; pnpm exec playwright test e2e/ide-interactions.spec.ts e2e/landing.spec.ts --reporter=line</automated>
  </verify>
  <acceptance_criteria>
    - src/lib/files-data.ts `FILES['tests/landing.spec.ts'].content` contains the literal substring `page.locator('aside').getByRole` (matches the current e2e/landing.spec.ts click target).
    - src/lib/files-data.ts `FILES['tests/landing.spec.ts'].content` contains the literal substring `getByRole('button', { name: /README\\.md/ }).first()` (matches the current e2e/landing.spec.ts `.first()` chain — note double backslash inside the template literal).
    - src/lib/files-data.ts `FILES['tests/ide-interactions.spec.ts'].content` contains the literal substring `This test will FAIL until plan 03 ships` (the gating comment from e2e/ide-interactions.spec.ts lines 14 and 24).
    - src/lib/files-data.ts `FILES['tests/playwright.config.ts'].content` contains the literal substring `testDir: './e2e'` and `pnpm build && pnpm start` (root playwright.config.ts signature lines).
    - The five `lang`, `path`, and `icon` triples are unchanged for the five test entries (only `content` was edited).
    - `pnpm typecheck` exits 0.
    - `pnpm exec playwright test e2e/ide-interactions.spec.ts e2e/landing.spec.ts --reporter=line` exits 0 — confirms the sidebar still wires through to the editor pane as the E2E tests expect.
    - No non-test FILES entry (README.md, bio.json, experience.yaml, skills.yaml, contact.json) was modified.
  </acceptance_criteria>
  <done>All five test-related FILES entries render source byte-equivalent to their on-disk counterparts; E2E suite exits 0; non-test entries unchanged.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| visitor browser → static rendered SiteHeader/Footer | Pure static content, no untrusted input crosses |
| visitor browser → IDE sidebar editor pane | files-data.ts content rendered as text inside `<pre><code>` — no `dangerouslySetInnerHTML` is involved |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-01 | Tampering | files-data.ts content drift vs. real e2e/ specs | mitigate | Task 3 enforces verbatim source copy; E2E suite asserts the synced strings render in the editor pane |
| T-04-02 | Information Disclosure | files-data.ts displayed source | accept | The displayed content is the public test code already committed to the repo — no secrets, tokens, or PII embedded |
| T-04-03 | Tampering (XSS) | IDE sidebar editor pane rendering of files-data content | accept | Editor pane renders content inside `<pre><code>` as text (no `dangerouslySetInnerHTML`); CLAUDE.md "What NOT to Use" explicitly forbids dangerouslySetInnerHTML for LLM/dynamic output, and this static data follows the same safe path |
| T-04-04 | Information Disclosure | SiteHeader title text | accept | Title is public role information — same data already on LinkedIn/resume |
</threat_model>

<verification>
- `pnpm typecheck` exits 0.
- `pnpm lint` exits 0 (no new ESLint violations introduced).
- `pnpm exec playwright test e2e/ide-interactions.spec.ts e2e/landing.spec.ts --reporter=line` exits 0.
- Manual visual check (dev server `pnpm dev`): SiteHeader on `/` and `/about` shows `Senior SDET / QA Automation Engineer`; clicking `tests/landing.spec.ts` in the IDE sidebar shows source that matches `e2e/landing.spec.ts` line-for-line in the editor pane.
</verification>

<success_criteria>
- DEBT-01: SiteHeader renders `Senior SDET / QA Automation Engineer` on every page where SiteHeader appears.
- DEBT-02: Footer.tsx verified — `© 2026 Ruslan Kanatbek` is present and `© 2024` is absent; no code change made.
- DEBT-04: All five `FILES['tests/...'].content` strings in files-data.ts render the current on-disk source (including the `.first()` chain, the `locator('aside')` scoping, and the FAIL-gating comments).
- E2E test suite still passes (no regression from the content swap).
- TypeScript compile clean.
</success_criteria>

<output>
Create `.planning/phases/04-tech-debt-sweep/04-01-SUMMARY.md` when done. The summary MUST record: (a) the exact line numbers changed in SiteHeader.tsx, (b) the verification outcome for Footer.tsx (no edit), (c) the list of FILES keys resynced in files-data.ts and a one-line diff summary per key.
</output>
</content>
</invoke>