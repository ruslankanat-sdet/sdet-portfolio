---
phase: 09-recruiter-view
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/recruiter/recruiter.module.css
  - src/components/recruiter/RecruiterView.tsx
  - src/components/recruiter/Masthead.tsx
  - src/components/recruiter/Hero.tsx
  - src/components/recruiter/__tests__/RecruiterView.test.tsx
  - src/app/page.tsx
  - e2e/recruiter.spec.ts
autonomous: true
requirements: [REC-01, REC-02]
tags: [recruiter, css-modules, sticky-header, next-app-router, react]

must_haves:
  truths:
    - "A recruiter (mode='recruiter') sees the editorial cream page background instead of the dark IDE background"
    - "The recruiter sees a sticky masthead at the top of the scrolling content with wordmark 'ruslan.kanat' and an 'Engineer view ↗' pill"
    - "Clicking the 'Engineer view ↗' pill writes localStorage['resume-mode']='ide' and renders the IDE view without page navigation"
    - "Below the masthead the recruiter sees an availability pill (with a pulsing forest dot when reduced-motion is not preferred), an editorial headline, a pitch paragraph, and two CTAs: 'Download PDF ↓' and 'alex@morgan.dev →'"
    - "Clicking the 'Download PDF ↓' button invokes window.print()"
    - "The masthead remains visible (sticky) when the user scrolls the recruiter view"
  artifacts:
    - path: "src/components/recruiter/recruiter.module.css"
      provides: "All scoped recruiter styles, avail-pulse @keyframes, two-level scroll container, sticky masthead, hero, CTA buttons"
      contains: ".recruiterScrollRoot, .recruiter, .masthead, .hero, @keyframes avail-pulse"
    - path: "src/components/recruiter/RecruiterView.tsx"
      provides: "Article wrapper with scroll root + Masthead + Hero composition; 'use client'"
      exports: ["RecruiterView"]
    - path: "src/components/recruiter/Masthead.tsx"
      provides: "Sticky header with LogoMark + wordmark + 'Engineer view ↗' button"
      exports: ["Masthead"]
    - path: "src/components/recruiter/Hero.tsx"
      provides: "Availability pill + editorial headline + pitch + Download PDF (window.print) + email CTA"
      exports: ["Hero"]
    - path: "src/components/recruiter/__tests__/RecruiterView.test.tsx"
      provides: "Vitest unit tests covering REC-01 and REC-02 render + interaction assertions"
      contains: "describe('RecruiterView — rendering'), describe('RecruiterView — click interactions')"
    - path: "src/app/page.tsx"
      provides: "ResumeGateInner branch for mode==='recruiter' renders <RecruiterView onSwitchToIDE={...}/>"
      contains: "import { RecruiterView }"
    - path: "e2e/recruiter.spec.ts"
      provides: "New Playwright spec with localStorage pre-seed; covers REC-01 + REC-02 + WCAG AA axe scan"
      contains: "test.describe('Recruiter view'"
  key_links:
    - from: "src/app/page.tsx (mode==='recruiter' branch)"
      to: "src/components/recruiter/RecruiterView.tsx"
      via: "import + JSX render with onSwitchToIDE prop"
      pattern: "<RecruiterView\\s+onSwitchToIDE="
    - from: "src/components/recruiter/Masthead.tsx (Engineer view button onClick)"
      to: "src/app/page.tsx ResumeGateInner setMode('ide')"
      via: "onSwitchToIDE prop callback chain"
      pattern: "onClick=\\{onSwitchToIDE\\}"
    - from: "src/components/recruiter/Hero.tsx (Download PDF onClick)"
      to: "browser window.print()"
      via: "onClick handler"
      pattern: "window\\.print\\(\\)"
    - from: "src/components/recruiter/recruiter.module.css .masthead"
      to: ".recruiterScrollRoot (scroll ancestor)"
      via: "position: sticky requires overflow-y: auto on ancestor"
      pattern: "position:\\s*sticky"
---

<objective>
Build the foundation of the recruiter editorial view: CSS module with two-level scroll container (full-bleed cream background + max-width 720px column), sticky masthead with the IDE-switch pill, hero section with availability pill (pulsing dot), headline, pitch, and two CTAs (Download PDF via window.print() and mailto link). Wire `RecruiterView` into `ResumeGateInner` to replace the existing stub. Create unit test scaffold and new E2E spec.

Purpose: REC-01 and REC-02 must be satisfied with a sticky masthead and hero that visually match the locked design contract (09-UI-SPEC.md), and the scroll containment pattern must be correctly established so that Plan 02 can add the long-scrolling content sections.

Output: 5 new files under `src/components/recruiter/`, 1 modified `src/app/page.tsx`, 1 new `e2e/recruiter.spec.ts`. After this plan, navigating to `/` with `localStorage['resume-mode']='recruiter'` shows the cream-background masthead+hero and clicking Engineer view returns to the IDE.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-recruiter-view/09-RESEARCH.md
@.planning/phases/09-recruiter-view/09-UI-SPEC.md
@.planning/phases/09-recruiter-view/09-PATTERNS.md
@.planning/design/design_handoff_resume_v1/recruiter.jsx
@src/app/page.tsx
@src/components/door/LogoMark.tsx
@src/components/door/DoorScreen.tsx
@src/components/door/__tests__/DoorScreen.test.tsx
@src/app/(ide)/about/about.module.css
@src/app/globals.css

<interfaces>
<!-- Extracted contracts: do not re-explore the codebase -->

From src/components/door/LogoMark.tsx:
- interface LogoMarkProps { size?: number; }
- export function LogoMark(props: LogoMarkProps): JSX.Element  // default size=18, aria-hidden SVG

From src/app/page.tsx (existing pattern, do not change shape):
- const MODE_KEY = 'resume-mode';
- type Mode = 'recruiter' | 'ide' | null;
- Existing recruiter branch lives at lines 60–78 inside ResumeGateInner (the stub div to replace)
- Existing setMode + localStorage pattern at lines 82–91 (onChooseRecruiter/onChooseIDE)

Tokens available in globals.css (DO NOT redeclare):
- --paper #f4ecdc, --paper-soft #faf4e6, --paper-deep #ede4d0
- --ink, --ink-soft, --ink-muted, --ink-faint
- --rule #d8d0bd, --rule-soft #e4ddca
- --forest oklch(0.40 0.09 152), --forest-deep, --forest-soft, --forest-line
- --serif var(--font-newsreader), --mono var(--font-mono)

Scroll constraint:
- .site-body has overflow:hidden; height:100dvh — recruiter view MUST own its own scroll context via `.recruiterScrollRoot { height: 100%; overflow-y: auto; }`
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create recruiter.module.css with scroll container, sticky masthead, hero, avail-pulse keyframe, and CTA styles</name>
  <files>src/components/recruiter/recruiter.module.css</files>
  <read_first>
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (Typography, Color, Component Inventory: RecruiterView, Masthead, Hero, Availability pill, Primary/Secondary CTA, Section component)
    - .planning/phases/09-recruiter-view/09-PATTERNS.md (recruiter.module.css section — scroll container, @keyframes scoping, sticky pattern, full code blocks)
    - .planning/phases/09-recruiter-view/09-RESEARCH.md (Pattern 1 scroll containment, Pattern 4 keyframes-in-modules, Pitfalls 1+2+4)
    - src/app/globals.css (verify --paper-*, --ink-*, --forest-*, --rule-*, --serif, --mono tokens already declared so this file references not redeclares)
    - src/app/(ide)/about/about.module.css (analog scroll container pattern lines 1–8)
    - .planning/design/design_handoff_resume_v1/resume.css (reference CSS for exact spacing/colors when in doubt)
  </read_first>
  <behavior>
    - The `.recruiterScrollRoot` class fills its parent height, scrolls vertically, and paints the full-bleed cream background — so the masthead can stick and the area outside the 720px column does not bleed dark IDE background through.
    - The `.recruiter` (article) class is a centered 720px max-width editorial column with serif body type, 18px / 1.55 line-height, 32px 40px 120px padding.
    - The `.masthead` is `position: sticky; top: 0; z-index: 10; background: var(--paper)` so content scrolls underneath it; bottom border `1px solid var(--rule)`, bottom padding 18px, bottom margin 64px; flex space-between row.
    - `.mastheadL` is a left flex cluster (logo box + wordmark + sep + section label) using mono typography; `.mastheadLogo` is a 26×26 rounded forest-tinted box containing the LogoMark.
    - `.mastheadSwitch` button is a 999px pill with `1px solid var(--rule)` border and `var(--paper-soft)` background; hover state turns border + text + bg to forest tones; the inner `.arrow` element transitions `transform .2s` and nudges to `translate(2px,-2px)` on hover.
    - The `.hero` section has 80px bottom margin; eyebrow row contains `.avail` pill, `.ghost` ghost text, and `.sep` separator.
    - `.avail` pill has forest-tinted gradient background, 1px forest-line border, forest box-shadow glow, and a `::before` pseudo-element 7×7 forest dot.
    - `@keyframes avail-pulse` lives in this same file (NOT globals.css) and is wired via `@media (prefers-reduced-motion: no-preference) { .availDot::before { animation: avail-pulse 1.8s ease-in-out infinite; } }`.
    - `.headline` is a serif display headline using `font-size: clamp(40px, 5.4vw, 64px)`, weight 400, line-height 1.04, letter-spacing -0.022em; nested `em` elements are italic, weight 500, forest color.
    - `.pitch` is 20px serif, 1.55 line-height.
    - `.ctas` flex row with 12px gap.
    - `.btnPrimary` is a 999px pill, 13px 20px padding, forest background, paper-soft text, 12px mono uppercase 0.08em tracking; hover background turns forest-deep with translateY(-1px) lift, 0.15s transition.
    - `.btnSecondary` is the same shape with transparent background, forest border, forest text; hover background tints to forest-soft.
    - `.section` placeholder class with `margin-bottom: 72px`; `.sectionHead` flex row align-items baseline gap 16px; `.sectionNum` mono 12px forest with 0.08em tracking; `.sectionTitle` mono 14px uppercase 0.14em tracking weight 500 ink-muted color; `.sectionRule` flex:1 1px height var(--rule) background.
    - File must include the `@media print` block scaffolding for Plan 02 to expand later: reset `overflow: visible; height: auto` on `.recruiterScrollRoot`, `.recruiter { max-width: 100%; padding: 0 }`, hide `.mastheadSwitch`, hide `.ctas`.
    - Initial `@media (max-width: 600px)` block: `.recruiter { padding: 24px 24px 80px }` only (other breakpoints come in Plan 02).
  </behavior>
  <action>
    Create `src/components/recruiter/recruiter.module.css` from scratch. Reference design tokens declared in globals.css using `var(--paper)`, `var(--ink)`, `var(--forest)`, `var(--rule)`, `var(--serif)`, `var(--mono)` etc. — do not redeclare any token. Use kebab-case CSS module class names so React imports them as camelCase from `styles` object (e.g. CSS `.masthead-switch` → JS `styles.mastheadSwitch`). Required class names in JS access form (CSS uses kebab equivalents): recruiterScrollRoot, recruiter, masthead, mastheadL, mastheadLogo, mastheadWordmark, mastheadSep, mastheadSection, mastheadSwitch, arrow, hero, eyebrow, avail, availDot, availLabel, availSep, availValue, ghost, sep, headline, pitch, ctas, btnPrimary, btnSecondary, section, sectionHead, sectionNum, sectionTitle, sectionRule. Implement avail-pulse keyframes per 09-UI-SPEC.md exact spec: 0%/100% box-shadow 0 0 6px var(--forest), 0 0 0 0 oklch(0.55 0.16 152 / 0.45) and 50% box-shadow 0 0 10px var(--forest), 0 0 0 6px oklch(0.55 0.16 152 / 0). Include @media print scaffold per Pitfall 4 (reset scroll root overflow:visible height:auto; hide masthead switch; hide footer placeholder; hide CTAs; remove recruiter padding/max-width). Include single @media (max-width: 600px) rule for `.recruiter` padding only — additional breakpoints belong to Plan 02. Do NOT use globalThis prefixes or `:global()` — keep keyframe name `avail-pulse` local to module (CSS Modules will scope it and link the animation property automatically).
  </action>
  <verify>
    <automated>test -f src/components/recruiter/recruiter.module.css &amp;&amp; grep -v '^[[:space:]]*\*' src/components/recruiter/recruiter.module.css | grep -v '^[[:space:]]*\/\*' | grep -cE '\.recruiter-scroll-root|\.recruiterScrollRoot' | head -1</automated>
    <automated>grep -c '@keyframes avail-pulse' src/components/recruiter/recruiter.module.css | grep -q '^1$' &amp;&amp; grep -c 'position: sticky' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -c 'var(--paper)' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]' &amp;&amp; grep -c 'var(--forest)' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>! grep -E '^\s*--paper\s*:' src/components/recruiter/recruiter.module.css &amp;&amp; ! grep -E '^\s*--forest\s*:' src/components/recruiter/recruiter.module.css</automated>
    <automated>grep -c '@media print' src/components/recruiter/recruiter.module.css | grep -q '^1$'</automated>
  </verify>
  <acceptance_criteria>
    - File `src/components/recruiter/recruiter.module.css` exists
    - File contains exactly one `@keyframes avail-pulse` block
    - File contains at least one `position: sticky` declaration (on `.masthead`)
    - File references `var(--paper)` and `var(--forest)` at least once each
    - File does NOT redeclare `--paper:` or `--forest:` (these are global tokens)
    - File contains exactly one `@media print` block
    - File contains an `@media (max-width: 600px)` block adjusting `.recruiter` padding
    - File contains `@media (prefers-reduced-motion: no-preference)` gating the avail-pulse animation
    - Class names present (kebab or camel CSS form) include: recruiter-scroll-root, recruiter, masthead, masthead-switch, hero, avail, avail-dot, headline, pitch, ctas, btn-primary, btn-secondary, section, section-head, section-num, section-title, section-rule
  </acceptance_criteria>
  <done>recruiter.module.css exists with all required selectors, the locally-scoped `avail-pulse` keyframe, sticky masthead positioning, print scaffold, and the first mobile breakpoint — all referencing globals.css tokens with zero redeclaration</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Masthead, Hero, and RecruiterView components; wire into ResumeGateInner; write unit tests covering REC-01 + REC-02</name>
  <files>src/components/recruiter/Masthead.tsx, src/components/recruiter/Hero.tsx, src/components/recruiter/RecruiterView.tsx, src/components/recruiter/__tests__/RecruiterView.test.tsx, src/app/page.tsx</files>
  <read_first>
    - src/app/page.tsx (current ResumeGateInner stub at lines 60–78 and the setMode pattern at lines 82–91 must remain consistent)
    - src/components/door/LogoMark.tsx (import target, do not duplicate)
    - src/components/door/DoorScreen.tsx (analog for `"use client"`, prop interface, button pattern)
    - src/components/door/__tests__/DoorScreen.test.tsx (test scaffold pattern to mirror exactly)
    - .planning/phases/09-recruiter-view/09-PATTERNS.md (RecruiterView/Masthead/Hero component sections — JSX structure already specified)
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (Copywriting Contract — lift exact strings: "ruslan.kanat", "résumé", "Engineer view ↗", "Status", "Available · Q3 start", "Remote · UTC-5", "Staff / Principal IC", full headline, full pitch, "Download PDF ↓", "alex@morgan.dev")
    - .planning/design/design_handoff_resume_v1/recruiter.jsx (lines 46–94 — Masthead + Hero source of truth for structure)
  </read_first>
  <behavior>
    Masthead.tsx behavior:
    - Renders `<header>` element (semantic banner role)
    - Contains LogoMark (size 16, imported from `../door/LogoMark`), wordmark "ruslan.kanat", "/" separator, "résumé" label
    - Renders a `<button type="button">` with accessible name "Engineer view ↗" that calls `onSwitchToIDE` when clicked
    - Receives `{ onSwitchToIDE: () => void }` as the only prop

    Hero.tsx behavior:
    - Renders a `<section>` with availability pill: dot span + "Status" mono label + ":" separator + "Available · Q3 start" value
    - Eyebrow row also contains "Remote · UTC-5" and "Staff / Principal IC" ghost text spans separated by "·"
    - Renders `<h1>` headline with text "I build the test infrastructure that keeps " + `<em>AI products</em>` + " honest in production."
    - Renders the pitch paragraph with full copy from 09-UI-SPEC.md row "Hero pitch"
    - Renders a CTA row with `<button type="button">` "Download PDF ↓" that calls `e.preventDefault()` then `window.print()`, and an `<a href="mailto:alex@morgan.dev">` showing "alex@morgan.dev →"

    RecruiterView.tsx behavior:
    - Top-level `"use client"` directive
    - Receives `{ onSwitchToIDE: () => void }` prop
    - Returns `<div class={styles.recruiterScrollRoot}><article class={styles.recruiter}>` wrapping Masthead, Hero, and a placeholder comment for future sections (Plan 02 expands)
    - Passes `onSwitchToIDE` down to Masthead; Hero needs no prop

    page.tsx wire-up behavior:
    - Replace the `if (mode === 'recruiter')` stub (lines 60–78) with `<RecruiterView onSwitchToIDE={() => { try { localStorage.setItem(MODE_KEY, 'ide'); } catch {} setMode('ide'); }} />`
    - Add `import { RecruiterView } from '@/components/recruiter/RecruiterView';` near the existing door import
    - Do NOT alter the ide branch, the door branch, the reset logic, or the Suspense wrapper

    Test file behavior (vitest + testing-library + user-event):
    - Mocks `window.print` via `Object.defineProperty(window, 'print', { value: vi.fn(), writable: true })` in beforeEach
    - REC-01 assertions: renders wordmark "ruslan.kanat", renders Engineer view button by accessible name /Engineer view/i, clicking that button calls onSwitchToIDE exactly once
    - REC-02 assertions: renders Status label + Available · Q3 start value (or the avail pill aria-presentation), renders the headline substring "AI products" inside an `<em>`, renders the pitch substring "Nine years writing self-healing", renders Download PDF button, renders mailto link with href="mailto:alex@morgan.dev"; clicking Download PDF calls window.print exactly once
    - Footer test (the "Open the IDE" button) is OUT OF SCOPE for this plan — Plan 02 adds the RecruiterFooter; do not add that test here
  </behavior>
  <action>
    Create three component files under `src/components/recruiter/`. All three start with `'use client';`. Each imports `styles from './recruiter.module.css'`. Use kebab-to-camel CSS Module access (`styles.recruiterScrollRoot`, `styles.mastheadSwitch`, `styles.btnPrimary`, etc.). Import LogoMark via relative path `import { LogoMark } from '../door/LogoMark';`. Masthead and RecruiterView declare prop interface `{ onSwitchToIDE: () => void }`. The Engineer view button uses `<button type="button" className={styles.mastheadSwitch} onClick={onSwitchToIDE}>` with two child spans (label text "Engineer view" and `<span className={styles.arrow}>↗</span>`). Hero Download PDF button uses `<button type="button" className={styles.btnPrimary} onClick={(e) => { e.preventDefault(); window.print(); }}>` with child spans "Download PDF" and "↓". Mailto link uses `<a className={styles.btnSecondary} href="mailto:alex@morgan.dev">` with child spans "alex@morgan.dev" and "→". Lift the exact copy from 09-UI-SPEC.md Copywriting Contract — no paraphrasing. RecruiterView composes `<div className={styles.recruiterScrollRoot}><article className={styles.recruiter}><Masthead onSwitchToIDE={onSwitchToIDE} /><Hero /></article></div>` plus a JSX comment `{/* Sections, contact, footer added in Plan 02 */}`. In `src/app/page.tsx`, add the import after the existing DoorScreen import (line 7) and replace lines 60–78 (the stub) with the RecruiterView render branch — match the existing setMode + try/catch localStorage pattern verbatim. Test file mirrors `DoorScreen.test.tsx` exactly: starts with `/// <reference types="vitest/globals" />`, imports render/screen/userEvent/vi/RecruiterView, defines `defaultProps = { onSwitchToIDE: vi.fn() }`, runs `vi.clearAllMocks()` plus the window.print mock in beforeEach, two describe blocks ("RecruiterView — rendering" with REC-01/REC-02 render assertions and "RecruiterView — interactions" with click assertions).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&amp;1 | tee /tmp/tsc-09-01.log | grep -E 'error TS' | wc -l | grep -q '^0$' || (echo "TSC errors:"; cat /tmp/tsc-09-01.log; exit 1)</automated>
    <automated>pnpm test -- RecruiterView 2>&amp;1 | tail -30 | grep -E 'Test Files\s+[0-9]+ passed' &amp;&amp; pnpm test -- RecruiterView 2>&amp;1 | tail -30 | grep -E 'Tests\s+[0-9]+ passed'</automated>
    <automated>grep -c "from '@/components/recruiter/RecruiterView'" src/app/page.tsx | grep -q '^1$'</automated>
    <automated>! grep -q 'Recruiter view — coming in Phase 9' src/app/page.tsx</automated>
    <automated>grep -c 'use client' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]'</automated>
    <automated>grep -q "import { LogoMark } from '../door/LogoMark'" src/components/recruiter/Masthead.tsx</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/recruiter/RecruiterView.tsx` exports default or named `RecruiterView`, begins with `'use client'`, accepts `onSwitchToIDE: () => void` prop, and renders `<div>` + `<article>` wrapping `<Masthead>` and `<Hero>`
    - `src/components/recruiter/Masthead.tsx` renders a `<header>` containing `<button type="button">` with accessible name matching `/Engineer view/i`; calls `onSwitchToIDE` on click
    - `src/components/recruiter/Hero.tsx` renders `<h1>` containing `<em>AI products</em>`, the pitch paragraph, a `<button type="button">` named `/Download PDF/i`, and an `<a href="mailto:alex@morgan.dev">` element
    - `src/app/page.tsx` no longer contains the literal string `Recruiter view — coming in Phase 9`
    - `src/app/page.tsx` contains exactly one import line for `RecruiterView` from `@/components/recruiter/RecruiterView`
    - `pnpm test -- RecruiterView` reports `Test Files  1 passed` and at least 5 tests passing (3 render + 2 interaction)
    - `pnpm exec tsc --noEmit` exits with zero TypeScript errors
    - `Masthead.tsx` imports `LogoMark` from `../door/LogoMark` (no duplicate file in recruiter/)
  </acceptance_criteria>
  <done>RecruiterView+Masthead+Hero render the masthead and hero exactly as 09-UI-SPEC.md specifies, page.tsx routes mode==='recruiter' to RecruiterView, REC-01 and REC-02 are covered by passing Vitest unit tests, and no LogoMark duplicate exists</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create e2e/recruiter.spec.ts Playwright spec with localStorage pre-seed, REC-01/REC-02 assertions, and WCAG AA axe scan</name>
  <files>e2e/recruiter.spec.ts</files>
  <read_first>
    - e2e/landing.spec.ts (analog — exact patterns for `test.beforeEach addInitScript localStorage`, AxeBuilder usage, page.goto, role-based locators)
    - .planning/phases/09-recruiter-view/09-PATTERNS.md (e2e/recruiter.spec.ts section — localStorage pre-seed, navigation, axe scan)
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (Copywriting Contract for stable assertion targets)
  </read_first>
  <behavior>
    - `test.describe('Recruiter view')` with `test.beforeEach` adding `localStorage.setItem('resume-mode', 'recruiter')` via `page.addInitScript`
    - Test "REC-01: shows masthead wordmark and Engineer view pill" — goto '/', expect getByText('ruslan.kanat') visible, expect getByRole('button', { name: /Engineer view/i }) visible
    - Test "REC-01: clicking Engineer view switches to IDE" — goto '/', click the Engineer view button, expect getByRole('button', { name: /README\.md/ }).first() visible AND localStorage value to equal 'ide'
    - Test "REC-02: hero renders availability, headline em, pitch, two CTAs" — goto '/', expect getByText('Available · Q3 start') visible, expect page.locator('h1 em').filter({ hasText: 'AI products' }) visible, expect getByText(/Nine years writing/i) visible, expect getByRole('button', { name: /Download PDF/i }) visible, expect page.getByRole('link', { name: /alex@morgan\.dev/i }) visible with href starting with 'mailto:'
    - Test "REC-A11Y: RecruiterView has no WCAG AA violations" — goto '/', AxeBuilder withTags wcag2a/wcag2aa/wcag21a/wcag21aa, expect violations to equal []
    - The Download PDF test must NOT actually invoke window.print() (it would hang Playwright); only assert the button is present and clickable. Window.print E2E coverage is intentionally left to unit tests.
  </behavior>
  <action>
    Create `e2e/recruiter.spec.ts` mirroring the structure of `e2e/landing.spec.ts` lines 1–9 + 49–56. Use `import { test, expect } from '@playwright/test';` and `import AxeBuilder from '@axe-core/playwright';` at the top. Wrap all tests in `test.describe('Recruiter view', () => { ... })`. The `test.beforeEach` calls `page.addInitScript(() => { localStorage.setItem('resume-mode', 'recruiter'); })`. Use stable locators: `page.getByText('ruslan.kanat')`, `page.getByRole('button', { name: /Engineer view/i })`, `page.getByRole('button', { name: /Download PDF/i })`, `page.getByRole('link', { name: /alex@morgan\.dev/i })`. The Engineer view click test reads localStorage via `await page.evaluate(() => localStorage.getItem('resume-mode'))` and asserts `'ide'`. The axe test uses `withTags(['wcag2a', 'wcag2aa', 'wcag21a', 'wcag21aa'])`. Do NOT add a test that triggers `window.print()` — skip that path in E2E.
  </action>
  <verify>
    <automated>test -f e2e/recruiter.spec.ts &amp;&amp; grep -c "test.describe('Recruiter view'" e2e/recruiter.spec.ts | grep -q '^1$'</automated>
    <automated>grep -c "localStorage.setItem('resume-mode', 'recruiter')" e2e/recruiter.spec.ts | grep -qE '^[1-9]'</automated>
    <automated>grep -c 'AxeBuilder' e2e/recruiter.spec.ts | grep -qE '^[1-9]'</automated>
    <automated>! grep -E 'window\.print\(\)' e2e/recruiter.spec.ts</automated>
    <automated>pnpm exec tsc --noEmit 2>&amp;1 | grep -E 'recruiter\.spec\.ts.*error TS' | wc -l | grep -q '^0$'</automated>
  </verify>
  <acceptance_criteria>
    - `e2e/recruiter.spec.ts` exists with `import { test, expect } from '@playwright/test';` on line 1
    - File imports AxeBuilder from '@axe-core/playwright'
    - File contains exactly one `test.describe('Recruiter view'` block
    - `test.beforeEach` calls `page.addInitScript` with `localStorage.setItem('resume-mode', 'recruiter')`
    - File contains at least 4 `test(...)` declarations: masthead render, Engineer view click+localStorage assertion, hero render (avail + headline em + pitch + CTAs), axe WCAG scan
    - File does NOT call or expect `window.print()` (would hang Playwright)
    - `pnpm exec tsc --noEmit` reports no errors in `e2e/recruiter.spec.ts`
  </acceptance_criteria>
  <done>e2e/recruiter.spec.ts provides REC-01 and REC-02 E2E coverage with WCAG AA axe scan; ready to expand in Plan 02 with REC-03 through REC-10 tests; no print call leaks into E2E</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → localStorage | RecruiterView writes 'resume-mode' value via onSwitchToIDE; ResumeGateInner reads it back. No PII; cleared by ?reset. |
| client → browser window.print() | Download PDF CTA triggers the browser print dialog. No data leaves the page. |
| SSR boundary → window access | `window.print()` and `localStorage` are browser-only APIs; `RecruiterView`/`Hero` must run on the client only. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-01 | Tampering | `Hero.tsx` Download PDF onClick | accept | Triggers browser-native `window.print()` only; no fetch, no dynamic eval, no remote payload. Risk: none. |
| T-09-02 | Information Disclosure | `Masthead.tsx`/`RecruiterFooter.tsx` external nav | mitigate (Plan 02) | Plan 01 has no external links yet (Masthead only switches modes). ContactSection in Plan 02 will own `rel="noopener noreferrer"` on GitHub/LinkedIn anchors. |
| T-09-03 | Tampering / XSS | All recruiter components | mitigate | All content is hardcoded JSX strings from 09-UI-SPEC.md Copywriting Contract. ZERO use of `dangerouslySetInnerHTML` — verified by grep gate in Plan 02 final task. No user input rendered. |
| T-09-04 | Denial of Service | `window.print()` invocation | accept | Browser blocks repeated print dialogs by default; user-initiated. Risk: trivial. |
| T-09-05 | SSR window access | `Hero.tsx`, `RecruiterView.tsx` | mitigate | `'use client'` directive at the top of every component touching `window`. Parent `ResumeGateInner` is already `'use client'` so the subtree is client-rendered; the explicit directive on `RecruiterView.tsx` and `Hero.tsx` is a defense-in-depth measure against accidental Server Component imports. |
</threat_model>

<verification>
After Task 3 completes, run:

```bash
pnpm test -- RecruiterView
pnpm exec tsc --noEmit
pnpm exec playwright test recruiter --reporter=line
```

Expected:
- Vitest: 1 Test File passed, ≥5 tests passing (REC-01 render + REC-01 click + REC-02 render + REC-02 download click + headline em)
- tsc: zero errors
- Playwright: 4 tests passing in `e2e/recruiter.spec.ts` (masthead, Engineer view click, hero, axe)

Note: `e2e/landing.spec.ts` DOOR-03 test (line 86) will FAIL at the end of this plan because the stub text is gone and the assertion is not yet updated. This is expected and intentional — Plan 02 updates that line.
</verification>

<success_criteria>
- [ ] `src/components/recruiter/` directory exists with 4 files (recruiter.module.css, Masthead.tsx, Hero.tsx, RecruiterView.tsx) plus `__tests__/RecruiterView.test.tsx`
- [ ] `src/app/page.tsx` recruiter branch renders `<RecruiterView onSwitchToIDE={...} />` (stub removed)
- [ ] `e2e/recruiter.spec.ts` exists with 4 tests
- [ ] `pnpm test -- RecruiterView` passes
- [ ] `pnpm exec tsc --noEmit` reports zero errors
- [ ] Manual: navigating to `/` with `localStorage['resume-mode']='recruiter'` shows the cream-background masthead+hero
- [ ] Manual: clicking "Engineer view ↗" switches to IDE view and localStorage updates to 'ide'
- [ ] Manual: clicking "Download PDF ↓" opens the browser print dialog
- [ ] No `dangerouslySetInnerHTML` anywhere in the new files
- [ ] LogoMark NOT duplicated under `src/components/recruiter/`
</success_criteria>

<output>
Create `.planning/phases/09-recruiter-view/09-01-SUMMARY.md` when done summarising files created/modified, requirements satisfied (REC-01, REC-02), threat dispositions, and any deviations from the plan.
</output>
