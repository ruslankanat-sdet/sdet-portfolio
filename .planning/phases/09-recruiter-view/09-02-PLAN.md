---
phase: 09-recruiter-view
plan: 02
type: execute
wave: 2
depends_on: [09-01]
files_modified:
  - src/components/recruiter/recruiter.module.css
  - src/components/recruiter/RecruiterView.tsx
  - src/components/recruiter/Metrics.tsx
  - src/components/recruiter/Timeline.tsx
  - src/components/recruiter/Skills.tsx
  - src/components/recruiter/AvailabilityCard.tsx
  - src/components/recruiter/ContactSection.tsx
  - src/components/recruiter/RecruiterFooter.tsx
  - src/components/recruiter/__tests__/RecruiterView.test.tsx
  - e2e/recruiter.spec.ts
  - e2e/landing.spec.ts
autonomous: true
requirements: [REC-03, REC-04, REC-05, REC-06, REC-07, REC-08, REC-10]
tags: [recruiter, css-modules, mobile-responsive, editorial-layout, e2e]

must_haves:
  truths:
    - "A recruiter sees a §01 'By the numbers' section with four stat tiles (9 yrs / 0.4 % / 98.2 % / 1,247) bordered top + bottom by 1px rule"
    - "A recruiter sees a §02 'What I'm doing now' italic lede paragraph whose first letter is rendered as a forest-colored drop cap"
    - "A recruiter sees a §03 'Experience' timeline with three jobs (Staff SDET / Senior SDET / Automation Engineer) in a date-span + body two-column grid, with the 2023 span year rendered in forest"
    - "A recruiter sees a §04 'Stack' section with four skill groups (Automation, AI / ML, Infra, Languages) each rendered as a category label + serif pill cluster"
    - "A recruiter sees a §05 'What I'm looking for' six-row spec card (Status / Location / Level / Best fit / Comp / Visa) with the Status value rendered in forest"
    - "A recruiter sees a contact section with the large editorial email line and a three-column Email/GitHub/LinkedIn grid; GitHub and LinkedIn anchors have rel='noopener noreferrer'"
    - "A recruiter sees a footer with the copyright text and a 'Prefer the engineer view? Open the IDE →' button that switches to the IDE view on click"
    - "On a 375px viewport the metrics wrap to 2 columns, the experience timeline stacks to a single column, the availability card stacks vertically (at ≤540px), and the contact grid collapses to 1 column"
    - "The DOOR-03 same-session E2E test in e2e/landing.spec.ts no longer asserts the stub phrase and instead asserts a stable RecruiterView element"
    - "No component file contains dangerouslySetInnerHTML"
  artifacts:
    - path: "src/components/recruiter/Metrics.tsx"
      provides: "§01 four-tile metrics grid"
      exports: ["Metrics"]
    - path: "src/components/recruiter/Timeline.tsx"
      provides: "§03 three-job experience timeline with current-year forest accent"
      exports: ["Timeline"]
    - path: "src/components/recruiter/Skills.tsx"
      provides: "§04 four skill groups + serif pills"
      exports: ["Skills"]
    - path: "src/components/recruiter/AvailabilityCard.tsx"
      provides: "§05 six-row spec sheet with Status .good forest accent"
      exports: ["AvailabilityCard"]
    - path: "src/components/recruiter/ContactSection.tsx"
      provides: "Contact line + 3-col grid with rel='noopener noreferrer' on external anchors"
      exports: ["ContactSection"]
    - path: "src/components/recruiter/RecruiterFooter.tsx"
      provides: "Footer with copyright + Open the IDE button"
      exports: ["RecruiterFooter"]
    - path: "src/components/recruiter/recruiter.module.css"
      provides: "All new selectors for sections + mobile breakpoints + full print block"
      contains: ".metricsGrid, .timelineJob, .skillGroup, .availCard, .contactGrid, .footer, .now (drop-cap)"
  key_links:
    - from: "src/components/recruiter/RecruiterView.tsx"
      to: "Metrics, Timeline, Skills, AvailabilityCard, ContactSection, RecruiterFooter"
      via: "JSX composition under <article class={styles.recruiter}>"
      pattern: "<(Metrics|Timeline|Skills|AvailabilityCard|ContactSection|RecruiterFooter)"
    - from: "src/components/recruiter/RecruiterFooter.tsx (onClick)"
      to: "ResumeGateInner setMode('ide') via onSwitchToIDE prop chain"
      via: "callback prop"
      pattern: "onClick=\\{onSwitchToIDE\\}"
    - from: "src/components/recruiter/ContactSection.tsx (anchor)"
      to: "github.com and linkedin.com"
      via: "anchor href + rel='noopener noreferrer'"
      pattern: "rel=\\\"noopener noreferrer\\\""
    - from: "e2e/landing.spec.ts line 86"
      to: "RecruiterView masthead element"
      via: "page.getByText('ruslan.kanat') or page.getByRole('button', { name: /Engineer view/i })"
      pattern: "ruslan\\.kanat|Engineer view"
---

<objective>
Add all remaining recruiter editorial sections (§01 Metrics, §02 Now lede with drop-cap, §03 Timeline, §04 Skills, §05 AvailabilityCard, ContactSection, RecruiterFooter) and the full mobile breakpoint matrix to `recruiter.module.css`. Expand the unit test file to cover REC-03/REC-05/REC-07/REC-08. Expand `e2e/recruiter.spec.ts` to cover REC-03 through REC-10 (mobile viewport). Update `e2e/landing.spec.ts` DOOR-03 assertion so the existing landing suite passes after the stub disappears. Complete the `@media print` block (Phase 10 will validate it; Phase 9 ships the CSS).

Purpose: REC-03, REC-04, REC-05, REC-06, REC-07, REC-08, REC-10 must be satisfied with components that visually match 09-UI-SPEC.md and 09-PATTERNS.md, mobile responsiveness must work at 375px, and the previously-stable landing E2E suite must continue passing.

Output: 6 new component files, 1 expanded CSS module, 1 expanded RecruiterView composition, 1 expanded unit test file, 1 expanded E2E spec, 1 single-line update to `e2e/landing.spec.ts`. After this plan, the recruiter view is feature-complete for Phase 9 (Phase 10 will swap in real Ruslan content + verify print).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/09-recruiter-view/09-RESEARCH.md
@.planning/phases/09-recruiter-view/09-UI-SPEC.md
@.planning/phases/09-recruiter-view/09-PATTERNS.md
@.planning/phases/09-recruiter-view/09-01-SUMMARY.md
@.planning/design/design_handoff_resume_v1/recruiter.jsx
@src/components/recruiter/recruiter.module.css
@src/components/recruiter/RecruiterView.tsx
@src/components/recruiter/__tests__/RecruiterView.test.tsx
@e2e/recruiter.spec.ts
@e2e/landing.spec.ts

<interfaces>
<!-- From Plan 01 (already shipped before this plan runs) -->

From src/components/recruiter/RecruiterView.tsx (Plan 01 form):
- 'use client'
- interface RecruiterViewProps { onSwitchToIDE: () => void; }
- export function RecruiterView(props): JSX  // currently renders Masthead + Hero only
- imports styles from './recruiter.module.css'

From src/components/recruiter/recruiter.module.css (Plan 01 form):
- Class names already present: recruiterScrollRoot, recruiter, masthead, mastheadL, mastheadLogo, mastheadWordmark, mastheadSep, mastheadSection, mastheadSwitch, arrow, hero, eyebrow, avail, availDot, availLabel, availSep, availValue, ghost, sep, headline, pitch, ctas, btnPrimary, btnSecondary, section, sectionHead, sectionNum, sectionTitle, sectionRule
- avail-pulse @keyframes already declared
- @media print block scaffold (must be expanded in this plan)
- @media (max-width: 600px) block exists with `.recruiter` padding only (must be expanded in this plan)

From recruiter.jsx (design handoff, source of truth for data):
- METRICS array: { num, unit, label } × 4
- JOBS array: { span, current, role, company, scope, stack[] } × 3
- SKILL_GROUPS array: { cat, items[] } × 4
- Availability rows array: { label, value, good? } × 6
- Contact items: 3 (Email mailto, GitHub external, LinkedIn external)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add all remaining selectors + mobile breakpoints + full print block to recruiter.module.css</name>
  <files>src/components/recruiter/recruiter.module.css</files>
  <read_first>
    - src/components/recruiter/recruiter.module.css (current Plan 01 state — extend, do not rewrite)
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (Component Inventory: Metrics grid, Now paragraph, Timeline, Skills, Availability card, Contact section, Footer, Engineer view pill; Mobile Responsiveness Contract; Print Contract)
    - .planning/phases/09-recruiter-view/09-PATTERNS.md (recruiter.module.css patterns + mobile breakpoint patterns)
    - .planning/design/design_handoff_resume_v1/resume.css (reference CSS for any exact spacing/border behavior questions)
  </read_first>
  <behavior>
    Add the following class blocks (CSS kebab-case; JS camelCase access):

    Now/lede (§02): `.now` italic serif 22px line-height 1.55 max-width 60ch; `.now::first-letter` font-size 1.4em weight 600 color var(--forest) non-italic line-height 1 float left margin-right ~6px (drop-cap style per UI-SPEC).

    Metrics (§01): `.metricsGrid` 4-column grid, border-top + border-bottom 1px solid var(--rule); `.metricTile` right border 1px solid var(--rule) + padding 24px 18px 24px 0; last tile loses right border; `.metricNum` clamp(32px, 4.2vw, 44px) serif weight 500 color forest line-height 1 letter-spacing -0.02em flex baseline row with `.unit` 0.55em ink-muted; `.metricLabel` mono 11px uppercase 0.08em tracking ink-muted line-height 1.4; preserve newlines in label (white-space: pre-line).

    Timeline (§03): `.timelineList` flex column; `.timelineJob` grid-template-columns 120px 1fr gap 32px padding 28px 0 border-bottom 1px solid var(--rule-soft); `.timelineJob:last-child` no bottom border; `.spanCol` mono 12px ink-muted padding-top 6px; `.spanYearCurrent` color forest weight 500; `.jobRole` h3 serif 22px weight 500 line-height 1.25; `.jobCompany` mono 12px uppercase 0.06em tracking ink-muted margin-bottom 14px; `.jobScope` serif 17px ink-soft max-width 60ch line-height 1.55; `.jobStack` flex wrap gap 6px margin-top 12px; `.stackPill` mono 11px ink-muted padding 3px 9px radius 999px bg var(--paper-deep) border 1px solid var(--rule).

    Skills (§04): `.skillsList` flex column gap 20px; `.skillGroup` grid 120px 1fr gap 32px align-items baseline; `.skillCat` mono 12px uppercase 0.08em ink-muted; `.skillPills` flex wrap gap 8px; `.skillPill` serif 15px padding 6px 14px radius 999px bg var(--paper-soft) border 1px solid var(--rule) ink color; hover: border forest, bg forest-soft, color forest, transition 0.15s.

    Availability card (§05): `.availCard` grid-template-columns 1fr 1fr border 1px solid var(--forest-line) border-radius 10px overflow hidden bg linear-gradient(180deg, var(--paper-soft), var(--paper)); `.availRow` flex column gap 6px padding 20px 24px border-right 1px solid var(--forest-line) border-bottom 1px solid var(--forest-line); nth-child(even) no right border; last two children no bottom border; `.availLabelCard` mono 10.5px uppercase 0.1em ink-muted; `.availValueCard` serif 17px ink line-height 1.35; `.good` color forest weight 500.

    Contact section: `.contactSection` margin-top 80px padding-top 48px border-top 2px solid var(--ink); `.contactPre` mono 12px uppercase 0.12em ink-muted margin-bottom 18px; `.contactLine` clamp(32px, 4vw, 42px) serif 400 line-height 1.05 letter-spacing -0.02em text-wrap balance; `.contactLine a` color forest no underline border-bottom 2px solid var(--forest-line); `.contactGrid` grid-template-columns repeat(3, 1fr) gap 24px padding-top 16px; `.contactItem` flex column gap 6px; `.contactItemLabel` mono 10.5px uppercase 0.1em ink-faint; `.contactItemValue` mono 14px ink-color no underline; hover: color forest.

    Footer: `.footer` margin-top 64px padding-top 24px border-top 1px solid var(--rule) flex row space-between align-center mono 11px 0.06em ink-faint; `.footerSwitch` background none border none cursor pointer inherit font uppercase text-decoration underline underline-color var(--rule) underline-offset 4px; hover: color forest, underline-color forest.

    Mobile breakpoints:
    - @media (max-width: 600px) extend the existing block: `.metricsGrid` 2 columns + nth-child(2) right border removed + top two get bottom border; `.timelineJob` grid 1fr gap 8px + `.spanCol` padding-top 0; `.skillGroup` grid 1fr gap 10px; `.contactGrid` grid 1fr gap 16px.
    - @media (max-width: 540px) new block: `.availCard` grid 1fr; `.availRow` no right border; last child no bottom border.

    Full @media print block: keep Plan 01 scaffold and add: `body { background: white; font-size: 11pt; }`, `.recruiter { max-width: 100%; padding: 0; }`, `.mastheadSwitch { display: none !important; }`, `.footer { display: none !important; }`, `.ctas { display: none !important; }`, `.recruiterScrollRoot { overflow: visible; height: auto; }`, `.section { margin-bottom: 24pt; page-break-inside: avoid; }`, `.headline { font-size: 28pt; }`, `.pitch { font-size: 13pt; }`.
  </behavior>
  <action>
    Open `src/components/recruiter/recruiter.module.css` and APPEND new selectors after existing Plan 01 selectors. Do NOT rewrite or reorder Plan 01 content. Use kebab CSS class names (`.metrics-grid`, `.timeline-job`, etc.) so React imports them via camelCase (`styles.metricsGrid`, `styles.timelineJob`). Reference `var(--paper)`, `var(--paper-soft)`, `var(--paper-deep)`, `var(--ink)`, `var(--ink-soft)`, `var(--ink-muted)`, `var(--ink-faint)`, `var(--rule)`, `var(--rule-soft)`, `var(--forest)`, `var(--forest-soft)`, `var(--forest-line)`, `var(--serif)`, `var(--mono)` — never redeclare a token. The drop-cap on `.now` MUST use `::first-letter` pseudo-element with `font-size: 1.4em`, `font-weight: 600`, `color: var(--forest)`, `font-style: normal`, `float: left`, `line-height: 1`, `margin-right: 6px`. Extend the existing `@media (max-width: 600px)` block by adding the new responsive rules INSIDE it (do not create a duplicate block). Add a new `@media (max-width: 540px)` block for the availability card collapse. Expand the existing `@media print` block (add all rules listed in behavior). Verify the file still contains exactly one `@keyframes avail-pulse` and one `@media print` and one `@media (max-width: 600px)` block after the edit.
  </action>
  <verify>
    <automated>grep -c '@keyframes avail-pulse' src/components/recruiter/recruiter.module.css | grep -q '^1$'</automated>
    <automated>grep -c '@media print' src/components/recruiter/recruiter.module.css | grep -q '^1$'</automated>
    <automated>grep -c '@media (max-width: 600px)' src/components/recruiter/recruiter.module.css | grep -q '^1$'</automated>
    <automated>grep -c '@media (max-width: 540px)' src/components/recruiter/recruiter.module.css | grep -q '^1$'</automated>
    <automated>grep -cE '::first-letter' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.metrics-grid|\.metricsGrid' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.timeline-job|\.timelineJob' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.skill-pill|\.skillPill' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.avail-card|\.availCard' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.contact-grid|\.contactGrid' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>grep -cE '\.footer-switch|\.footerSwitch' src/components/recruiter/recruiter.module.css | grep -qE '^[1-9]'</automated>
    <automated>! grep -E '^\s*--(paper|forest|ink|rule)[^:]*:\s' src/components/recruiter/recruiter.module.css</automated>
  </verify>
  <acceptance_criteria>
    - File still contains exactly one `@keyframes avail-pulse` block
    - File contains exactly one `@media print` block (expanded from Plan 01 scaffold)
    - File contains exactly one `@media (max-width: 600px)` block (expanded — do not create a duplicate)
    - File contains exactly one new `@media (max-width: 540px)` block for `.availCard` collapse
    - File contains `::first-letter` rule for the drop-cap on `.now`
    - File contains selectors for: `.metrics-grid`, `.metric-tile`, `.metric-num`, `.metric-label`, `.timeline-job`, `.span-col`, `.span-year-current`, `.job-role`, `.job-company`, `.job-scope`, `.stack-pill`, `.skill-group`, `.skill-cat`, `.skill-pill`, `.avail-card`, `.avail-row`, `.avail-label-card`, `.avail-value-card`, `.good`, `.contact-section`, `.contact-pre`, `.contact-line`, `.contact-grid`, `.contact-item`, `.contact-item-label`, `.contact-item-value`, `.footer`, `.footer-copy`, `.footer-switch`, `.now`
    - File does NOT redeclare any `--paper`, `--forest`, `--ink`, or `--rule` token
  </acceptance_criteria>
  <done>recruiter.module.css contains every selector needed by Plan 02 components, complete mobile breakpoint matrix at 600px + 540px, and the full @media print block per UI-SPEC Print Contract</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create Metrics, Timeline, Skills, AvailabilityCard, ContactSection, RecruiterFooter components and compose full RecruiterView</name>
  <files>src/components/recruiter/Metrics.tsx, src/components/recruiter/Timeline.tsx, src/components/recruiter/Skills.tsx, src/components/recruiter/AvailabilityCard.tsx, src/components/recruiter/ContactSection.tsx, src/components/recruiter/RecruiterFooter.tsx, src/components/recruiter/RecruiterView.tsx</files>
  <read_first>
    - src/components/recruiter/RecruiterView.tsx (current Plan 01 composition — add new sections)
    - .planning/phases/09-recruiter-view/09-PATTERNS.md (Metrics, Timeline, Skills, AvailabilityCard, ContactSection, RecruiterFooter pattern sections with full code patterns)
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (Copywriting Contract: every string for METRICS / JOBS / SKILL_GROUPS / availability rows / contact / footer)
    - .planning/design/design_handoff_resume_v1/recruiter.jsx (lines 112–245 — data arrays and JSX shape; lift exact copy strings)
  </read_first>
  <behavior>
    Metrics.tsx behavior:
    - Static `METRICS` const array of 4 items: `{ num, unit, label }` matching UI-SPEC exactly: '9'/'yrs'/'Building test\ninfrastructure', '0.4'/'%'/'Flake rate\nacross suites', '98.2'/'%'/'Coverage on\ncritical paths', '1,247'/''/'Bugs caught\nYTD'
    - Renders a `<div class={styles.metricsGrid}>` containing 4 `<div class={styles.metricTile}>` each with `.metricNum` (number + optional `.unit` span) and `.metricLabel`

    Timeline.tsx behavior:
    - Static `JOBS` const array of 3 jobs matching UI-SPEC exactly (Staff SDET @ Lumen Systems current=true, Senior SDET @ Northwind Robotics 2020–2023, Automation Engineer @ Helix Health 2017–2020)
    - Renders `<div class={styles.timelineList}>` with 3 `<div class={styles.timelineJob}>` each containing a span column and body column
    - Current job (Lumen) renders the year "2023" wrapped in `<span class={styles.spanYearCurrent}>2023</span>` followed by " — Now"; other jobs render the plain span string
    - Body column: h3 role, div company (uppercase via CSS), p scope, stack pills container with a span per stack item

    Skills.tsx behavior:
    - Static `SKILL_GROUPS` of 4 groups matching UI-SPEC exactly (Automation, AI / ML, Infra, Languages with the exact pill lists)
    - Renders `<div class={styles.skillsList}>` with 4 `<div class={styles.skillGroup}>` each containing `.skillCat` label and `.skillPills` flex container of `.skillPill` spans

    AvailabilityCard.tsx behavior:
    - Static `ROWS` of 6 items matching UI-SPEC exactly (Status / Location / Level / Best fit / Comp / Visa with the exact values)
    - Status row gets `good: true` (its `.availValueCard` adds the `.good` class)
    - Renders `<div class={styles.availCard}>` with 6 `<div class={styles.availRow}>` each containing `.availLabelCard` and `.availValueCard`

    ContactSection.tsx behavior:
    - Renders `<section class={styles.contactSection}>` with:
      - `.contactPre` div: "Get in touch"
      - `.contactLine` p: "The fastest way to reach me is " + `<a href="mailto:alex@morgan.dev">alex@morgan.dev</a>` + ". I reply within a day."
      - `.contactGrid` div with 3 items (Email/GitHub/LinkedIn) — each has label + value anchor
    - Email anchor: `href="mailto:alex@morgan.dev"`
    - GitHub anchor: `href="https://github.com/amorgan"` + `target="_blank"` + `rel="noopener noreferrer"`
    - LinkedIn anchor: `href="https://linkedin.com/in/amorgan-sdet"` + `target="_blank"` + `rel="noopener noreferrer"`

    RecruiterFooter.tsx behavior:
    - Props: `{ onSwitchToIDE: () => void }`
    - Renders `<footer class={styles.footer}>` with `.footerCopy` span "© 2026 Alex Morgan" and `<button type="button" class={styles.footerSwitch}>Prefer the engineer view? Open the IDE →</button>` whose onClick calls onSwitchToIDE

    RecruiterView.tsx behavior (expand Plan 01):
    - Add imports for all 6 new components
    - Inside `<article class={styles.recruiter}>`, after `<Hero />` add:
      - `<section class={styles.section}>` containing `<div class={styles.sectionHead}><span class={styles.sectionNum}>§01</span><span class={styles.sectionTitle}>By the numbers</span><span class={styles.sectionRule} aria-hidden="true" /></div><Metrics />`
      - `<section class={styles.section}>` with §02 head "What I'm doing now" + a `<p class={styles.now}>` rendering the full §02 lede with "Lumen Systems" inline `<strong>`
      - `<section class={styles.section}>` with §03 head "Experience" + `<Timeline />`
      - `<section class={styles.section}>` with §04 head "Stack" + `<Skills />`
      - `<section class={styles.section}>` with §05 head "What I'm looking for" + `<AvailabilityCard />`
      - `<ContactSection />`
      - `<RecruiterFooter onSwitchToIDE={onSwitchToIDE} />`
  </behavior>
  <action>
    Create 6 component files under `src/components/recruiter/`. None of these access `window` so the `"use client"` directive is optional but ADD it to RecruiterFooter (since it uses onClick) and to RecruiterView (already present). Add it explicitly to ContactSection/Metrics/Timeline/Skills/AvailabilityCard if convenient (defense-in-depth). All component files import `styles from './recruiter.module.css'`. Use static const arrays defined at module scope (above the component function, marked `as const`) for METRICS/JOBS/SKILL_GROUPS/availability ROWS — match the design handoff data verbatim per the Copywriting Contract. The §02 `.now` paragraph is rendered directly inside RecruiterView (not its own component) — wrap "Lumen Systems" in `<strong>`. The drop-cap on the first letter is handled in CSS via `.now::first-letter` (Task 1) — no JSX manipulation needed. In ContactSection, ALL `target="_blank"` anchors MUST have `rel="noopener noreferrer"` (security gate). Apply `.good` className to the Status row value: render `<div className={`${styles.availValueCard} ${row.good ? styles.good : ''}`}>{row.value}</div>`. Update RecruiterView.tsx to import all new components and compose the full section list inside `<article>` per the behavior block. Lift every copy string exactly from 09-UI-SPEC.md Copywriting Contract — DO NOT paraphrase, abbreviate, or normalize unicode characters (the en-dashes "—", middot "·", and italic punctuation are intentional and locked).
  </action>
  <verify>
    <automated>pnpm exec tsc --noEmit 2>&amp;1 | tee /tmp/tsc-09-02-t2.log | grep -E 'error TS' | wc -l | grep -q '^0$' || (echo "TSC errors:"; cat /tmp/tsc-09-02-t2.log; exit 1)</automated>
    <automated>for f in Metrics Timeline Skills AvailabilityCard ContactSection RecruiterFooter; do test -f "src/components/recruiter/$f.tsx" || (echo "Missing $f.tsx"; exit 1); done</automated>
    <automated>grep -c "rel=\"noopener noreferrer\"" src/components/recruiter/ContactSection.tsx | grep -qE '^[2-9]'</automated>
    <automated>grep -cE 'href="mailto:alex@morgan\.dev"' src/components/recruiter/ContactSection.tsx | grep -qE '^[1-9]'</automated>
    <automated>grep -cE 'href="https://github\.com/amorgan"' src/components/recruiter/ContactSection.tsx | grep -qE '^[1-9]'</automated>
    <automated>grep -cE 'href="https://linkedin\.com/in/amorgan-sdet"' src/components/recruiter/ContactSection.tsx | grep -qE '^[1-9]'</automated>
    <automated>grep -c 'dangerouslySetInnerHTML' src/components/recruiter/ 2>/dev/null | grep -q '^0$' || ! grep -rE 'dangerouslySetInnerHTML' src/components/recruiter/</automated>
    <automated>grep -c '<Metrics' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]' &amp;&amp; grep -c '<Timeline' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]' &amp;&amp; grep -c '<Skills' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]' &amp;&amp; grep -c '<AvailabilityCard' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]' &amp;&amp; grep -c '<ContactSection' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]' &amp;&amp; grep -c '<RecruiterFooter' src/components/recruiter/RecruiterView.tsx | grep -qE '^[1-9]'</automated>
    <automated>grep -c '§01\|§02\|§03\|§04\|§05' src/components/recruiter/RecruiterView.tsx | grep -qE '^[5-9]|[0-9]{2,}'</automated>
  </verify>
  <acceptance_criteria>
    - All 6 new component files exist under `src/components/recruiter/`
    - `RecruiterView.tsx` imports and renders `<Metrics>`, `<Timeline>`, `<Skills>`, `<AvailabilityCard>`, `<ContactSection>`, `<RecruiterFooter>`
    - `RecruiterView.tsx` renders section heads §01 through §05 inline (5 distinct section number strings)
    - `ContactSection.tsx` has ≥2 occurrences of `rel="noopener noreferrer"` (one for GitHub, one for LinkedIn)
    - `ContactSection.tsx` contains the exact hrefs `mailto:alex@morgan.dev`, `https://github.com/amorgan`, `https://linkedin.com/in/amorgan-sdet`
    - `RecruiterFooter.tsx` exports `RecruiterFooter`, accepts `onSwitchToIDE: () => void`, contains a `<button type="button">` calling that callback
    - Zero occurrences of `dangerouslySetInnerHTML` anywhere under `src/components/recruiter/`
    - `pnpm exec tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>All 5 content sections + contact + footer render with locked copy from 09-UI-SPEC.md; the recruiter view is composed end-to-end; external links are secured with rel="noopener noreferrer"</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expand RecruiterView unit tests (REC-03, REC-05, REC-07, REC-08); expand recruiter E2E spec (REC-03/05/07/08/10 + mobile viewport); fix landing.spec.ts DOOR-03 assertion</name>
  <files>src/components/recruiter/__tests__/RecruiterView.test.tsx, e2e/recruiter.spec.ts, e2e/landing.spec.ts</files>
  <read_first>
    - src/components/recruiter/__tests__/RecruiterView.test.tsx (Plan 01 test scaffold — extend, do not rewrite)
    - e2e/recruiter.spec.ts (Plan 01 spec — extend, do not rewrite)
    - e2e/landing.spec.ts (line 86 is the specific assertion to change — DO NOT alter other tests)
    - .planning/phases/09-recruiter-view/09-RESEARCH.md (Pitfall 3 — exact DOOR-03 replacement assertion)
    - .planning/phases/09-recruiter-view/09-UI-SPEC.md (stable text targets: "9", "Lumen Systems", "Status", "Open to offers · Q3 start", "Prefer the engineer view? Open the IDE →")
  </read_first>
  <behavior>
    Unit test expansion behavior:
    - Add new describe block "RecruiterView — content sections" with:
      - REC-03: renders all four metric numbers (9, 0.4, 98.2, 1,247) — assert each `getByText('9')`, `getByText('0.4')`, `getByText('98.2')`, `getByText('1,247')` is in document
      - REC-05: renders all three job role headings — assert `getByRole('heading', { level: 3, name: 'Staff SDET, AI Platform' })`, "Senior SDET", "Automation Engineer" are visible
      - REC-07: renders "Open to offers · Q3 start" (Status row value), "US citizen — no sponsorship needed" (Visa row)
      - REC-08: renders large email link to mailto:alex@morgan.dev; renders 3 contact item labels (Email / GitHub / LinkedIn); GitHub anchor has rel attribute containing 'noopener'; LinkedIn anchor has rel attribute containing 'noopener'
    - Extend "RecruiterView — click interactions" describe with: footer button (accessible name /Open the IDE/i) click calls onSwitchToIDE once
    - All new tests must pass; existing Plan 01 tests must still pass

    Recruiter E2E spec expansion behavior:
    - Add desktop test "REC-03: renders four metric numbers" — goto '/', expect getByText('9'), '0.4', '98.2', '1,247' all visible
    - Add desktop test "REC-05: renders three job role headings" — goto '/', expect getByRole('heading', { level: 3, name: /Staff SDET/ }), /Senior SDET/, /Automation Engineer/ all visible
    - Add desktop test "REC-07: renders availability spec with Status forest accent" — goto '/', expect getByText('Open to offers · Q3 start') visible, expect getByText('US citizen — no sponsorship needed') visible
    - Add desktop test "REC-08: renders contact + footer with external link security" — goto '/', verify GitHub link has rel attribute containing 'noopener' AND 'noreferrer'; verify LinkedIn link same; verify Open the IDE footer button calls localStorage('ide') after click
    - Add mobile test "REC-10: renders without horizontal overflow at 375px" — setViewportSize 375×812, goto '/', expect getByText('ruslan.kanat') visible AND expect getByText('Lumen Systems') visible AND assert that `document.documentElement.scrollWidth <= 375 + small tolerance` via page.evaluate (verify no horizontal scrollbar)

    landing.spec.ts DOOR-03 fix behavior:
    - Locate the test "DOOR-03 same-session: clicking recruiter half re-renders without navigation" (around line 82–89)
    - Replace ONLY the line `await expect(page.getByText(/Recruiter view — coming in Phase 9/i)).toBeVisible();` with `await expect(page.getByText('ruslan.kanat')).toBeVisible();`
    - Keep all other lines (the click action, the URL invariance check, the "Enter the résumé" not-visible check) unchanged
    - Do NOT touch any other test in landing.spec.ts
  </behavior>
  <action>
    Open `src/components/recruiter/__tests__/RecruiterView.test.tsx` and APPEND a new describe block "RecruiterView — content sections" with 4 it() blocks for REC-03/05/07/08. Append one more it() to the existing "RecruiterView — interactions" describe block for the footer button. Use the same imports (no new packages). Verify accessible queries: prefer `getByRole('heading', { level: 3, name: ... })` over `getByText` for headings; use `getByText('Open to offers · Q3 start')` for the Status value (do not use regex when the exact string is known and unique); for `rel` attribute checks use `screen.getByRole('link', { name: /github.com\/amorgan/ }).getAttribute('rel')` and expect it to contain 'noopener'. Open `e2e/recruiter.spec.ts` and APPEND 5 new tests inside the existing `test.describe('Recruiter view'` block (4 content tests + 1 mobile viewport test). The mobile test calls `await page.setViewportSize({ width: 375, height: 812 })` BEFORE `await page.goto('/')`. The overflow check uses `await page.evaluate(() => document.documentElement.scrollWidth)` then expects the returned value to be `<= 375` (use `expect(scrollWidth).toBeLessThanOrEqual(375)`). For `landing.spec.ts`, use Edit to change the SINGLE line 86 to `await expect(page.getByText('ruslan.kanat')).toBeVisible();` — verify by re-reading the file that the surrounding test logic (lines 82–89) remains intact.
  </action>
  <verify>
    <automated>pnpm test -- RecruiterView 2>&amp;1 | tail -40 | grep -E 'Test Files\s+1 passed'</automated>
    <automated>pnpm test -- RecruiterView 2>&amp;1 | tail -40 | grep -E 'Tests\s+([1-9][0-9]|[8-9]) passed' || (echo "Expected at least 8 unit tests passing"; pnpm test -- RecruiterView 2>&amp;1 | tail -40; exit 1)</automated>
    <automated>! grep -q 'Recruiter view — coming in Phase 9' e2e/landing.spec.ts</automated>
    <automated>grep -c "page.getByText('ruslan.kanat')" e2e/landing.spec.ts | grep -qE '^[1-9]'</automated>
    <automated>grep -c "REC-10" e2e/recruiter.spec.ts | grep -qE '^[1-9]'</automated>
    <automated>grep -c "setViewportSize" e2e/recruiter.spec.ts | grep -qE '^[1-9]'</automated>
    <automated>pnpm exec tsc --noEmit 2>&amp;1 | grep -E 'error TS' | wc -l | grep -q '^0$'</automated>
    <automated>pnpm exec playwright test recruiter --reporter=line 2>&amp;1 | tail -10 | grep -E '[0-9]+ passed' &amp;&amp; pnpm exec playwright test landing --reporter=line 2>&amp;1 | tail -10 | grep -E '[0-9]+ passed'</automated>
  </verify>
  <acceptance_criteria>
    - `pnpm test -- RecruiterView` reports `Test Files  1 passed` and ≥8 tests passing (3 Plan 01 render + 2 Plan 01 interaction + 4 Plan 02 content + 1 Plan 02 footer interaction)
    - `e2e/recruiter.spec.ts` contains ≥9 `test(...)` declarations (Plan 01: 4; Plan 02: ≥5 — REC-03 metrics, REC-05 jobs, REC-07 availability, REC-08 contact+rel+footer, REC-10 mobile)
    - `e2e/landing.spec.ts` no longer contains the literal `Recruiter view — coming in Phase 9`
    - `e2e/landing.spec.ts` contains `page.getByText('ruslan.kanat')` inside the DOOR-03 same-session test block
    - `pnpm exec playwright test recruiter` passes all tests
    - `pnpm exec playwright test landing` passes all tests (DOOR-03 unblocked)
    - `pnpm exec tsc --noEmit` reports zero errors
  </acceptance_criteria>
  <done>Unit tests cover REC-03/REC-05/REC-07/REC-08; E2E spec covers REC-03 through REC-10 including 375px viewport overflow check; landing E2E suite passes; entire Vitest + Playwright suite green</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → external website (GitHub, LinkedIn) | ContactSection anchors with `target="_blank"` open new tabs; must isolate window context. |
| client → mailto handler | Email link triggers OS mail client; no data exposure. |
| client → localStorage | RecruiterFooter switch writes 'resume-mode' = 'ide' via onSwitchToIDE; same channel as Plan 01. |
| SSR boundary → window/document | Recruiter components remain inside `'use client'` subtree rendered by ResumeGateInner. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-09-02 | Information Disclosure | `ContactSection.tsx` external anchors | mitigate | Every `target="_blank"` anchor (GitHub, LinkedIn) MUST set `rel="noopener noreferrer"`. Enforced by Task 2 grep gate `grep -c 'rel="noopener noreferrer"' ContactSection.tsx >= 2`. Without `noopener` the opened tab can access `window.opener` and run navigation attacks. |
| T-09-03 | Tampering / XSS | All Plan 02 components | mitigate | All copy hardcoded from 09-UI-SPEC.md Copywriting Contract. Task 2 grep gate `! grep -rE 'dangerouslySetInnerHTML' src/components/recruiter/` blocks merge if any component reintroduces innerHTML rendering. |
| T-09-04 | Tampering | Static section content (METRICS / JOBS / SKILL_GROUPS / availability rows / contact data) | accept | All data is hardcoded const arrays — no API, no user input, no LLM output. Tampering requires source-tree access; out of scope for this view. |
| T-09-05 | SSR window access | `RecruiterFooter` onClick handler | mitigate | Component is rendered inside ResumeGateInner ('use client'). RecruiterFooter explicitly carries `'use client'` directive as defense-in-depth. |
| T-09-06 | Spoofing | mailto and external hrefs (`mailto:alex@morgan.dev`, `https://github.com/amorgan`, `https://linkedin.com/in/amorgan-sdet`) | accept | Hrefs are hardcoded design-handoff placeholders; Phase 10 (CONT-RK-01) replaces with real Ruslan Kanatbek addresses. Spoofing risk is "developer typo" rather than runtime tampering — acceptable for static prototype copy. |
</threat_model>

<verification>
After Task 3 completes, run the full suite:

```bash
pnpm exec tsc --noEmit
pnpm test
pnpm exec playwright test
```

Expected:
- tsc: zero errors
- Vitest: all suites pass; RecruiterView reports ≥8 tests passing
- Playwright: every spec passes (landing, recruiter, plus any pre-existing specs)

Manual smoke (recommended but optional):
1. `pnpm dev`, navigate to `/?reset`, click recruiter door half — full editorial page loads
2. Scroll: masthead remains sticky at the top of the cream column; content scrolls beneath
3. Verify §01 four tiles, §02 drop-cap, §03 three jobs with current-year forest accent, §04 four skill groups, §05 six rows with Status in forest
4. Click "Prefer the engineer view? Open the IDE →" — switches to IDE
5. Resize browser to 375px — metrics → 2-col, timeline → 1-col, contact → 1-col; resize ≤540px — availability card collapses to 1-col
6. `Cmd+P` (or call window.print() via Download PDF) — masthead switch + CTAs + footer hidden in preview
</verification>

<success_criteria>
- [ ] All 6 new Plan 02 components exist under `src/components/recruiter/`
- [ ] `recruiter.module.css` contains all selectors + mobile breakpoints (600px + 540px) + full print block
- [ ] `RecruiterView.tsx` composes all sections (§01–§05 + Contact + Footer)
- [ ] `ContactSection.tsx` external anchors have `rel="noopener noreferrer"`
- [ ] No `dangerouslySetInnerHTML` anywhere under `src/components/recruiter/`
- [ ] `pnpm test` passes with ≥8 RecruiterView unit tests
- [ ] `pnpm exec playwright test` passes (recruiter + landing both green; DOOR-03 unblocked)
- [ ] `pnpm exec tsc --noEmit` reports zero errors
- [ ] REC-03, REC-04, REC-05, REC-06, REC-07, REC-08, REC-10 all satisfied
- [ ] All copy lifted verbatim from 09-UI-SPEC.md Copywriting Contract (Phase 10 will swap to real Ruslan content)
</success_criteria>

<output>
Create `.planning/phases/09-recruiter-view/09-02-SUMMARY.md` when done summarising files created/modified, requirements satisfied (REC-03, REC-04, REC-05, REC-06, REC-07, REC-08, REC-10), threat dispositions (T-09-02 mitigated), test counts, and any deviations from the plan.
</output>
