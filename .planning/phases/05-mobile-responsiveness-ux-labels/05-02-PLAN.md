---
phase: 05-mobile-responsiveness-ux-labels
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - src/components/ide/Sidebar.tsx
  - src/components/ide/Sidebar.module.css
  - src/components/ide/Terminal.tsx
  - src/components/ide/Terminal.module.css
  - src/components/ide/EditorArea.tsx
  - src/components/ide/EditorArea.module.css
autonomous: true
requirements:
  - UX-01
  - MOB-01
tags:
  - mobile
  - ux
  - labels
  - sidebar

must_haves:
  truths:
    - "Sidebar exposes an optional `onSelect: () => void` prop (no breaking change to existing callers)"
    - "When `onSelect` is passed, tapping any file row or activating it via Enter/Space also calls `onSelect()` after `setActiveFile` + `openTab`"
    - "Sidebar EXPLORER header reads 'EXPLORER · File browser' with the · marked aria-hidden"
    - "Terminal TERMINAL tab (when active or rendered in the tab strip) reads 'TERMINAL · Test output' with the · marked aria-hidden"
    - "Editor breadcrumb bar reads '<last crumb> · Code viewer' with the · marked aria-hidden"
    - "Pane subtitle text uses font-family var(--ui), font-size 10.5px, color var(--text-faint)"
    - "Screen readers do not announce the · separator character"
    - "Desktop behavior is unchanged — sidebar does not auto-close on file click when `onSelect` is not passed"
  artifacts:
    - path: "src/components/ide/Sidebar.tsx"
      provides: "Optional onSelect prop, EXPLORER pane subtitle JSX"
      contains: "onSelect?: () => void"
    - path: "src/components/ide/Sidebar.module.css"
      provides: ".paneSubtitle class"
      contains: ".paneSubtitle"
    - path: "src/components/ide/Terminal.tsx"
      provides: "TERMINAL tab pane subtitle JSX"
      contains: "Test output"
    - path: "src/components/ide/Terminal.module.css"
      provides: ".paneSubtitle class"
      contains: ".paneSubtitle"
    - path: "src/components/ide/EditorArea.tsx"
      provides: "Editor breadcrumb pane subtitle JSX"
      contains: "Code viewer"
    - path: "src/components/ide/EditorArea.module.css"
      provides: ".paneSubtitle class"
      contains: ".paneSubtitle"
  key_links:
    - from: "Sidebar.tsx fileRow onClick + onKeyDown"
      to: "onSelect?.() invocation"
      via: "optional-chaining call after setActiveFile + openTab"
      pattern: "onSelect\\?\\.\\(\\)"
    - from: "Sidebar.tsx .explorerHeader JSX"
      to: "<span className={styles.paneSubtitle}>File browser</span>"
      via: "inline append after EXPLORER span with aria-hidden separator"
      pattern: "File browser"
    - from: "Terminal.tsx TERM_TABS.map conditional render"
      to: "paneSubtitle span inside TERMINAL tab"
      via: "t === 'TERMINAL' && (<>…</>)"
      pattern: "Test output"
    - from: "EditorArea.tsx .breadcrumb JSX"
      to: "paneSubtitle span after last crumb"
      via: "inline append with aria-hidden separator"
      pattern: "Code viewer"
---

<objective>
Add the UX-01 plain-language pane labels (D-09, D-10, D-11) to the three IDE panes — Sidebar EXPLORER, Editor breadcrumb, Terminal TERMINAL tab — and add the optional `onSelect` prop to Sidebar that Plan 03 will use to auto-close the mobile sidebar on file tap.

Purpose: Non-technical visitors immediately understand each pane's purpose ("File browser", "Code viewer", "Test output") without prior developer knowledge. Simultaneously establishes the `Sidebar` interface contract (`onSelect?: () => void`) that Plan 03 (IDEShell mobile state) consumes — without this prop declared first, Plan 03's `onSelect={isMobile ? toggleSidebar : undefined}` would fail TypeScript compilation.

Output: Six files updated — three JSX/TSX files add subtitle spans and (Sidebar only) the new prop, three CSS Module files add the `.paneSubtitle` class definition. No IDEShell changes (deferred to Plan 03).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-CONTEXT.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-01-SUMMARY.md

<interfaces>
<!-- Existing Sidebar interface (Sidebar.tsx lines 22-27) - extend with one optional prop -->

interface SidebarProps {
  activeFile: string;
  setActiveFile: (name: string) => void;
  openTab: (name: string) => void;
  sidebarOpen?: boolean;
  // ADD:
  onSelect?: () => void;
}

<!-- Existing fileRow handler pattern (Sidebar.tsx lines 67-86) -->
<!-- onClick: setActiveFile(name); openTab(name); -->
<!-- onKeyDown (Enter/Space): setActiveFile(name); openTab(name); -->
<!-- After this plan: both also call onSelect?.() -->

<!-- Existing Terminal TERM_TABS.map conditional render pattern (Terminal.tsx lines 81-95)
     already uses the t === '...' conditional pattern for PROBLEMS and DEBUG CONSOLE.
     Adding TERMINAL follows the same convention. -->

<!-- Existing EditorArea breadcrumb render (EditorArea.tsx lines 92-99)
     .breadcrumb div uses display: flex; align-items: center; gap: 6px
     (EditorArea.module.css lines 132-143). -->

<!-- New .paneSubtitle class — collocated in EACH of the three CSS Module files
     (per UI-SPEC: "Add directly to each module file to keep colocation"): -->

.paneSubtitle {
  font-size: 10.5px;
  font-weight: 400;
  color: var(--text-faint);
  letter-spacing: 0.02em;
  font-family: var(--ui);
  margin-left: 4px;
}

<!-- Existing tokens (globals.css):
     --text-faint = #5a6678 (dark theme), used for explorerMeta / termMeta — same color tier
     --ui = Inter -> -apple-system -> system-ui font stack
     --mono = JetBrains Mono -> Fira Code -> SF Mono -> ui-monospace
     Subtitle MUST use var(--ui), not var(--mono) — signals human-facing copy, not code metadata. -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Sidebar — add onSelect prop, wire to fileRow, add EXPLORER pane subtitle, add .paneSubtitle class</name>
  <files>src/components/ide/Sidebar.tsx, src/components/ide/Sidebar.module.css</files>
  <read_first>
    - src/components/ide/Sidebar.tsx (full file — confirm SidebarProps interface at lines 22-27, fileRow handler at lines 67-86, explorerHeader JSX at lines 151-154)
    - src/components/ide/Sidebar.module.css (full file — locate .explorerMeta block at lines 137-143 as the color/size tier reference; confirm no existing .paneSubtitle class; confirm Plan 01's @media (max-width: 768px) block now contains .treeRow/.treeFolder min-height: 44px)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (section "src/components/ide/Sidebar.tsx" lines 121-183 and section "src/components/ide/Sidebar.module.css" lines 186-232)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (sections "MOB-01 Auto-close on file tap (D-01)" and "UX-01: Pane Labels")
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-CONTEXT.md (decisions D-01, D-09, D-10, D-11)
  </read_first>
  <action>
    Four edits to Sidebar.tsx and one CSS addition. All values are concrete; no judgement calls required.

    **Edit 1 — SidebarProps interface (lines 22-27)**: Add `onSelect?: () => void` as the fifth optional prop AT THE END of the existing interface. The final interface MUST be exactly:
    ```typescript
    interface SidebarProps {
      activeFile: string;
      setActiveFile: (name: string) => void;
      openTab: (name: string) => void;
      sidebarOpen?: boolean;
      onSelect?: () => void;
    }
    ```

    **Edit 2 — function signature (line 63)**: Destructure `onSelect` from props (no default — leave `undefined` when not passed). The final signature MUST be exactly:
    ```typescript
    export function Sidebar({ activeFile, setActiveFile, openTab, sidebarOpen = true, onSelect }: SidebarProps) {
    ```

    **Edit 3 — fileRow handlers (lines 76, 79)**: APPEND `onSelect?.()` AFTER the existing `setActiveFile(name); openTab(name);` calls in BOTH `onClick` and `onKeyDown`. Use optional-chaining call (`?.()`) so the prop being undefined is a safe no-op on desktop:
    ```tsx
    onClick={() => { setActiveFile(name); openTab(name); onSelect?.(); }}
    role="button"
    tabIndex={0}
    onKeyDown={(e) => { if (e.key === 'Enter' || e.key === ' ') { setActiveFile(name); openTab(name); onSelect?.(); } }}
    ```

    Do NOT add `onSelect?.()` to the folder-toggle handlers (lines ~161 and ~188 — the `.treeFolder` divs that toggle `open.about` / `open.tests`). Folders are not files; D-01 only auto-closes on file tap.

    **Edit 4 — EXPLORER header JSX (lines 151-154)**: Modify the `<span>EXPLORER</span>` to include the pane subtitle inline. Per D-10 the exact label is `EXPLORER · File browser`. Replace the existing JSX with:
    ```tsx
    <div className={styles.explorerHeader}>
      <span>
        EXPLORER
        <span aria-hidden="true"> · </span>
        <span className={styles.paneSubtitle}>File browser</span>
      </span>
      <span className={styles.explorerMeta}>ruslankanat/portfolio</span>
    </div>
    ```
    The outer `<span>` wraps EXPLORER + separator + subtitle so they stay grouped on the left side of the flex container; `.explorerMeta` remains on the right.

    **CSS addition — src/components/ide/Sidebar.module.css**: Append to the non-media section (before any `@media` blocks):
    ```css
    .paneSubtitle {
      font-size: 10.5px;
      font-weight: 400;
      color: var(--text-faint);
      letter-spacing: 0.02em;
      font-family: var(--ui);
      margin-left: 4px;
    }
    ```

    Do NOT modify `.explorerMeta` (stays at `font-family: var(--mono)`; per D-11 the pane subtitle uses `var(--ui)` to signal human-facing copy). Do NOT touch the existing `@media (max-width: 768px)` block — Plan 01 already populated it with `.treeRow` / `.treeFolder` touch targets.
  </action>
  <verify>
    <automated>grep -E "onSelect\?: \(\) => void" src/components/ide/Sidebar.tsx && grep -cE "onSelect\?\.\(\)" src/components/ide/Sidebar.tsx | grep -qE "^[[:space:]]*2$" && grep -F "File browser" src/components/ide/Sidebar.tsx && grep -F 'aria-hidden="true"' src/components/ide/Sidebar.tsx && grep -E "^\.paneSubtitle" src/components/ide/Sidebar.module.css && grep -E "var\(--text-faint\)" src/components/ide/Sidebar.module.css && grep -E "var\(--ui\)" src/components/ide/Sidebar.module.css && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ide/Sidebar.tsx interface `SidebarProps` contains `onSelect?: () => void`
    - src/components/ide/Sidebar.tsx function signature destructures `onSelect` (no default value)
    - src/components/ide/Sidebar.tsx contains exactly TWO `onSelect?.()` invocations (one inside onClick, one inside onKeyDown of fileRow)
    - src/components/ide/Sidebar.tsx contains the literal text `File browser`
    - src/components/ide/Sidebar.tsx contains `aria-hidden="true"` on the `·` separator span inside .explorerHeader
    - src/components/ide/Sidebar.tsx contains `className={styles.paneSubtitle}` exactly once (the File browser span)
    - src/components/ide/Sidebar.module.css contains a `.paneSubtitle` class with all six declared properties: `font-size: 10.5px`, `font-weight: 400`, `color: var(--text-faint)`, `letter-spacing: 0.02em`, `font-family: var(--ui)`, `margin-left: 4px`
    - `.paneSubtitle` is OUTSIDE any `@media` block
    - `.explorerMeta` is NOT modified (still references `var(--mono)` and `var(--text-muted)`)
    - Folder toggle handlers (`.treeFolder` onClick for `about` and `tests`) do NOT contain `onSelect?.()` — `grep -E "setOpen.*onSelect"` returns 0 hits
    - `pnpm exec tsc --noEmit` exits 0 (no TypeScript errors)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    Sidebar accepts an optional `onSelect: () => void` prop. File row tap and keyboard activation call `onSelect?.()` after `setActiveFile` + `openTab`. Folder toggles do NOT call `onSelect`. EXPLORER header reads `EXPLORER · File browser` with the · screen-reader-hidden. `.paneSubtitle` class exists with the exact six declared properties. `pnpm exec tsc --noEmit` and `pnpm build` both pass. Plan 03 can now wire `onSelect={isMobile ? toggleSidebar : undefined}` from IDEShell.
  </done>
</task>

<task type="auto">
  <name>Task 2: Terminal — TERMINAL tab pane subtitle + .paneSubtitle class</name>
  <files>src/components/ide/Terminal.tsx, src/components/ide/Terminal.module.css</files>
  <read_first>
    - src/components/ide/Terminal.tsx (full file — confirm TERM_TABS array, termTab/setTab state, the TERM_TABS.map block at lines 81-95 with existing conditional render pattern for PROBLEMS and DEBUG CONSOLE)
    - src/components/ide/Terminal.module.css (full file — confirm Plan 01's `@media (max-width: 768px)` block is present with log wrap + termResizer hide + terminal max-height; locate `.termMeta` and `.termTab` classes)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (section "src/components/ide/Terminal.tsx" lines 235-266 and section "src/components/ide/Terminal.module.css" lines 270-334)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (section "UX-01: Pane Labels" — Terminal placement)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-CONTEXT.md (decisions D-09, D-10, D-11)
  </read_first>
  <action>
    Two edits: one JSX in Terminal.tsx, one CSS class added to Terminal.module.css.

    **Edit 1 — src/components/ide/Terminal.tsx, inside the TERM_TABS.map block (lines 81-95)**:

    Locate the existing tab body which currently reads:
    ```tsx
    {t}
    {t === 'PROBLEMS' && <span className={styles.termTabBadge}>0</span>}
    {t === 'DEBUG CONSOLE' && running && <span className={styles.termTabDot} />}
    ```

    Insert a new conditional render IMMEDIATELY AFTER `{t}` and BEFORE the PROBLEMS line. The fragment renders only when `t === 'TERMINAL'`:
    ```tsx
    {t}
    {t === 'TERMINAL' && (
      <>
        <span aria-hidden="true"> · </span>
        <span className={styles.paneSubtitle}>Test output</span>
      </>
    )}
    {t === 'PROBLEMS' && <span className={styles.termTabBadge}>0</span>}
    {t === 'DEBUG CONSOLE' && running && <span className={styles.termTabDot} />}
    ```

    The subtitle renders INSIDE the TERMINAL tab's div container — so it appears next to the TERMINAL label whether the tab is active or not (since TERM_TABS.map renders one div per tab and the conditional fires only on the TERMINAL iteration). No state changes, no new handlers, no new imports needed.

    Do NOT modify the existing PROBLEMS or DEBUG CONSOLE conditionals. Do NOT add the subtitle to non-TERMINAL tabs. Do NOT change `termMeta` (the "playwright" label on the right side of `.termTabs`) — that is a separate visual element and keeps its existing mono font.

    **Edit 2 — src/components/ide/Terminal.module.css**: Add the `.paneSubtitle` class. Append to the non-media section (BEFORE the `@media (max-width: 768px)` block added by Plan 01):
    ```css
    .paneSubtitle {
      font-size: 10.5px;
      font-weight: 400;
      color: var(--text-faint);
      letter-spacing: 0.02em;
      font-family: var(--ui);
      margin-left: 4px;
    }
    ```

    Class definition is character-identical to Sidebar.module.css's `.paneSubtitle` (collocation pattern per UI-SPEC). Do NOT touch the `@media (max-width: 768px)` block populated by Plan 01.
  </action>
  <verify>
    <automated>grep -F "t === 'TERMINAL'" src/components/ide/Terminal.tsx && grep -F "Test output" src/components/ide/Terminal.tsx && grep -cE 'aria-hidden="true"' src/components/ide/Terminal.tsx | grep -qE "^[1-9]" && grep -E "^\.paneSubtitle" src/components/ide/Terminal.module.css && grep -A6 "^\.paneSubtitle" src/components/ide/Terminal.module.css | grep -E "var\(--ui\)" && pnpm exec tsc --noEmit</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ide/Terminal.tsx contains the conditional `t === 'TERMINAL' && (` inside the TERM_TABS.map block
    - src/components/ide/Terminal.tsx contains the literal text `Test output`
    - src/components/ide/Terminal.tsx contains at least one `aria-hidden="true"` attribute (the · separator)
    - src/components/ide/Terminal.tsx contains `className={styles.paneSubtitle}` exactly once
    - The PROBLEMS conditional (`t === 'PROBLEMS' && <span className={styles.termTabBadge}>0</span>`) and DEBUG CONSOLE conditional (`t === 'DEBUG CONSOLE' && running && <span className={styles.termTabDot} />`) remain unchanged
    - src/components/ide/Terminal.module.css contains a `.paneSubtitle` class with all six declared properties (font-size 10.5px, font-weight 400, color var(--text-faint), letter-spacing 0.02em, font-family var(--ui), margin-left 4px)
    - `.paneSubtitle` definition appears BEFORE the `@media (max-width: 768px)` block (in source order)
    - `.termMeta` is NOT modified
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    TERMINAL tab in Terminal.tsx renders ` · Test output` inline after the "TERMINAL" label using the same conditional-render pattern as PROBLEMS / DEBUG CONSOLE. The · is screen-reader-hidden. `.paneSubtitle` class exists in Terminal.module.css. PROBLEMS, DEBUG CONSOLE, and termMeta are untouched. `pnpm exec tsc --noEmit` and `pnpm build` both pass.
  </done>
</task>

<task type="auto">
  <name>Task 3: EditorArea — breadcrumb pane subtitle + .paneSubtitle class</name>
  <files>src/components/ide/EditorArea.tsx, src/components/ide/EditorArea.module.css</files>
  <read_first>
    - src/components/ide/EditorArea.tsx (full file — confirm the .breadcrumb render block at lines 92-99 that maps over `breadcrumb.split(' › ')`)
    - src/components/ide/EditorArea.module.css (full file — confirm .breadcrumb block at lines 131-143 with display: flex; align-items: center; gap: 6px)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (section "src/components/ide/EditorArea.tsx" lines 402-428 and section "src/components/ide/EditorArea.module.css" lines 432-467)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (section "UX-01: Pane Labels" — Editor placement; the exact label is `· Code viewer` appended after the last crumb)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-CONTEXT.md (decisions D-09, D-10, D-11)
  </read_first>
  <action>
    Two edits: one JSX in EditorArea.tsx (append to .breadcrumb div), one CSS in EditorArea.module.css (add .paneSubtitle + .crumbSubtitleSep classes).

    **Edit 1 — src/components/ide/EditorArea.tsx, .breadcrumb render (lines 92-99)**:

    The existing JSX is:
    ```tsx
    <div className={styles.breadcrumb}>
      {breadcrumb.split(' › ').map((part, i, arr) => (
        <span key={i}>
          <span className={cn(styles.crumb, { [styles.crumbActive]: i === arr.length - 1 })}>{part}</span>
          {i < arr.length - 1 && <span className={styles.crumbSep}> › </span>}
        </span>
      ))}
    </div>
    ```

    Append two new spans AFTER the `.map(...)` block but INSIDE the closing `</div>`. The first span is the aria-hidden separator with `margin-left: auto` (via the new `.crumbSubtitleSep` class) so the subtitle is right-aligned within the flex breadcrumb bar; the second span is the visible "Code viewer" subtitle:
    ```tsx
    <div className={styles.breadcrumb}>
      {breadcrumb.split(' › ').map((part, i, arr) => (
        <span key={i}>
          <span className={cn(styles.crumb, { [styles.crumbActive]: i === arr.length - 1 })}>{part}</span>
          {i < arr.length - 1 && <span className={styles.crumbSep}> › </span>}
        </span>
      ))}
      <span aria-hidden="true" className={styles.crumbSubtitleSep}> · </span>
      <span className={styles.paneSubtitle}>Code viewer</span>
    </div>
    ```

    Right-alignment (via `margin-left: auto` on `.crumbSubtitleSep`) is the chosen placement per UI-SPEC ("trailing right side using margin-left: auto"). Do NOT touch `.crumb`, `.crumbActive`, or `.crumbSep`. Do NOT modify the breadcrumb prop or its derivation.

    **Edit 2 — src/components/ide/EditorArea.module.css**: Append two new classes to the non-media section. Add AFTER the existing `.breadcrumb` block (around line 143):

    ```css
    .paneSubtitle {
      font-size: 10.5px;
      font-weight: 400;
      color: var(--text-faint);
      letter-spacing: 0.02em;
      font-family: var(--ui);
      margin-left: 4px;
    }

    .crumbSubtitleSep {
      color: var(--text-faint);
      margin-left: auto;
    }
    ```

    `.paneSubtitle` is the same definition as in Sidebar.module.css and Terminal.module.css (collocation). `.crumbSubtitleSep` is unique to EditorArea — its `margin-left: auto` pushes the subtitle to the right edge of the flex `.breadcrumb` bar; its color matches `.breadcrumb`'s existing `var(--text-faint)`.

    Do NOT modify `.breadcrumb` itself (its existing display: flex; align-items: center; gap: 6px is what makes margin-left: auto work).
  </action>
  <verify>
    <automated>grep -F "Code viewer" src/components/ide/EditorArea.tsx && grep -F 'aria-hidden="true"' src/components/ide/EditorArea.tsx && grep -E "crumbSubtitleSep" src/components/ide/EditorArea.tsx && grep -E "^\.paneSubtitle" src/components/ide/EditorArea.module.css && grep -E "^\.crumbSubtitleSep" src/components/ide/EditorArea.module.css && grep -A2 "^\.crumbSubtitleSep" src/components/ide/EditorArea.module.css | grep -E "margin-left:\s*auto" && pnpm exec tsc --noEmit && pnpm build 2>&1 | tail -3</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ide/EditorArea.tsx contains the literal text `Code viewer`
    - src/components/ide/EditorArea.tsx contains `aria-hidden="true"` on the new separator span
    - src/components/ide/EditorArea.tsx contains `className={styles.crumbSubtitleSep}` and `className={styles.paneSubtitle}` each exactly once
    - The new spans are siblings of the `.map(...)` output (inside the same `<div className={styles.breadcrumb}>`)
    - src/components/ide/EditorArea.module.css contains a `.paneSubtitle` class with the same six declared properties as Sidebar/Terminal
    - src/components/ide/EditorArea.module.css contains a `.crumbSubtitleSep` class with `color: var(--text-faint)` and `margin-left: auto`
    - `.breadcrumb`, `.crumb`, `.crumbActive`, `.crumbSep` are NOT modified
    - `pnpm exec tsc --noEmit` exits 0
    - `pnpm build` exits 0
    - All three pane subtitles now exist across the three IDE pane files: `grep -rF "File browser" src/components/ide/Sidebar.tsx && grep -rF "Code viewer" src/components/ide/EditorArea.tsx && grep -rF "Test output" src/components/ide/Terminal.tsx` all return matches
  </acceptance_criteria>
  <done>
    EditorArea breadcrumb renders ` · Code viewer` right-aligned at the trailing edge of the breadcrumb bar via `margin-left: auto`. The · is screen-reader-hidden. `.paneSubtitle` and `.crumbSubtitleSep` classes exist in EditorArea.module.css. All three pane labels (File browser, Code viewer, Test output) are now live in their respective panes. `pnpm exec tsc --noEmit` and `pnpm build` both pass. Plan 03 can wire IDEShell logic next.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan adds one optional prop and three static text labels — no new code paths, no new inputs, no new network boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Information Disclosure | Pane subtitle text | accept | All subtitle strings are static literals in JSX (`File browser`, `Code viewer`, `Test output`) — no user data, no PII, no secrets rendered. |
| T-05-05 | Tampering | Sidebar onSelect prop | accept | `onSelect` is a typed callback (`() => void`) invoked via optional chaining. Caller controls the function passed; Sidebar only invokes it. No mutation of internal state by external code. |
| T-05-06 | Spoofing | `aria-hidden="true"` on separator spans | accept | `aria-hidden` is an accessibility hint, not an authorization control. Screen-reader users do not hear the `·` character. No identity / auth implications. |
| T-05-07 | Tampering | Build pipeline | accept | No new dependencies. No package installs in this plan. ASVS L1 supply-chain requirements not triggered. |

This plan introduces no new backend routes, no new auth/session changes, no new data persistence, no new user input handlers, and no new external API calls. The only behavioral change is an additional optional callback (`onSelect`) invoked by Sidebar after existing handlers — Plan 03 wires it to `toggleSidebar` (idempotent UI state mutation) and `undefined` elsewhere. The threat surface is unchanged from the v1.0 baseline.
</threat_model>

<verification>
- All three pane subtitle strings render in their respective panes (`File browser`, `Code viewer`, `Test output`)
- All three `·` separators carry `aria-hidden="true"`
- Sidebar's `onSelect?: () => void` prop is declared in the interface and called via `?.()` in both fileRow handlers (onClick + onKeyDown)
- Folder toggle handlers do NOT call `onSelect` (D-01 scope: files only)
- `.paneSubtitle` class is defined identically in Sidebar.module.css, Terminal.module.css, and EditorArea.module.css (collocation)
- `.crumbSubtitleSep` class exists only in EditorArea.module.css with `margin-left: auto` for right-alignment of the editor subtitle
- TypeScript compiles cleanly: `pnpm exec tsc --noEmit` exits 0
- Production build succeeds: `pnpm build` exits 0
- Desktop behavior unchanged: when IDEShell does not pass `onSelect`, Sidebar's `onSelect?.()` is a no-op (desktop sidebars remain open after file click)
- Manual visual verification (developer at 1440px and 375px viewports):
  - EXPLORER header shows "EXPLORER · File browser ruslankanat/portfolio"
  - Editor breadcrumb shows "<filename>" on the left and "· Code viewer" on the right edge
  - Terminal tab strip shows "TERMINAL · Test output PROBLEMS DEBUG CONSOLE"
</verification>

<success_criteria>
- Six files updated: 3 TSX (props/JSX) + 3 CSS (.paneSubtitle class, plus .crumbSubtitleSep for editor)
- Sidebar exposes a typed optional `onSelect?: () => void` prop ready for IDEShell consumption in Plan 03
- All three IDE panes show their plain-language label
- `pnpm exec tsc --noEmit` passes (no breaking changes to Sidebar callers — prop is optional)
- `pnpm build` passes
- All requirements addressed: UX-01 (three pane subtitles) and the prop-half of MOB-01 (onSelect prop infrastructure for Plan 03 to wire `toggleSidebar`)
- Plan 03 can implement IDEShell mobile state + backdrop + termHeight without touching any of the files in this plan
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsiveness-ux-labels/05-02-SUMMARY.md` when done.
</output>
