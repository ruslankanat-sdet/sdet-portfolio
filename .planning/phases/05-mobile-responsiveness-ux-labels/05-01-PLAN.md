---
phase: 05-mobile-responsiveness-ux-labels
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/ide/FileView.module.css
  - src/components/ide/Terminal.module.css
  - src/components/ide/Sidebar.module.css
  - src/components/ide/TopBar.module.css
  - src/components/layout/SiteHeader.module.css
  - src/app/about/about.module.css
autonomous: true
requirements:
  - MOB-02
  - MOB-03
  - MOB-04
  - MOB-05
tags:
  - mobile
  - css
  - responsive

must_haves:
  truths:
    - "At ≤768px the editor minimap is hidden so the code area has full width"
    - "At ≤768px terminal log entries wrap (no horizontal page overflow)"
    - "At ≤768px the terminal drag-resize handle is hidden"
    - "At ≤768px the terminal is capped at 180px max-height (safety net for D-04)"
    - "At ≤768px file rows and folder rows have a 44px minimum touch target"
    - "At ≤768px the hamburger, theme toggle, and Run Smoke Test buttons meet 44px touch target"
    - "At ≤480px the TopBar tab divider and ⌘↵ shortcut badge are hidden"
    - "At ≤768px the SiteHeader title truncates with ellipsis instead of pushing the nav off-screen"
    - "At ≤768px the About nav link meets the 44px touch target"
    - "At ≤768px tables in the About page get horizontal scroll instead of breaking the page width"
    - "At ≤480px the About page side padding reduces to 16px"
  artifacts:
    - path: "src/components/ide/FileView.module.css"
      provides: "Minimap hide-on-mobile rule"
      contains: "@media (max-width: 768px)"
    - path: "src/components/ide/Terminal.module.css"
      provides: "Log wrap + resizer hide + terminal max-height on mobile"
      contains: "white-space: pre-wrap"
    - path: "src/components/ide/Sidebar.module.css"
      provides: "Touch-target floor for tree rows on mobile"
      contains: "min-height: 44px"
    - path: "src/components/ide/TopBar.module.css"
      provides: "Mobile button touch targets + ≤480px decorative hides"
      contains: "min-height: 44px"
    - path: "src/components/layout/SiteHeader.module.css"
      provides: "Title ellipsis + nav link touch target at ≤768px"
      contains: "text-overflow: ellipsis"
    - path: "src/app/about/about.module.css"
      provides: "MDX table horizontal scroll + narrow-screen padding"
      contains: "overflow-x: auto"
  key_links:
    - from: "FileView.module.css .minimap @media (max-width: 768px)"
      to: "FileView.tsx rendered minimap"
      via: "display: none"
      pattern: "display:\\s*none"
    - from: "Terminal.module.css .log @media (max-width: 768px)"
      to: "Terminal.tsx log row spans"
      via: "white-space: pre-wrap; word-break: break-word"
      pattern: "white-space:\\s*pre-wrap"
    - from: "SiteHeader.module.css .title @media (max-width: 768px)"
      to: "SiteHeader.tsx title span"
      via: "overflow: hidden; text-overflow: ellipsis; white-space: nowrap"
      pattern: "text-overflow:\\s*ellipsis"
    - from: "about.module.css .pageWrapper table @media (max-width: 768px)"
      to: "MDX rendered <table> elements inside .pageWrapper"
      via: "display: block; overflow-x: auto"
      pattern: "overflow-x:\\s*auto"
---

<objective>
Apply pure-CSS mobile responsiveness fixes across IDE panes, header, and the About page. No JSX or component logic changes — every modification is a `@media` block addition or extension inside an existing CSS Module.

Purpose: Unblock Plans 02 and 03 by establishing the visual baseline. Mobile users get readable code (no minimap stealing horizontal space), wrapped terminal output, a non-overflowing header title, and tappable controls — all before the IDEShell logic plan (02) and the UX-labels plan (03) layer on top.

Output: Six CSS Module files updated with `@media (max-width: 768px)` and `@media (max-width: 480px)` blocks. No new component files, no JSX edits, no TypeScript changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-CONTEXT.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md
@.planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md

<interfaces>
<!-- All edits are CSS-only. The classes targeted already exist in their respective files. -->
<!-- Existing CSS Module class names (verified in 05-PATTERNS.md): -->

src/components/ide/FileView.module.css:
- .minimap          (lines 72–82, width: 70px)
- .editorInner      (lines 3–10, currently overflow: hidden)
- .code             (lines 36–47, already has overflow: auto; white-space: pre — D-07 already satisfied)

src/components/ide/Terminal.module.css:
- .terminal         (lines 3–11, min-height: 120px)
- .termResizer      (lines 13–21, drag handle, z-index: 5)
- .log              (lines 130–135, log row block)
- .logTag.pass, .logTag.fail  (inline child of .log — must remain unaffected)

src/components/ide/Sidebar.module.css:
- .sidebar          (existing @media (max-width: 768px) block lines 20–42 already handles overlay)
- .treeRow          (file row in tree)
- .treeFolder       (folder row in tree)

src/components/ide/TopBar.module.css:
- .menuBtn          (lines 322–331, shown only at ≤768px via display: grid)
- .iconBtn          (lines 296–318, theme toggle — 34×34px)
- .runBtn           (lines 334–351, padding: 9px 14px → ~38px height)
- .runShortcut      (lines 405–417, ⌘↵ badge inside run button)
- .tabDivider       (lines 81–84, decorative separator)
- existing @media breakpoints (descending order): 1380 → 1200 → 1060 → 920px

src/components/layout/SiteHeader.module.css:
- .title            (full title "Senior SDET / QA Automation Engineer")
- .separator        (· between name and title)
- .identity         (flex container holding name + title)
- nav               (nav element wrapping NavLink)
- .navLink          (lines 44–51, inline text — no explicit height)
- existing @media (max-width: 480px) (lines 65–70) hides .separator + .title

src/app/about/about.module.css:
- .pageWrapper      (lines 9–13, max-width: 720px; padding: 48px 24px 80px)
- (no existing @media blocks — add at bottom of file)

CSS naming convention: camelCase (treeRow, runBtn, paneSubtitle).
Media-query ordering: descending max-width values, one block per breakpoint per file.
Token references: never hardcode hex; always use var(--*) from globals.css (one exception: rgba(0,0,0,0.5) backdrop in Plan 02).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: IDE pane CSS — FileView minimap hide, Terminal log wrap + resizer hide + max-height, Sidebar touch targets</name>
  <files>src/components/ide/FileView.module.css, src/components/ide/Terminal.module.css, src/components/ide/Sidebar.module.css</files>
  <read_first>
    - src/components/ide/FileView.module.css (full file — confirm .minimap and .editorInner current state)
    - src/components/ide/Terminal.module.css (full file — confirm no existing @media block, locate .log, .termResizer, .terminal)
    - src/components/ide/Sidebar.module.css (full file — locate existing @media (max-width: 768px) block to APPEND to, not create a new one)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (sections "src/components/ide/FileView.module.css", "src/components/ide/Terminal.module.css", "src/components/ide/Sidebar.module.css" — exact CSS to add is specified there)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (sections "MOB-02", "MOB-03", "Spacing Scale — Sidebar Overlay Z-Index Hierarchy")
  </read_first>
  <action>
    Add three sets of `@media (max-width: 768px)` rules across three CSS Module files. All values are concrete and come from D-04, D-05, D-06, and the UI-SPEC.

    **A. src/components/ide/FileView.module.css** — append at the end of the file:
    ```css
    @media (max-width: 768px) {
      .minimap {
        display: none;
      }
    }
    ```
    Do NOT touch `.editorInner` or `.code` — `.code` already has `overflow: auto; white-space: pre` (D-07 satisfied). If after Plan 02 verification the `.code` horizontal scrollbar is visually clipped by `.editorInner`'s `overflow: hidden`, a future gap-closure plan can add `.editorInner { overflow: visible; }` inside the same block — do NOT add it speculatively here.

    **B. src/components/ide/Terminal.module.css** — append at the end of the file:
    ```css
    @media (max-width: 768px) {
      .log {
        white-space: pre-wrap;
        word-break: break-word;
      }
      .termResizer {
        display: none;
      }
      .terminal {
        max-height: 180px;
      }
    }
    ```
    Rationale: `.log` wrapping per D-06; resizer hide per D-05; `max-height: 180px` is a CSS safety net for D-04 (the JS sets 120px in Plan 02; 180px caps any future drift). Do NOT modify `.logTag.pass`, `.logTag.fail`, or any other class — the inline tag spans remain unaffected by parent `pre-wrap`.

    **C. src/components/ide/Sidebar.module.css** — APPEND to the existing `@media (max-width: 768px)` block (located around lines 20–42, currently contains `.sidebar` and `.sidebarClosed` rules). Add INSIDE the existing braces:
    ```css
      .treeRow,
      .treeFolder {
        min-height: 44px;
      }
    ```
    Do NOT create a second `@media (max-width: 768px)` block — the established convention is one block per breakpoint per file (see 05-PATTERNS.md "CSS Media Query Ordering Convention"). Do NOT add `align-items: center` — it is already inherited from the existing `.treeRow` / `.treeFolder` rules.

    All new CSS uses existing classes only. No new class names are introduced in this task (`.paneSubtitle` is Plan 03's responsibility).
  </action>
  <verify>
    <automated>grep -E "@media \(max-width: 768px\)" src/components/ide/FileView.module.css | wc -l | grep -q "^[[:space:]]*1$" && grep -E "white-space:\s*pre-wrap" src/components/ide/Terminal.module.css && grep -E "display:\s*none" src/components/ide/Terminal.module.css | grep -v "^#" | head -1 && grep -E "max-height:\s*180px" src/components/ide/Terminal.module.css && grep -E "min-height:\s*44px" src/components/ide/Sidebar.module.css</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ide/FileView.module.css contains exactly one `@media (max-width: 768px)` block
    - src/components/ide/FileView.module.css contains `.minimap` with `display: none` inside that @media block
    - src/components/ide/Terminal.module.css contains `white-space: pre-wrap` (in a @media block)
    - src/components/ide/Terminal.module.css contains `word-break: break-word`
    - src/components/ide/Terminal.module.css contains `.termResizer { display: none; }` (or equivalent) inside a `@media (max-width: 768px)` block
    - src/components/ide/Terminal.module.css contains `max-height: 180px` for `.terminal` inside a `@media (max-width: 768px)` block
    - src/components/ide/Sidebar.module.css contains `min-height: 44px` for `.treeRow` and `.treeFolder` inside a `@media (max-width: 768px)` block
    - `pnpm exec next lint --file src/components/ide/FileView.module.css --file src/components/ide/Terminal.module.css --file src/components/ide/Sidebar.module.css` exits 0 (or skip if Next.js does not lint CSS — `pnpm build` exits 0 as fallback)
    - No new classes added — grep for `.paneSubtitle` returns 0 hits in Sidebar.module.css and Terminal.module.css after this task
  </acceptance_criteria>
  <done>
    FileView.module.css hides the minimap below 768px. Terminal.module.css wraps log rows, hides the resizer, and caps terminal height at 180px below 768px. Sidebar.module.css guarantees 44px touch targets for file/folder rows below 768px. No JSX changes. No new classes. `pnpm build` passes.
  </done>
</task>

<task type="auto">
  <name>Task 2: TopBar mobile touch targets + ≤480px decorative hides</name>
  <files>src/components/ide/TopBar.module.css</files>
  <read_first>
    - src/components/ide/TopBar.module.css (full file — verify class names and existing breakpoint ordering 1380 → 1200 → 1060 → 920px)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (section "src/components/ide/TopBar.module.css" — exact rules specified)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (sections "Touch targets (WCAG AA)" and "TopBar at ≤375px")
  </read_first>
  <action>
    Append TWO new `@media` blocks at the end of src/components/ide/TopBar.module.css, in descending-breakpoint order (768px first, then 480px), following the file's existing convention (existing blocks: 1380px, 1200px, 1060px, 920px, then the existing `@media (max-width: 768px)` that turns `.menuBtn` from `display: none` to `display: grid`).

    **First new block (or extend existing 768px block if one already exists — see file):**
    ```css
    @media (max-width: 768px) {
      .menuBtn {
        width: 44px;
        height: 44px;
      }
      .iconBtn {
        width: 44px;
        height: 44px;
      }
      .runBtn {
        min-height: 44px;
      }
    }
    ```
    If a `@media (max-width: 768px)` block already exists that toggles `.menuBtn` to `display: grid`, APPEND `width: 44px; height: 44px;` inside that existing `.menuBtn` rule AND add the `.iconBtn` and `.runBtn` rules inside the same block — do NOT create a duplicate block (per the file's convention).

    **Second new block (always add at the bottom):**
    ```css
    @media (max-width: 480px) {
      .tabDivider {
        display: none;
      }
      .runShortcut {
        display: none;
      }
    }
    ```

    Do NOT hide `.logoMark` — keep it visible at ≤480px (UI-SPEC marks it as "evaluate visually first"; default is keep). Do NOT touch `.runBtn` padding — only adding `min-height`. Do NOT change the existing button widths at desktop breakpoints. Token references stay as `var(--*)` — no hardcoded colors are introduced (only sizes in px).
  </action>
  <verify>
    <automated>grep -E "min-height:\s*44px" src/components/ide/TopBar.module.css && grep -E "@media \(max-width: 480px\)" src/components/ide/TopBar.module.css && grep -A2 "@media \(max-width: 480px\)" src/components/ide/TopBar.module.css | grep -E "\.tabDivider|\.runShortcut"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/ide/TopBar.module.css contains `min-height: 44px` (applied to `.runBtn` inside a `@media (max-width: 768px)` block)
    - src/components/ide/TopBar.module.css contains `width: 44px` and `height: 44px` for `.menuBtn` inside a `@media (max-width: 768px)` block
    - src/components/ide/TopBar.module.css contains `width: 44px` and `height: 44px` for `.iconBtn` inside a `@media (max-width: 768px)` block
    - src/components/ide/TopBar.module.css contains a `@media (max-width: 480px)` block
    - Inside that ≤480px block, `.tabDivider` has `display: none` and `.runShortcut` has `display: none`
    - `.logoMark` is NOT hidden (grep `.logoMark.*display:\s*none` inside `@media (max-width: 480px)` returns 0 hits)
    - `.runBtn` padding is unchanged (grep for `padding: 9px 14px 9px 12px` still present in the non-media `.runBtn` rule)
    - `pnpm build` exits 0
  </acceptance_criteria>
  <done>
    TopBar buttons (hamburger, theme toggle, Run Smoke Test) meet 44×44px touch targets at ≤768px. Decorative `.tabDivider` and `.runShortcut` (⌘↵ badge) are hidden at ≤480px. Logo mark remains visible. No desktop regressions. `pnpm build` passes.
  </done>
</task>

<task type="auto">
  <name>Task 3: SiteHeader title ellipsis + nav touch target; About page table scroll + narrow padding</name>
  <files>src/components/layout/SiteHeader.module.css, src/app/about/about.module.css</files>
  <read_first>
    - src/components/layout/SiteHeader.module.css (full file — confirm existing `@media (max-width: 480px)` at lines 65–70 hides `.separator` and `.title`)
    - src/app/about/about.module.css (full file — confirm no existing @media blocks)
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-PATTERNS.md (sections "src/components/layout/SiteHeader.module.css" and "src/app/about/about.module.css")
    - .planning/phases/05-mobile-responsiveness-ux-labels/05-UI-SPEC.md (sections "MOB-04: Header Nav" and "MOB-05: About Page")
    - src/components/layout/SiteHeader.tsx (one read — confirm `.title`, `.separator`, `.identity`, nav, `.navLink` are the JSX class references in use)
  </read_first>
  <action>
    **A. src/components/layout/SiteHeader.module.css** — INSERT a new `@media (max-width: 768px)` block BEFORE the existing `@media (max-width: 480px)` block (descending-breakpoint order is reversed in this file — 768 must come BEFORE 480 in source order so the 480 rules take precedence via cascade). Verify order during edit; if existing block is at lines 65–70, insert immediately above line 65.

    Block to add:
    ```css
    @media (max-width: 768px) {
      .title {
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
      }
      .identity {
        flex: 1;
        min-width: 0;
      }
      nav {
        flex-shrink: 0;
      }
      .navLink {
        display: inline-flex;
        align-items: center;
        min-height: 44px;
        padding: 0 8px;
      }
    }
    ```

    Rationale: `flex: 1; min-width: 0` on `.identity` lets the flex container shrink below its content's intrinsic width, which is what allows `text-overflow: ellipsis` on `.title` to actually take effect. `flex-shrink: 0` on `nav` prevents the About link from being crushed before the title truncates. `.navLink` becomes a 44px-tall inline-flex with 8px horizontal padding to meet WCAG AA touch target.

    Do NOT modify the existing `@media (max-width: 480px)` block — its `.separator { display: none }` and `.title { display: none }` rules already handle the very-narrow case and cascade correctly after the 768px block.

    **B. src/app/about/about.module.css** — APPEND at the end of the file (no existing @media blocks):
    ```css
    @media (max-width: 768px) {
      .pageWrapper table {
        display: block;
        overflow-x: auto;
        -webkit-overflow-scrolling: touch;
      }
    }

    @media (max-width: 480px) {
      .pageWrapper {
        padding-left: 16px;
        padding-right: 16px;
      }
    }
    ```

    Rationale: MDX-rendered `<table>` elements (skills, experience comparisons) become block-level with horizontal scroll at ≤768px instead of forcing page-width overflow. At ≤480px, reduce side padding from the default 24px down to 16px (per UI-SPEC MOB-05). `-webkit-overflow-scrolling: touch` enables momentum scroll inside the table on iOS Safari.

    Do NOT alter `.pageWrapper`'s max-width (720px) or vertical padding (48px / 80px). Do NOT touch `.proseH1` size (32px is intentional per UI-SPEC).
  </action>
  <verify>
    <automated>grep -E "text-overflow:\s*ellipsis" src/components/layout/SiteHeader.module.css && grep -E "min-height:\s*44px" src/components/layout/SiteHeader.module.css && grep -E "overflow-x:\s*auto" src/app/about/about.module.css && grep -E "padding-left:\s*16px" src/app/about/about.module.css && pnpm build 2>&1 | tail -5 | grep -E "Compiled|✓|success"</automated>
  </verify>
  <acceptance_criteria>
    - src/components/layout/SiteHeader.module.css contains `text-overflow: ellipsis` for `.title` inside a `@media (max-width: 768px)` block
    - src/components/layout/SiteHeader.module.css contains `min-height: 44px` for `.navLink` inside a `@media (max-width: 768px)` block
    - src/components/layout/SiteHeader.module.css contains `flex: 1` and `min-width: 0` for `.identity` inside a `@media (max-width: 768px)` block
    - src/components/layout/SiteHeader.module.css contains `flex-shrink: 0` for `nav` inside a `@media (max-width: 768px)` block
    - The 768px block appears BEFORE the existing 480px block in source order (verify with `grep -n "@media" src/components/layout/SiteHeader.module.css`)
    - src/app/about/about.module.css contains `.pageWrapper table` with `display: block; overflow-x: auto` inside a `@media (max-width: 768px)` block
    - src/app/about/about.module.css contains `padding-left: 16px` and `padding-right: 16px` for `.pageWrapper` inside a `@media (max-width: 480px)` block
    - `pnpm build` exits 0 (full project build succeeds — no CSS syntax errors)
    - At 375px viewport (visual check via DevTools): SiteHeader title truncates with ellipsis instead of pushing the About nav link off-screen — captured in the post-task summary's visual notes
  </acceptance_criteria>
  <done>
    SiteHeader title ellipsis-truncates at ≤768px while the About nav link stays reachable with a 44px touch target. About page tables scroll horizontally instead of breaking layout at ≤768px. About page side padding reduces to 16px at ≤480px. `pnpm build` passes.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| (none introduced) | This plan is CSS-only — no new code paths, no new inputs, no new network boundaries, no new persistence. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Information Disclosure | CSS bundle | accept | CSS Modules are static, scoped class names, no secrets. No new attack surface introduced. |
| T-05-02 | Denial of Service | About page table scroll on touch devices | accept | `-webkit-overflow-scrolling: touch` is a passive browser hint; no JS handlers added. Resource usage unchanged. |
| T-05-03 | Tampering | Build pipeline | accept | No new dependencies. No package installs in this plan. ASVS L1 supply-chain requirements not triggered. |

This plan introduces no new backend routes, no new auth/session changes, no new data persistence, no new user input handlers, no new external API calls, and no new dependencies. The threat model is intentionally minimal — every change is a CSS Module edit affecting visual rendering only.
</threat_model>

<verification>
- All six CSS Module files compile cleanly: `pnpm build` exits 0
- Existing desktop layout is unchanged (no rules outside `@media (max-width: 768px)` or `@media (max-width: 480px)` were modified)
- No new global CSS introduced; every rule lives inside a `*.module.css` file
- No hex colors hardcoded (only the existing `rgba(0,0,0,0.5)` backdrop is deferred to Plan 02)
- All required spec items present: minimap hide, terminal log wrap, terminal resizer hide, terminal max-height, sidebar touch targets, TopBar button touch targets, TopBar ≤480px hides, SiteHeader ellipsis + nav touch target, About table scroll, About narrow padding
- Manual visual verification (developer): open at 375px viewport in DevTools — code area shows no minimap, SiteHeader title truncates rather than pushing nav off-screen, About page tables scroll horizontally inside the prose container
</verification>

<success_criteria>
- Six CSS Module files updated with `@media` blocks per the UI-SPEC
- Zero JSX, TypeScript, or component prop changes
- `pnpm build` passes (no CSS syntax errors, no Next.js build failures)
- All requirements addressed: MOB-02 (minimap hide enables full code width — `.code` already has `overflow: auto; white-space: pre` from prior work), MOB-03 (log wrap + resizer hide + max-height), MOB-04 (header title ellipsis + nav touch target), MOB-05 (About page table scroll + narrow padding)
- Plans 02 and 03 can proceed without revisiting any of the files in this plan
</success_criteria>

<output>
Create `.planning/phases/05-mobile-responsiveness-ux-labels/05-01-SUMMARY.md` when done.
</output>
