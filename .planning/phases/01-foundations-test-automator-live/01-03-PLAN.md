---
phase: 01-foundations-test-automator-live
plan: 03
type: execute
wave: 2
depends_on: [01, 04]
files_modified:
  - src/app/page.tsx
  - src/components/ide/IDEShell.tsx
  - src/components/ide/IDEShell.module.css
  - src/components/ide/TopBar.tsx
  - src/components/ide/TopBar.module.css
  - src/components/ide/Sidebar.tsx
  - src/components/ide/Sidebar.module.css
  - src/components/ide/EditorArea.tsx
  - src/components/ide/EditorArea.module.css
  - src/components/ide/FileView.tsx
  - src/components/ide/FileView.module.css
  - src/components/ide/Terminal.tsx
  - src/components/ide/Terminal.module.css
  - src/components/ide/StatusBar.tsx
  - src/components/ide/StatusBar.module.css
autonomous: false
requirements:
  - FOUND-04
  - SHELL-01
  - SHELL-02
  - SHELL-03
  - SHELL-04
  - SHELL-05

must_haves:
  truths:
    - "Visiting `/` renders the IDE chrome: 56px topbar, 232px sidebar (48px activity bar + 184px explorer), editor area with tab bar + breadcrumb + file viewer + decorative minimap, resizable 220px-default terminal, 26px statusbar"
    - "README.md is the active file on first load (D-04); its content is syntax-highlighted via the ported tokenizer"
    - "The sidebar file tree shows `about/` folder containing `bio.json`, `experience.yaml`, `skills.toml`, plus root-level `README.md` and `contact.json`"
    - "Clicking a file in the sidebar switches the editor area to display that file's syntax-highlighted content"
    - "Clicking the Run Smoke Test button (or pressing ⌘↵ / Ctrl+↵) clears the terminal and streams the Ruslan-specific SAMPLE_LOGS entries with 280–540ms jitter; while running the Run button pulses; the terminal cursor `ruslan@portfolio ❯` blinks"
    - "The statusbar's rightmost item is `● Available for hire` in `var(--green)` and never wraps or hides at any breakpoint"
    - "Tab close (×), tab switch, and tab open from sidebar all work; closing the active tab falls back to the last remaining tab; closing the last tab leaves the editor empty without crashing"
    - "Theme toggle button flips `<html data-theme>` between dark and light and persists to localStorage['portfolio-theme']"
    - "Activity bar is `<nav aria-label='Activity bar'>` with `aria-label` + `title` on every icon button"
    - "Terminal log region has `aria-live='polite'` so screen readers announce new log lines"
    - "Pixel-perfect token use: every color, font-size, border value in every `.module.css` resolves to `var(--*)` from globals.css — no raw hex literals introduced"
    - "No AIChat component — no floating chat FAB, no AI panel, no canned responses"
  artifacts:
    - path: "src/components/ide/IDEShell.tsx"
      provides: "Root Client Component that owns the entire IDE state model (activeFile, tabs, termHeight, termTab, running, logs, lastRun, theme) and wires keyboard shortcuts; renders TopBar + Sidebar + EditorArea + Terminal + StatusBar + AIChat"
      contains: "use client"
      min_lines: 60
    - path: "src/components/ide/IDEShell.module.css"
      provides: ".ideBody flex layout, .main flex column"
      contains: "var(--bg-deep)"
    - path: "src/components/ide/TopBar.tsx"
      provides: "Logo, branch indicator, status badge rail (CI/Coverage/Tests), clock, theme toggle, Run Smoke Test button with ⌘↵ shortcut badge"
      contains: "ruslan.kanatbek"
    - path: "src/components/ide/Sidebar.tsx"
      provides: "Activity bar (7 nav buttons) + explorer panel (header + file tree with tools/ + about/ folders + root files)"
      contains: "aria-label=\"Activity bar\""
    - path: "src/components/ide/EditorArea.tsx"
      provides: "Tab bar + breadcrumb + content dispatch (FileView vs TestAutomatorPane based on activeFile)"
      contains: "TestAutomatorPane"
    - path: "src/components/ide/FileView.tsx"
      provides: "Gutter (line numbers) + syntax-highlighted code (via tokenize from @/lib/syntax-highlighter) + decorative minimap"
      contains: "tokenize"
    - path: "src/components/ide/Terminal.tsx"
      provides: "Resizable terminal (drag, min 120/max 500), tab strip (TERMINAL/PROBLEMS/OUTPUT/DEBUG CONSOLE), log row variants, blinking prompt while running"
      contains: "aria-live=\"polite\""
    - path: "src/components/ide/StatusBar.tsx"
      provides: "Bottom 26px bar with branch/state on left, position/lang/lines/Available for hire on right"
      contains: "Available for hire"
  key_links:
    - from: "src/app/page.tsx"
      to: "src/components/ide/IDEShell"
      via: "import { IDEShell } from '@/components/ide/IDEShell'; render in default export"
      pattern: "IDEShell"
    - from: "src/components/ide/Sidebar.tsx"
      to: "src/lib/files-data.ts (FILES + TOOLS + SAMPLE_LOGS)"
      via: "named import — drives the tree structure"
      pattern: "from '@/lib/files-data'"
    - from: "src/components/ide/FileView.tsx"
      to: "src/lib/syntax-highlighter.ts"
      via: "import { tokenize }; called with file.content + file.lang"
      pattern: "tokenize\\("
    - from: "src/components/ide/EditorArea.tsx"
      to: "src/components/ide/FileView"
      via: "renders FileView for every file entry in FILES"
      pattern: "FileView"
    - from: "src/components/ide/IDEShell.tsx"
      to: "all child IDE components"
      via: "prop drilling (state lives in IDEShell, passed down — no global state library per CLAUDE.md)"
      pattern: "useState"
---

<objective>
Port the IDE chrome from the design handoff JSX prototype (`app.jsx`, `sidebar.jsx`, `workspace.jsx`) to TypeScript React Client Components with CSS Modules. After this plan ships, `pnpm dev` on `/` shows a fully visible IDE: topbar with logo/badges/run button, sidebar with Ruslan's about/ file tree, editor showing README.md syntax-highlighted, animated terminal with smoke-test logs, and statusbar with `● Available for hire`.

NO AI chat component. NO Test Automator pane. The editor area renders FileView for every file — that is the full scope.

Purpose: Deliver FOUND-04 (public Vercel URL with IDE chrome as landing page) and SHELL-01..SHELL-05 (IDE chrome requirements).
Output: 7 component files + 7 CSS Module files + page.tsx wiring.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundations-test-automator-live/01-CONTEXT.md
@.planning/phases/01-foundations-test-automator-live/01-UI-SPEC.md
@.planning/phases/01-foundations-test-automator-live/01-PATTERNS.md
@.planning/design/design_handoff_ide_portfolio/README.md
@.planning/design/design_handoff_ide_portfolio/app.jsx
@.planning/design/design_handoff_ide_portfolio/sidebar.jsx
@.planning/design/design_handoff_ide_portfolio/workspace.jsx
@.planning/design/design_handoff_ide_portfolio/ai-chat.jsx
@.planning/design/design_handoff_ide_portfolio/styles.css
@src/types/ide.ts
@src/lib/utils.ts
</context>

<interfaces>
<!-- Contracts emitted by plan 01 and plan 04 that this plan's components consume. -->

From src/types/ide.ts:
- `FileEntry`, `LogEntry`, `LogKind`, `Theme`, `TerminalTab`, `FileEntryLang`, `FileEntryIcon`

From src/lib/utils.ts:
- `cn(...inputs: ClassValue[]): string`

From src/lib/files-data.ts (plan 04 — depends_on):
- `FILES: Record<string, FileEntry>` — keyed by filename
- `SAMPLE_LOGS: LogEntry[]` — Ruslan-specific test log lines

From src/lib/syntax-highlighter.ts (plan 04):
- `tokenize(src: string, lang: string): ReactNode[]` — emits React `<span class="tk-*">` children

The Lucide icons used across the IDE (import from `lucide-react`):
- `ChevronRight`, `Folder`, `FileJson2`, `FileCode2`, `FileText`, `Search`, `GitBranch`, `Play`, `X`, `Sparkles`, `Send`, `Bell`, `Sun`, `Moon`, `Settings`, `User`, `Boxes`, `Bug`
</interfaces>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Server-rendered HTML → client hydration | IDE shell hydrates with `useState` lazy initializers reading `localStorage` — must avoid hydration mismatch |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-02 | Tampering | FileView render of file.content | mitigate | `tokenize()` returns `ReactNode[]` (React span elements), NOT HTML strings. FileView renders `<code>{tokens}</code>`, never `dangerouslySetInnerHTML`. CI guards via the workflow's bundle grep (plan 02) plus a local `grep -r 'dangerouslySetInnerHTML' src/` assertion below. |
| T-1-06 | Tampering | Theme localStorage read on SSR boundary | accept | Per RESEARCH Pitfall 6 — `useState` lazy initializer is wrapped in try/catch; server-rendered HTML hard-codes `data-theme="dark"`; client may flip on hydration. Acceptable single-tick visual delta on first paint for users who previously chose light. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Build IDEShell root component (state owner) + TopBar + Sidebar</name>
  <files>
    src/components/ide/IDEShell.tsx
    src/components/ide/IDEShell.module.css
    src/components/ide/TopBar.tsx
    src/components/ide/TopBar.module.css
    src/components/ide/Sidebar.tsx
    src/components/ide/Sidebar.module.css
    src/app/page.tsx
  </files>
  <read_first>
    .planning/design/design_handoff_ide_portfolio/app.jsx                       # Full root state model + keyboard shortcut wiring — port verbatim with TS types
    .planning/design/design_handoff_ide_portfolio/sidebar.jsx                   # Sidebar function — port; replace folder set to about/ only (no tools/)
    .planning/design/design_handoff_ide_portfolio/workspace.jsx                 # TopBar + StatusBadge (lines 44–92) — port; substitute Ruslan content per UI-SPEC
    .planning/design/design_handoff_ide_portfolio/styles.css                    # Source of truth for every CSS Module — copy selectors that match each component
    .planning/phases/01-foundations-test-automator-live/01-PATTERNS.md          # IDEShell + TopBar + Sidebar sections — exact port instructions + Migration Rules
    .planning/phases/01-foundations-test-automator-live/01-UI-SPEC.md           # Component Inventory, Copywriting Contract, Content Substitutions, A11y Requirements
    .planning/phases/01-foundations-test-automator-live/01-CONTEXT.md           # D-03 (Alex → Ruslan), D-04 (README default), D-15 (Available for hire)
    src/types/ide.ts                                                             # FileEntry, LogEntry, Theme — already exported
    src/lib/utils.ts                                                             # cn() helper signature
  </read_first>
  <action>
**1. `src/components/ide/IDEShell.tsx`** — first line `"use client";`. Port of `app.jsx` App function. State model (typed per PATTERNS.md `IDEShell.tsx` section):
   - `activeFile: string` — initial `"README.md"` (D-04, NOT `bio.json` from the original)
   - `tabs: string[]` — initial `["README.md"]`
   - `termHeight: number` — initial `220`
   - `termTab: TerminalTab` — initial `"TERMINAL"`
   - `running: boolean` — initial `false`
   - `logs: LogEntry[]` — initial `SAMPLE_LOGS.slice(0, 8)` (import from `@/lib/files-data`)
   - `lastRun: string` — initial `"3m ago"`
   - `theme: Theme` — initial via lazy try/catch localStorage read (per PATTERNS.md and RESEARCH Pitfall 6)

   Callbacks (per PATTERNS.md):
   - `toggleTheme`
   - `openTab(name)` — adds if not present
   - `closeTab(name)` — removes; if was active and `next.length > 0` set active to last; if `next.length === 0` leave activeFile pointing at the closed name (EditorArea handles empty-tab fallback)
   - `runSmoke()` — clears logs, sets running=true, ticks through SAMPLE_LOGS with `setTimeout(tick, 280 + Math.random() * 260)` jitter

   Effects:
   - Theme sync: `document.documentElement.setAttribute("data-theme", theme)` + localStorage write (in try/catch)
   - Keyboard listener: `keydown` on window; if `(metaKey || ctrlKey) && key === "Enter"` → preventDefault + runSmoke

   Render structure:
   ```tsx
   return (
     <>
       <TopBar onRun={runSmoke} running={running} theme={theme} toggleTheme={toggleTheme} />
       <div className={styles.ideBody}>
         <Sidebar activeFile={activeFile} setActiveFile={setActiveFile} openTab={openTab} />
         <main className={styles.main}>
           <EditorArea tabs={tabs} activeFile={activeFile} setActiveFile={setActiveFile} closeTab={closeTab} />
           <Terminal logs={logs} running={running} height={termHeight} setHeight={setTermHeight} tab={termTab} setTab={setTermTab} />
         </main>
       </div>
       <StatusBar activeFile={activeFile} running={running} />
     </>
   );  // NO AIChat — removed from scope
   ```

**2. `src/components/ide/IDEShell.module.css`** — port `.ide-body` and `.main` selectors from styles.css. Use `var(--bg-deep)`, `var(--border)`. `.ideBody` is flex row, fills remaining height after topbar (56px) and before statusbar (26px). `.main` is flex column, fills width.

**3. `src/components/ide/TopBar.tsx`** — `"use client";`. Port of workspace.jsx lines 44–92 (TopBar + StatusBadge). Props per PATTERNS.md TopBar section.
   Content substitutions per UI-SPEC Content Substitutions table and Copywriting Contract:
   - Logo name: `ruslan.kanatbek` (mono 13px, 600)
   - Logo sub: `SDET · AI Automation` (mono 10.5px, `var(--text-faint)`, letter-spacing 0.04em)
   - Branch label: `main` + meta `· up to date`
   - Badges (left-to-right): CI · Passing (tone-green, pulsing dot, always visible), Coverage · 98% (tone-green, hidden at ≤1200px via `.badge-rail-2` class), Tests · 312 (tone-blue, hidden at ≤1380px via `.badge-rail-3` class)
   - Clock: `HH:MM` + timezone `UTC+5` (mono 12px), hidden at ≤920px
   - Theme toggle: lucide `Sun` (when dark) / `Moon` (when light), `aria-label="Toggle theme"`, `title="Toggle theme"`
   - Run button: per UI-SPEC — `Run Smoke Test` + `⌘↵` kbd badge when idle, `Running…` when running; pulsing class while running; lucide `Play` size 11; `aria-label="Run smoke test"` (or "Running smoke test" when active)
   - Status rail: implement as `<div role="status">` (UI-SPEC doesn't require this explicitly; safe default)

**4. `src/components/ide/TopBar.module.css`** — port `.topbar`, `.logo*`, `.branch*`, `.badge*`, `.status-rail`, `.clock`, `.icon-btn`, `.run-btn`, `.run-btn.running`, `.run-shortcut` from styles.css. Height 56px, `var(--bg-deep)`, `var(--border)` bottom edge, green gradient underline pseudo-element. Run button matches `.run-btn` spec from UI-SPEC (green gradient, `padding: 9px 14px 9px 12px`, font Inter 12.5px 600).

**5. `src/components/ide/Sidebar.tsx`** — `"use client";`. Port of sidebar.jsx but file tree restructured per D-05:
   - `tools/` folder — expanded by default — contains ToolEntry rows. Phase 1 only entry: `test-automator` (render via toolRow helper with `Sparkles` icon in `var(--green)`)
   - `about/` folder — collapsed by default — contains `bio.json`, `experience.yaml`, `skills.toml`
   - Root level files (no folder): `README.md`, `contact.json`
   - State: `const [open, setOpen] = useState({ tools: true, about: false });`
   - File row: per PATTERNS.md — clickable with `role="button"`, `tabIndex={0}`, calls `setActiveFile(name)` + `openTab(name)`
   - Tool row: identical handler, distinct icon (`Sparkles` size 14, green)
   - Active row gets `styles.active` (green gradient left bg + 2px inset-left `var(--green)` border per UI-SPEC)
   - Activity bar (per UI-SPEC + PATTERNS.md): `<nav aria-label="Activity bar">` containing 5 top buttons + 2 bottom buttons. Each `<button>` has BOTH `aria-label` AND `title` with the same text (UI-SPEC accessibility line: "title supplements aria-label — both are required; they should carry the same text value"). Top: Explorer (active by default — `Folder`), Search, Source Control (`GitBranch`), Run & Debug (`Play`), Extensions (`Boxes`). Bottom: Profile (`User`), Settings (`Settings`). Active button gets the green glow bar (`position: absolute; left: -10px` per UI-SPEC).
   - Explorer header: `EXPLORER` (10.5px, weight 600, letter-spacing 0.08em) + `ruslankanat/portfolio` (10px mono, `var(--text-faint)`)
   - Outline panel at bottom — port from sidebar.jsx if present; if absent in design handoff, ship a minimal stub with `OUTLINE` header + a couple of symbol rows derived from active file (or leave as static — outline is decorative in Phase 1)

**6. `src/components/ide/Sidebar.module.css`** — port `.sidebar`, `.activity-bar`, `.act-btn`, `.act-btn.active`, `.explorer`, `.explorer-header`, `.explorer-meta`, `.tree`, `.tree-folder`, `.tree-row`, `.tree-row.active`, `.tree-label`, `.tree-badge`, `.tree-indent`, `.outline`, `.outline-row`, `.branch` (if sidebar uses it). Activity bar width 48px, explorer width 184px (total 232px), use `var(--bg-deep)` background.

**7. `src/components/ide/AIChat.tsx`** — `"use client";`. Port of ai-chat.jsx with these changes:
   - Replace the `window.claude.complete({ messages })` call with a hard-coded `CANNED_RESPONSES` map keyed off the three quick prompts per UI-SPEC and PATTERNS.md
   - Greeting (per UI-SPEC Copywriting Contract): `"Hi — I'm Ruslan's portfolio assistant. Ask me about test strategy, AI eval pipelines, or why you should hire him."`
   - Quick prompts (per UI-SPEC AIChat section): `"Walk me through your eval pipeline"`, `"How do you keep flake rate under 1%?"`, `"Are you open to staff roles?"`
   - Canned responses per PATTERNS.md (the three response strings); fallback message for any other text: `"Great question — I can speak to test architecture, AI eval pipelines, or Ruslan's background. What specifically would you like to know?"`
   - Fake latency: `await new Promise(r => setTimeout(r, 600 + Math.random() * 400))` before posting AI reply
   - FAB: 52×52, fixed bottom-right (`right: 22px; bottom: 44px`), `aria-label="Ask my AI Assistant"`, `aria-expanded={open}`
   - Panel header sub label: `online · claude-haiku-4-5` (per UI-SPEC — even though no real call happens in Phase 1; this aligns with the future Phase-4 wiring)
   - System prompt content (per PATTERNS.md) — store in a comment in the file for Phase 4 reference (no API call yet, but documenting now prevents loss):
     ```
     // SYSTEM_PROMPT (Phase 4 wiring): "You are the AI assistant on the portfolio site of Ruslan Kanatbek..."
     ```

**8. `src/components/ide/AIChat.module.css`** — port `.ai-widget`, `.ai-fab`, `.ai-fab-glow`, `.ai-panel`, `.ai-header`, `.ai-avatar`, `.ai-name`, `.ai-foot`, `.ai-body`, `.ai-msg`, `.ai-bubble`, `.ai-quick-btn`, `.ai-input-row`, `.ai-input`, `.ai-send`, `.ai-typing`. Apply `panel-in` animation on open (220ms cubic-bezier(.2,.9,.3,1.1) per UI-SPEC + globals.css keyframes).

**9. `src/app/page.tsx`** — overwrite the placeholder from plan 01:
   ```tsx
   import { IDEShell } from '@/components/ide/IDEShell';
   export default function Home() {
     return <IDEShell />;
   }
   ```
   `page.tsx` remains a Server Component (no `"use client"`). IDEShell carries the directive.

**Universal rules across all 9 files** (per PATTERNS.md Migration Rules):
- First line of every `.tsx` file with interactivity: `"use client";`
- Replace every inline SVG from the JSX prototype with the appropriate Lucide React import (see PATTERNS.md Lucide mapping table)
- Replace every `window.FILES` / `window.tokenize` / `window.Sidebar = ...` pattern with proper ES module imports
- Use `cn()` from `@/lib/utils` for every conditional className (no string concatenation of class names)
- No `any` types — use the shared types from `@/types/ide`
- No `@apply` in any CSS Module (RESEARCH Pitfall 3)
- No raw hex colors in CSS Modules — every color via `var(--*)`
- No `dangerouslySetInnerHTML` anywhere (CLAUDE.md ban, T-1-02 mitigation)

DO NOT in this task: build EditorArea/FileView/Terminal/StatusBar (Task 2 of this plan); build the Test Automator pane (plan 06); wire any API call from AIChat (deferred per CONTEXT.md — Phase 4); add Tailwind utility classes to any of these CSS Modules (IDE chrome is CSS Modules per RESEARCH Pattern 2 + UI-SPEC).
  </action>
  <verify>
    <automated>grep -c '"use client"' src/components/ide/IDEShell.tsx && grep -c 'useState' src/components/ide/IDEShell.tsx && grep -c '"README.md"' src/components/ide/IDEShell.tsx && grep -c 'metaKey || e.ctrlKey' src/components/ide/IDEShell.tsx && grep -c 'ruslan.kanatbek' src/components/ide/TopBar.tsx && grep -c 'Run Smoke Test' src/components/ide/TopBar.tsx && grep -c 'aria-label="Activity bar"' src/components/ide/Sidebar.tsx && grep -c 'tools: true' src/components/ide/Sidebar.tsx && grep -c "Ruslan's portfolio assistant" src/components/ide/AIChat.tsx && grep -c 'aria-label="Ask my AI Assistant"' src/components/ide/AIChat.tsx && grep -c 'aria-expanded' src/components/ide/AIChat.tsx && grep -c "from '@/components/ide/IDEShell'" src/app/page.tsx && (grep -v '^\s*\(//\|\*\|#\)' src/components/ide/IDEShell.module.css src/components/ide/TopBar.module.css src/components/ide/Sidebar.module.css src/components/ide/AIChat.module.css | grep -cE '#[0-9a-fA-F]{3,8}' | grep -q '^0$') && ! grep -r 'dangerouslySetInnerHTML' src/components/ide/ 2>/dev/null && ! grep -r '@apply' src/components/ide/ 2>/dev/null && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ide/IDEShell.tsx` starts with `"use client";`, declares all 8 state hooks (`activeFile`, `tabs`, `termHeight`, `termTab`, `running`, `logs`, `lastRun`, `theme`), and uses `"README.md"` as the activeFile initial value
    - `IDEShell.tsx` registers a `keydown` listener on `window` matching `(metaKey || ctrlKey) && key === "Enter"`
    - `IDEShell.tsx` imports `SAMPLE_LOGS` from `@/lib/files-data` (depends_on plan 04)
    - `src/components/ide/TopBar.tsx` contains the string `ruslan.kanatbek` (logo) and `Run Smoke Test` and `⌘↵` and `Coverage · 98%`
    - `TopBar.tsx` uses lucide `Play` icon on the Run button (`import { Play } from 'lucide-react'`)
    - `src/components/ide/Sidebar.tsx` declares `<nav aria-label="Activity bar">`
    - `Sidebar.tsx` initial folder state: `useState({ tools: true, about: false })`
    - `Sidebar.tsx` renders a row for `test-automator` (the tool id) — `grep test-automator src/components/ide/Sidebar.tsx` matches
    - Every activity-bar `<button>` in `Sidebar.tsx` has both `aria-label=` and `title=` attributes (manually verifiable by inspecting each button — automated: grep for `aria-label=` count >= grep for `<button` count inside the activity bar block)
    - `src/components/ide/AIChat.tsx` contains the exact greeting string `Hi — I'm Ruslan's portfolio assistant.`
    - `AIChat.tsx` button carries `aria-label="Ask my AI Assistant"` and `aria-expanded={open}`
    - `AIChat.tsx` contains a `CANNED_RESPONSES` constant with at least the three quick-prompt keys
    - `src/app/page.tsx` imports `IDEShell` from `@/components/ide/IDEShell` and renders `<IDEShell />`
    - `src/app/page.tsx` does NOT have `"use client"` (remains a Server Component)
    - None of the CSS Modules (`IDEShell|TopBar|Sidebar|AIChat`.module.css) contain raw `#hex` literals when comments are stripped (color must come from `var(--*)`)
    - `grep -r 'dangerouslySetInnerHTML' src/components/ide/` returns nothing
    - `grep -r '@apply' src/components/ide/` returns nothing
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    `pnpm dev` serves a page that visually shows: topbar with Ruslan branding + status badges + Run button, sidebar with the activity bar + explorer + tools/ folder containing test-automator + about/ folder. The AI FAB sits bottom-right and the panel opens. Run button + ⌘↵ both trigger a callback (the terminal log animation lands when Task 2 ships). EditorArea + Terminal + StatusBar slots in IDEShell render whatever child components Task 2 provides; until Task 2 ships in this same plan, executor can leave temporary empty `<div className={styles.main} />` placeholders for them inside IDEShell.tsx.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Build EditorArea + FileView + Terminal + StatusBar (with TestAutomatorPane dispatch)</name>
  <files>
    src/components/ide/EditorArea.tsx
    src/components/ide/EditorArea.module.css
    src/components/ide/FileView.tsx
    src/components/ide/FileView.module.css
    src/components/ide/Terminal.tsx
    src/components/ide/Terminal.module.css
    src/components/ide/StatusBar.tsx
    src/components/ide/StatusBar.module.css
  </files>
  <read_first>
    .planning/design/design_handoff_ide_portfolio/workspace.jsx                 # Editor (lines 4–41), Terminal + LogRow (lines 113–168), StatusStrip (lines 171–185) — port verbatim
    .planning/design/design_handoff_ide_portfolio/styles.css                    # .editor-wrap, .tabbar, .tab, .tab.active, .tab-close, .breadcrumb, .editor, .gutter, .ln, .code, .minimap, .terminal, .term-resizer, .term-tabs, .term-body, .log, .log-info, .log-warn, .log-ok, .log-pass, .log-fail, .term-cursor, .statusbar, .sb-l, .sb-r, .sb-item
    .planning/phases/01-foundations-test-automator-live/01-PATTERNS.md          # EditorArea + FileView + Terminal + StatusBar sections — exact port + Lucide mapping
    .planning/phases/01-foundations-test-automator-live/01-UI-SPEC.md           # Component Inventory items 3 (Editor) + 4 (Terminal) + 5 (StatusBar); A11y Requirements (terminal aria-live, tab-close aria-label, hire CTA always visible)
    .planning/phases/01-foundations-test-automator-live/01-CONTEXT.md           # D-14 (terminal smoke test), D-15 (Available for hire always visible)
    src/types/ide.ts                                                             # FileEntry, LogEntry, TerminalTab
    src/lib/utils.ts                                                             # cn() helper
    src/components/ide/IDEShell.tsx                                              # Confirm prop shapes match what Task 1 passes to each child
  </read_first>
  <action>
**1. `src/components/ide/EditorArea.tsx`** — `"use client";`. Port of workspace.jsx Editor (lines 4–41). Props per PATTERNS.md.
   - Imports: `FILES, TOOLS from '@/lib/files-data'`, `FileView from './FileView'`, `TestAutomatorPane from '@/components/tools/TestAutomatorPane'`. If plan 06 has not yet shipped, create a temporary one-liner stub at `src/components/tools/TestAutomatorPane.tsx`:
     ```tsx
     "use client";
     export function TestAutomatorPane() { return <div style={{ padding: 24 }}>Test Automator (lands in plan 06)</div>; }
     ```
     Plan 06 will replace it.
   - Content dispatch (per PATTERNS.md):
     ```tsx
     const isTool = activeFile in TOOLS;
     const file = !isTool ? FILES[activeFile] : null;
     ```
   - Tab bar (per UI-SPEC item 3 + PATTERNS.md):
     - Each tab is clickable `<div role="tab" aria-selected={active} tabIndex={0}>`
     - Active tab: 2px top green border (via styles class), `var(--bg-elevated)` bg, `var(--text)` color
     - Inactive tab: `var(--bg-deepest)` bg, `var(--text-muted)` color, right separator `var(--border-soft)`
     - Tab close button (`<button aria-label={\`Close ${name}\`}>`) — 16×16, lucide `X` size 10, visible on hover/active only (CSS)
     - Tab icon: file icon from PATTERNS.md mapping; tool tab uses `Sparkles`
   - Breadcrumb (per UI-SPEC): `~28px` height, mono 11px `var(--text-faint)`, format `ruslankanat › {folder?} › {filename}`. For tool tabs: `ruslankanat › tools › test-automator`.
   - Content area: render `<FileView file={file} />` when not a tool; `<TestAutomatorPane />` when `isTool`; render an empty state with copy `"No file open."` when both are null (empty `tabs` case).

**2. `src/components/ide/EditorArea.module.css`** — port `.editor-wrap`, `.tabbar`, `.tab`, `.tab.active`, `.tab-close`, `.tab-spacer`, `.breadcrumb`. Tab bar height 36px, breadcrumb ~28px.

**3. `src/components/ide/FileView.tsx`** — `"use client";`. Port of workspace.jsx lines 30–40.
   - Props: `{ file: FileEntry }`
   - Imports: `tokenize from '@/lib/syntax-highlighter'`
   - Render: gutter + `<pre className={cn(styles.code, styles['lang-' + file.lang])}><code>{tokens}</code></pre>` + minimap
   - Tokens: `const tokens = tokenize(file.content, file.lang);` — `ReactNode[]` returned by the ported tokenizer; spread inside `<code>`
   - Gutter: line numbers via `file.content.split("\n").map((_, i) => <div className={cn(styles.ln, { [styles.active]: i === 0 })} key={i}>{i + 1}</div>)`
   - Minimap: 70px wide decorative — render ~140 small randomly-colored bars per design handoff README ("Performance: the minimap is purely decorative — don't compute it from real document content; just render ~140 random-colored bars")
   - `aria-hidden="true"` on minimap (decorative)
   - **CRITICAL — T-1-02:** Never use `dangerouslySetInnerHTML`. The tokenizer returns React elements (span nodes), which renders safely as children of `<code>`.

**4. `src/components/ide/FileView.module.css`** — port `.editor` (inner flex), `.gutter`, `.ln`, `.ln.active`, `.code`, `.minimap`. Code is mono 13px, 1.65 line-height; gutter mono 12px `var(--text-faint)`. Pad code 16px y, 24px x per UI-SPEC. Long lines: horizontal scroll, `white-space: pre`, no wrap.

**5. `src/components/ide/Terminal.tsx`** — `"use client";`. Port of workspace.jsx lines 113–168 (Terminal + LogRow).
   - Props per PATTERNS.md: `{ logs, running, height, setHeight, tab, setTab }`
   - Drag-to-resize: handler per PATTERNS.md; min 120, max 500 (UI-SPEC values)
   - Auto-scroll: `useRef<HTMLDivElement>` + `useEffect([logs])` setting `scrollTop = scrollHeight`
   - Tab strip (per UI-SPEC item 4): `TERMINAL` (default active), `PROBLEMS` + numeric badge, `OUTPUT`, `DEBUG CONSOLE` + pulsing dot. Right meta: `playwright` (mono 10.5px `var(--text-faint)`).
   - Term body: `<div ref={termBodyRef} aria-live="polite" aria-label="Test output log">` — `aria-live="polite"` is REQUIRED per UI-SPEC accessibility row "Terminal log region"
   - Log row variants per PATTERNS.md (the LogRow function): info, warn, ok, pass, fail. PASS tag glows in `var(--green)`; FAIL in `var(--red)`.
   - Cursor prompt when `running`: `ruslan@portfolio ❯ ▌` (NOT `alex@portfolio` — UI-SPEC Copywriting Contract). The `▌` block uses the `blink` animation from globals.css.
   - Resizer: 6px hit area at top edge with hover showing green gradient

**6. `src/components/ide/Terminal.module.css`** — port `.terminal`, `.term-resizer`, `.term-tabs`, `.term-tab`, `.term-tab.active`, `.term-tab-badge`, `.term-meta`, `.term-body`, `.log`, `.log-info`, `.log-warn`, `.log-ok`, `.log-pass`, `.log-fail`, `.log-tag`, `.log-tag.pass`, `.log-tag.fail`, `.log-test`, `.log-detail`, `.term-cursor`, `.term-prompt`, `.term-prompt-sep`, `.term-prompt-host`, `.term-prompt-arrow`, `.blink`.

**7. `src/components/ide/StatusBar.tsx`** — `"use client";` (reads `activeFile` prop and derives values; useful to keep boundary explicit). Port of workspace.jsx lines 171–185.
   - Props: `{ activeFile: string; running: boolean }`
   - Render: 26px footer
     - Left: `<GitBranch size={11} /> main` (in `styles.green` color), `running ? "● running" : "✓ ready"`
     - Right (only when file is open): `Ln 1, Col 1`, `{LANG}` (green-bright, weight 500 — note: this is the ONE 500-weight exception declared in UI-SPEC Typography override), `{N} lines`
     - **D-15 requirement:** ALWAYS RENDER `<div className={cn(styles.sbItem, styles.hireCta)}>● Available for hire</div>` at the rightmost position. This must NEVER be conditional, never hidden at any breakpoint, never truncated. Color: `var(--green)`.

**8. `src/components/ide/StatusBar.module.css`** — port `.statusbar`, `.sb-l`, `.sb-r`, `.sb-item`, `.sb-item.green`, `.sb-item.lang`, `.sb-item.hireCta`. Statusbar height 26px, `var(--bg-deep)` bg, `var(--border)` top edge, mono 11px `var(--text-muted)`. `.hireCta` color `var(--green)`, weight unchanged (mono 11px).

**Universal rules:** same as Task 1 (use client, Lucide imports, cn(), no any, no @apply, no raw hex in CSS Modules, no dangerouslySetInnerHTML).

DO NOT in this task: implement the actual TestAutomatorPane functionality (plan 06); add the URL state update for `/tools/test-automator` (plan 06 task 2); skip the `aria-live="polite"` on the terminal body; skip the `● Available for hire` always-visible rule.
  </action>
  <verify>
    <automated>grep -c '"use client"' src/components/ide/EditorArea.tsx && grep -c 'TestAutomatorPane' src/components/ide/EditorArea.tsx && grep -c 'role="tab"' src/components/ide/EditorArea.tsx && grep -c 'aria-label={`Close' src/components/ide/EditorArea.tsx && grep -c 'tokenize' src/components/ide/FileView.tsx && grep -c 'aria-hidden="true"' src/components/ide/FileView.tsx && ! grep 'dangerouslySetInnerHTML' src/components/ide/FileView.tsx && grep -c 'aria-live="polite"' src/components/ide/Terminal.tsx && grep -c 'ruslan@portfolio' src/components/ide/Terminal.tsx && grep -c 'Available for hire' src/components/ide/StatusBar.tsx && grep -c 'GitBranch' src/components/ide/StatusBar.tsx && (grep -v '^\s*\(//\|\*\|#\)' src/components/ide/EditorArea.module.css src/components/ide/FileView.module.css src/components/ide/Terminal.module.css src/components/ide/StatusBar.module.css | grep -cE '#[0-9a-fA-F]{3,8}' | grep -q '^0$') && ! grep -r 'dangerouslySetInnerHTML' src/components/ide/ 2>/dev/null && ! grep -r '@apply' src/components/ide/ 2>/dev/null && pnpm typecheck && pnpm build</automated>
  </verify>
  <acceptance_criteria>
    - `src/components/ide/EditorArea.tsx` starts with `"use client";`, imports `TestAutomatorPane`, imports `FILES, TOOLS`
    - `EditorArea.tsx` renders tab elements with `role="tab"` and `aria-selected`
    - `EditorArea.tsx` close button has `aria-label={\`Close ${name}\`}` (per UI-SPEC)
    - `src/components/ide/FileView.tsx` calls `tokenize(file.content, file.lang)` (from plan 04)
    - `FileView.tsx` minimap has `aria-hidden="true"`
    - `FileView.tsx` does NOT contain `dangerouslySetInnerHTML`
    - `src/components/ide/Terminal.tsx` term body has `aria-live="polite"` (required)
    - `Terminal.tsx` shows the prompt `ruslan@portfolio` (NOT `alex@portfolio`)
    - `Terminal.tsx` uses `setTimeout` jitter pattern when `running` (auto-scroll on logs)
    - `src/components/ide/StatusBar.tsx` ALWAYS renders the string `Available for hire` (it is NOT inside any conditional render)
    - `StatusBar.tsx` uses `GitBranch` from lucide for the main branch indicator
    - None of the 4 new CSS Modules contain raw `#hex` literals when comments stripped
    - `grep -r 'dangerouslySetInnerHTML' src/components/ide/` empty (entire IDE chrome)
    - `grep -r '@apply' src/components/ide/` empty (entire IDE chrome)
    - `pnpm typecheck` exits 0
    - `pnpm build` exits 0 (full Next.js build must succeed — confirms no client/server import boundary violation)
  </acceptance_criteria>
  <done>
    Visiting `/` after `pnpm dev` shows the complete IDE: topbar, sidebar with Ruslan's file tree, editor with README.md content rendered as syntax-highlighted markdown via the ported tokenizer, terminal that animates Ruslan-specific test logs on Run Smoke Test, statusbar with `● Available for hire` always visible on the right. Clicking `test-automator` in the sidebar replaces the file view with the TestAutomatorPane (stub or real depending on whether plan 06 has shipped). The full Walking Skeleton chrome is now visible — only the interactive Test Automator and its server route remain (plans 05–06).
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Human verification — IDE chrome loads, renders correctly, interactions work</name>
  <what-built>
    Tasks 1 and 2 produced the full IDE chrome: TopBar (logo, badges, Run Smoke Test), Sidebar (activity bar + tools/+about/ tree), EditorArea (tabs, breadcrumb, file view + tool dispatch), FileView (syntax-highlighted markdown with line numbers), Terminal (resizable, fake smoke-test animation, Ruslan-specific log lines), StatusBar (with always-visible `● Available for hire`), and AIChat (FAB + glassmorphic panel with canned responses). `pnpm dev` should now show a complete, navigable IDE.
  </what-built>
  <how-to-verify>
    Run `pnpm dev` and open `http://localhost:3000` in a browser. Verify each of the following in order:

    **Visual (chrome correctness):**
    1. Topbar shows `ruslan.kanatbek` logo + `SDET · AI Automation` sub-label, green-glow run button labeled `Run Smoke Test ⌘↵`, theme toggle icon, clock with timezone
    2. Sidebar left edge shows the 48px activity bar with 5 top icons (Explorer active with green glow bar) + 2 bottom icons
    3. Sidebar right column shows `EXPLORER` header + `ruslankanat/portfolio` meta + a `tools/` folder (open by default, containing a green-sparkle `test-automator` row) + an `about/` folder (collapsed, click chevron to expand and see `bio.json`, `experience.yaml`, `skills.toml`) + root files `README.md` (highlighted as active) + `contact.json`
    4. Editor tab bar shows a single tab `README.md` with green top border
    5. Breadcrumb shows `ruslankanat › README.md`
    6. Editor body shows README.md content with syntax highlighting (markdown headings in `var(--green-bright)`, code spans in string-yellow) + gutter line numbers
    7. Terminal at bottom (220px tall by default) — has `TERMINAL` tab active, shows 8 pre-loaded Ruslan-flavored log lines
    8. Statusbar at bottom — left shows `⎇ main` and `✓ ready`; right shows `Ln 1, Col 1`, `MARKDOWN`, `{N} lines`, and the FAR-RIGHT `● Available for hire` in green
    9. Bottom-right corner: 52×52 circular green-glow FAB with a sparkle icon

    **Interactions:**
    10. Click `Run Smoke Test` — terminal clears, then streams the full Ruslan-specific SAMPLE_LOGS list (test_resmed_sdet, test_playwright_expertise, test_ai_toolkit, etc.) with visible jitter; Run button pulses; statusbar left flips to `● running` then back to `✓ ready`
    11. Press `⌘+Enter` (Mac) or `Ctrl+Enter` — same animation triggers
    12. Click `test-automator` in sidebar — editor area replaces FileView with the Test Automator pane (stub or real). A new tab appears in the tab bar.
    13. Click the `×` on the README.md tab — README.md tab closes; test-automator tab becomes active (the falls-back-to-last-remaining-tab rule)
    14. Click the AI FAB — panel opens with `panel-in` animation, shows the greeting, three quick-reply chips
    15. Click `Walk me through your eval pipeline` — user bubble appears, after ~700ms an AI bubble appears with the canned response about Braintrust evals
    16. Click theme toggle — `<html data-theme>` flips between `dark` and `light`; reload preserves the choice (localStorage)
    17. Drag the top edge of the terminal — terminal height adjusts within 120-500px bounds
    18. Resize browser to ≤920px — clock disappears, logo text hides, branch indicator hides (per UI-SPEC responsive breakpoints)

    **Accessibility (quick keyboard probe):**
    19. Tab through the page — focus rings visible on every interactive element (per UI-SPEC focus styles)
    20. Activity bar buttons reveal tooltips on hover (`title` attribute)
    21. Use a screen reader briefly (VoiceOver / NVDA) on the terminal log area — new log lines are announced (aria-live="polite")

    Reply `approved` if all 21 checks pass, or describe specific failures.
  </how-to-verify>
  <resume-signal>Type `approved` (or describe issues encountered)</resume-signal>
</task>

</tasks>

<verification>
- `pnpm dev` serves `/` with all IDE chrome visible
- `pnpm build` succeeds (proves no client/server import violation)
- DevTools shows no React hydration warnings on first load
- `view-source:http://localhost:3000` shows `data-theme="dark"` server-rendered (no FOUC)
- Lighthouse Quick Audit (manual) reports no critical accessibility violations on the chrome (full WCAG AA verification is Phase 5 scope)
</verification>

<success_criteria>
- FOUND-05: Base IDE chrome (the Phase 1 equivalent of "placeholder landing page" per D-01/D-02) deployed to public Vercel URL
- FRAME-02: Shared tool layout is the IDE editor pane — every future tool renders in the same chrome
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-test-automator-live/01-03-SUMMARY.md` recording: screenshots (or descriptions) of the dark-theme IDE shell at standard desktop width + at 920px breakpoint; confirmation that the smoke test animation runs end-to-end; confirmation that all 7 activity bar buttons have `aria-label` + `title`; localStorage theme persistence verified.
</output>
