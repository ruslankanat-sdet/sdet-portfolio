---
phase: 1
slug: foundations-test-automator-live
status: draft
shadcn_initialized: false
preset: none
created: 2026-05-16
---

# Phase 1 — UI Design Contract

> Visual and interaction contract for Phase 1: Foundations + Test Automator Live.
> Generated by gsd-ui-researcher. Source of truth: `.planning/design/design_handoff_ide_portfolio/styles.css`.
> All token values are ported exactly from the design handoff. Do not invent new values.

---

## Design System

| Property | Value |
|----------|-------|
| Tool | shadcn (optional supplement — see note) |
| Preset | not applicable |
| Component library | Radix UI (via shadcn primitives where used) |
| Icon library | lucide-react (swap the inline SVGs from the design handoff JSX to Lucide equivalents) |
| Font | Inter (UI), JetBrains Mono (code/mono) — both from Google Fonts |

**Note on shadcn:** The IDE shell's visual identity is driven by CSS custom properties in `styles.css`, not a shadcn preset. The canonical approach is to port `styles.css` into `src/app/globals.css` as CSS custom properties and use CSS Modules for per-component styles. shadcn components (`Button`, `Dialog`, `Tooltip`) may be added for the Test Automator input form and output area where they accelerate development, but they must be restyled to match the dark IDE token set — do not use shadcn defaults. `components.json` is not initialized because the IDE chrome is not Tailwind-first.

**Tailwind decision:** Tailwind v4 may be used for utility classes on non-IDE-chrome surfaces (e.g., the Test Automator tool pane rendered inside the editor area). The IDE shell layout CSS must remain CSS Modules / global CSS to match the design handoff exactly. Do not apply Tailwind utilities to `.topbar`, `.sidebar`, `.terminal`, `.statusbar` — those use the custom property system.

---

## Spacing Scale

The design handoff uses ad-hoc spacing, not an 8-point grid. The values below are the actually-used measurements extracted from `styles.css`. Implement with CSS custom properties or inline values — do not attempt to map to Tailwind spacing tokens for IDE chrome components.

| Token | Value | Usage |
|-------|-------|-------|
| space-1 | 4px | Tree-row padding, badge internal gaps, outline-row padding |
| space-2 | 6px | Tree padding, crumb gap, term-resizer extension, badge padding-y |
| space-3 | 8px | Tab gap, ai-msg gap, term-cursor gap |
| space-4 | 10px | Branch padding-x, clock padding-x, act-btn gap, term-body padding-y |
| space-5 | 12px | Tab padding-x, term-body padding-x, run-btn padding-y |
| space-6 | 14px | Logo gap, tab-close size container, ai-header padding |
| space-7 | 16px | Code padding-y, gutter padding, statusbar padding-x |
| space-8 | 18px | Topbar gap |
| space-9 | 22px | AI widget right offset |
| space-10 | 24px | Code padding-x, ai-body padding-x |

Exceptions:
- `badge` pill: `6px 11px 6px 9px` (asymmetric for visual balance with leading dot)
- `.topbar` height: 56px (fixed)
- `.sidebar` width: 232px (fixed)
- `.activity-bar` width: 48px (fixed)
- `.tabbar` height: 36px (fixed)
- `.breadcrumb` height: ~28px (6px padding × 2 + content)
- `.terminal` default height: 220px, min 120px, max 500px (resizable)
- `.statusbar` height: 26px (fixed)
- `.run-btn` padding: `9px 14px 9px 12px`
- Touch targets: icon buttons (`icon-btn`, `act-btn`) are 34×36px — below 44px WCAG target; compensate with 8px invisible hit-area padding via `::after` pseudo-element on interactive icon buttons
- AI FAB: 52×52px (meets WCAG touch target)

### Design System Override — IDE Chrome Exception (Spacing)

**Checker rule overridden:** "All spacing values must be multiples of 4px (4-point grid)."

**Why this override applies:** All spacing values for the IDE chrome are ported exactly from the design handoff `styles.css` and are intentionally not on a 4-point grid. The design asset is a high-fidelity IDE chrome with optical spacing decisions baked in. Changing these values to the nearest 4px multiple would produce a visible mismatch from the design handoff, defeating the purpose of a pixel-perfect port.

**Design-handoff exceptions — each value is locked in the design asset:**

| Value | CSS context | Classification |
|-------|-------------|---------------|
| 6px | Sidebar tree row gap; breadcrumb padding-y; term-resizer extension; badge padding-y | design-handoff exception |
| 9px | `.run-btn` padding-top and padding-bottom (`9px 14px 9px 12px`) | design-handoff exception |
| 10px | Branch padding-x; clock padding-x; act-btn gap; term-body padding-y | design-handoff exception |
| 11px | Badge pill right padding (`6px 11px 6px 9px`) | design-handoff exception |
| 12px | Tab padding-x; term-body padding-x; run-btn padding-y | design-handoff exception |
| 14px | Logo gap; tab-close size container; ai-header padding; run-btn padding-right | design-handoff exception |
| 18px | Topbar gap | design-handoff exception |
| 22px | AI widget right offset | design-handoff exception |
| 26px | `.statusbar` height (fixed, per design spec) | design-handoff exception |
| 34px | Activity bar icon button width (`.act-btn` — 34×36px per design spec) | design-handoff exception |

**Rationale:** IDE-chrome pixel-perfect fidelity takes precedence over the 4-point grid constraint. These values are locked in the design asset and changing them would produce a visible mismatch from the design handoff. The 4-point grid rule applies to surfaces built from scratch (e.g., Tailwind-based tool panes). It does not govern values ported verbatim from a pre-existing high-fidelity design file.

---

## Typography

Two font families. All sizes from `styles.css`. No size outside this list should be introduced.

### Font Stacks

```css
--mono: "JetBrains Mono", "Fira Code", "SF Mono", ui-monospace, Menlo, Consolas, monospace;
--ui:   "Inter", -apple-system, BlinkMacSystemFont, "Segoe UI", system-ui, sans-serif;
```

### Type Ramp

| Role | Family | Size | Weight | Line Height | Usage |
|------|--------|------|--------|-------------|-------|
| Body / base | `--ui` | 13px | 400 | 1.5 | Global base, explorer items |
| Code / editor | `--mono` | 13px | 400 | 1.65 | `.code`, logo-name |
| Code small | `--mono` | 12px | 400 | 1.65 | `.gutter`, `.tab`, `.tree-label`, `.branch`, `.clock` |
| Code xs | `--mono` | 11.5px | 400 | 1.4 | `.outline-row`, `.badge`, `.ai-quick-btn` |
| Meta / faint | `--mono` | 10.5px | 400 | 1.4 | `.logo-sub`, `.branch-meta`, `.clock-tz`, `.term-meta`, `.explorer-header` |
| Sub-label | `--mono` | 10px | 400 | 1.4 | `.tree-badge`, `.term-tab-badge`, `.explorer-meta`, `.ai-foot` |
| Statusbar | `--mono` | 11px | 400 | 26px (fixed height) | `.statusbar` |
| Run button | `--ui` | 12.5px | 600 | — | `.run-btn` |
| AI name | `--ui` | 13.5px | 600 | — | `.ai-name` |
| AI bubble | `--ui` | 12.5px | 400 | 1.5 | `.ai-bubble` |
| Heading h1 (README.md) | `--mono` | 13px | 600 | 1.65 | `.tk-md-h` (green-bright, bold) |

**Weight constraint:** Only 400 (regular) and 600 (semibold) are used. Inter 500 appears on `.badge-value` only. Do not introduce 700 or 300.

### Design System Override — IDE Chrome Exception (Typography)

**Checker rules overridden:**
- "Maximum 4 distinct font sizes" — this phase declares 8 distinct sizes (10px, 10.5px, 11px, 11.5px, 12px, 12.5px, 13px, 13.5px).
- "Maximum 2 font weights" — this phase uses weight 500 on `.badge-value` in addition to the declared 400 and 600.

**Why this override applies:** This phase faithfully ports a high-fidelity IDE chrome from `styles.css`, where precise optical sizing across the UI hierarchy is required by the design handoff. The IDE chrome has many distinct functional zones (statusbar, breadcrumb, gutter, tree labels, terminal output, AI widget, logo, run button) each requiring a slightly different optical size to match VS Code–grade visual fidelity. The sizes are not arbitrary additions — they are exact values extracted from the design asset.

**Font sizes — all values are design-handoff-exact:**

| Size | Source in `styles.css` | Classification |
|------|------------------------|---------------|
| 10px | `.tree-badge`, `.term-tab-badge`, `.explorer-meta`, `.ai-foot` | design-handoff exact |
| 10.5px | `.logo-sub`, `.branch-meta`, `.clock-tz`, `.term-meta`, `.explorer-header` | design-handoff exact |
| 11px | `.statusbar` (line-height locked to 26px fixed height) | design-handoff exact |
| 11.5px | `.outline-row`, `.badge`, `.ai-quick-btn` | design-handoff exact |
| 12px | `.gutter`, `.tab`, `.tree-label`, `.branch`, `.clock` | design-handoff exact |
| 12.5px | `.run-btn`, `.ai-bubble` | design-handoff exact |
| 13px | Body base, `.code`, logo-name, `.tk-md-h` | design-handoff exact |
| 13.5px | `.ai-name` | design-handoff exact |

**Inter 500 weight on `.badge-value`:** This is a one-off design-handoff exception, not a declared "third weight" in the system. The system uses 400 and 600. The single 500-weight instance on `.badge-value` is locked to that specific element per the design asset and must not be applied elsewhere. It is listed here for implementation accuracy, not as a systemic addition to the weight scale.

**Rationale:** IDE-chrome pixel-fidelity from a pre-existing design asset takes precedence over the "max 4 sizes, max 2 weights" checker rule. These values are locked in `styles.css` and changing them would produce a visible optical mismatch. The size ramp rule applies to surfaces built from scratch. It does not govern values ported verbatim from a finalized high-fidelity design file.

---

## Color

### Dark Theme (Default) — Source: `styles.css :root`

All production surfaces use dark theme. Light theme CSS variables exist in the handoff and should be ported, but light theme polish is deferred per D-deferred.

#### Surface Hierarchy (60/30/10)

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Dominant (60%) | `--bg-deepest` | `#06090e` | `html/body`, `#root` gradient base, activity bar, tabbar background |
| Dominant variant | `--bg-deep` | `#0b1117` | Topbar, sidebar, main area, statusbar |
| Secondary (30%) | `--bg-panel` | `#0f1620` | Terminal background |
| Secondary variant | `--bg-elevated` | `#141c28` | Editor content area, active tab background |
| Secondary variant | `--bg-hover` | `#1a2433` | Hover state backgrounds |
| Secondary variant | `--bg-tab` | `#0b1117` | Inactive tab background |
| Secondary variant | `--bg-tab-active` | `#141c28` | Active tab background |

#### Text

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Primary text | `--text` | `#e6edf3` | Editor content, active states, headings |
| Muted text | `--text-muted` | `#8b96a8` | Explorer, inactive tabs, statusbar items |
| Faint text | `--text-faint` | `#5a6678` | Line numbers, breadcrumb separators, tree chevrons, comments |

#### Borders

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Default border | `--border` | `#1f2937` | All dividers between major regions |
| Soft border | `--border-soft` | `#182230` | Intra-panel dividers, tab separators |
| Strong border | `--border-strong` | `#2a3849` | Hover state border reinforcement |

#### Accent (10%) — Green

**Reserved for:** active state indicators, primary CTA button, available-for-hire status, CI passing badge, cursor blink, active tab top-border, active activity-bar indicator, terminal PASS tags, AI widget FAB border/glow, "Try with example" button, copy-to-clipboard confirmation.

| Role | Token | Value | Usage |
|------|-------|-------|-------|
| Brand green | `--green` | `#3ddc84` | Active indicators, borders, dot fills |
| Bright green | `--green-bright` | `#4dff95` | Run button text, PASS log text, active value text |
| Green glow | `--green-glow` | `rgba(61,220,132,0.55)` | Box-shadow glow on green elements |
| Green soft | `--green-soft` | `rgba(61,220,132,0.12)` | Logo-mark background, tab gradient, topbar underline gradient |

#### Semantic Colors

| Role | Token | Value | Reserved For |
|------|-------|-------|-------------|
| Destructive / fail | `--red` | `#ff6b6b` | FAIL log tags, error states in tool output, rate-limit error messages |
| Warning | `--amber` | `#ffb547` | WARN log lines, oversized-input warnings |
| Info / link | `--blue` | `#79b8ff` | Syntax token `.tk-key`, CI coverage badge, keyboard shortcut hints |
| Keyword | `--pink` | `#ff7eb6` | Syntax token `.tk-kw` (keywords, italic) |

#### Syntax Tokens (editor content only)

| Token | CSS var | Value |
|-------|---------|-------|
| Keys / identifiers | `--syn-key` | `#79b8ff` |
| Strings | `--syn-str` | `#c9d178` |
| Numbers | `--syn-num` | `#ffab70` |
| Keywords | `--syn-kw` | `#ff7eb6` |
| Comments | `--syn-cmt` | `#5a6678` |
| Punctuation | `--syn-punct` | `#8b96a8` |
| Functions | `--syn-fn` | `#d2a8ff` |
| Types | `--syn-type` | `#ffab70` |
| Decorators | `--syn-dec` | `#f97583` |

#### Background Gradients

```css
/* Root background — port exactly */
background:
  radial-gradient(ellipse 1200px 600px at 80% -20%, rgba(61,220,132,0.06), transparent 60%),
  radial-gradient(ellipse 800px 500px at -10% 110%, rgba(121,184,255,0.04), transparent 60%),
  var(--bg-deepest);
```

---

## Component Inventory

### IDE Chrome Components

These must be implemented from the design handoff JSX + CSS, not from shadcn defaults.

#### 1. `<TopBar>`

- Height: 56px, `--bg-deep` background, `--border` bottom edge, green gradient underline pseudo-element
- Left: logo-mark (34×34, 8px radius, `--green-soft` bg) + logo-name (`ruslan.kanatbek`, JetBrains Mono 13px 600) + logo-sub (`SDET · AI Automation`, JetBrains Mono 10px, `--text-faint`, letter-spacing 0.04em)
- Divider: 1×24px `--border`
- Branch badge: git icon + `main` + `· up to date`, JetBrains Mono 12px, `--text-muted`
- Status rail (centered, flex): CI badge (tone-green, pulsing dot, `CI · Passing`), Coverage badge (tone-green, `Coverage · 98%`, hides at ≤1200px), Tests badge (tone-blue, `Tests · 312`, hides at ≤1380px)
- Clock: `HH:MM` + timezone `UTC+5`, JetBrains Mono 12px — hides at ≤920px
- Theme toggle: 34×34 icon-btn (sun/moon lucide icon)
- Run button: `Run Smoke Test` + `⌘↵` shortcut badge, green gradient, 12.5px Inter 600, pulses (`.running` animation) while terminal is animating

#### 2. `<Sidebar>`

Two-column layout: activity bar (48px) + explorer panel (184px = 232px − 48px)

**Activity bar:**
- 5 icon buttons top, 2 bottom (Settings, Profile)
- Active button: white icon + 2px left green glow bar (`position: absolute; left: -10px`)
- Explorer is active by default
- Each `.act-btn` must include a `title` attribute for hover tooltip (sighted keyboard users): `title="Explorer"`, `title="Search"`, `title="Source Control"`, `title="Run & Debug"`, `title="Extensions"` (top 5); `title="Profile"`, `title="Settings"` (bottom 2)
- `title` supplements `aria-label` — both are required; they should carry the same text value

**Explorer panel:**
- Header: `EXPLORER` (10.5px, 600, letter-spacing 0.08em) + repo label `ruslankanat/portfolio` (10px, mono, `--text-faint`)
- File tree — two root folders:
  - `tools/` — expanded by default, contains: `test-automator` (tool entry, not a file)
  - `about/` — collapsed by default, contains: `bio.json`, `experience.yaml`, `skills.toml`
  - Root-level files: `README.md`, `contact.json`
- Active file row: green gradient left bg + 2px inset-left `--green` border
- Outline panel at bottom: symbols in current file

**File tree structure (Phase 1):**
```
📁 tools/             ← expanded
   ⚡ test-automator  ← opens Test Automator pane in editor
📁 about/             ← collapsed
   {} bio.json
   YM experience.yaml
   TM skills.toml
📄 README.md          ← active/open on first visit
📄 contact.json
```

#### 3. `<Editor>` / `<EditorWrap>`

Editor wrap is a flex-column that contains the tab bar, breadcrumb, and the editor content area.

**Tab bar:**
- 36px height, `--bg-deepest` bg, horizontal scroll (no scrollbar)
- Active tab: `--bg-elevated` bg, 2px top `--green` border
- Inactive tab: `--bg-deepest` bg, `--border-soft` right separator
- Close button (×): 16×16, visible on hover/active only

**Breadcrumb:**
- ~28px height, `--bg-elevated` bg, mono 11px `--text-faint`
- Path: `ruslankanat › {folder} › {filename}`, segments separated by `›`

**Editor content area:**
The editor area renders one of two things based on `activeFile`:
1. **File view** — gutter (line numbers, mono 12px, `--text-faint`) + code area (mono 13px, 1.65 line-height, syntax-highlighted via ported tokenizer) + minimap (70px decorative)
2. **Tool view** — Test Automator UI (see below), rendered when `activeFile === "test-automator"`

#### 4. `<Terminal>`

- `--bg-deepest` background, `--border` top edge
- Resizable via drag handle (6px hit area at top edge, hover shows green gradient)
- Default height: 220px. Min: 120px. Max: 500px.
- Tab strip (32px, `--bg-deep`): TERMINAL (active, `--green` underline), PROBLEMS + numeric badge, OUTPUT, DEBUG CONSOLE + pulsing dot
- Right of tab strip: `playwright` meta label, mono 10.5px, `--text-faint`
- Terminal body: mono 12px, 1.65 line-height, logs animate in with `log-in` keyframe
- Log line variants: `.log-info` (`--text-muted`), `.log-warn` (`--amber`), `.log-ok` (`--green-bright`), `.log-pass` (`[PASS]` in `--green`, glowing), `.log-fail` (`[FAIL]` in `--red`, glowing)
- Cursor prompt: `ruslan@portfolio ❯ ▌` (blinking block) — visible while `running === true`

**Phase 1 smoke test log content (replace Alex Morgan placeholders):**
```
$ npx playwright test --grep smoke --reporter=line
Running 12 tests using 6 workers
[PASS] test_resmed_sdet.py::test_device_monitoring_flow       142ms
[PASS] test_resmed_sdet.py::test_data_integrity_pipeline      318ms
[PASS] test_playwright_expertise.py::test_cross_browser_e2e   412ms
[PASS] test_playwright_expertise.py::test_visual_regression   624ms
[PASS] test_api_coverage.py::test_health_endpoints_200        41ms
[PASS] test_ai_toolkit.py::test_automator_streaming           891ms
[WARN] ⚠ Self-healing selector rewrote `#submit` → `[data-testid=submit]` (auto-PR #4821)
[PASS] test_ai_toolkit.py::test_rate_limiting_429_returned    156ms
[PASS] test_perf.py::test_lcp_under_2s                        742ms  budget 800ms
[PASS] test_a11y.py::test_wcag_aa_contrast                    0 violations
─────────────────────────────────────────────────────────────
✓ 10 passed (6.2s)  ·  0 failed  ·  0 flaky
```

#### 5. `<StatusBar>`

- 26px height, `--bg-deep` bg, `--border` top edge
- Mono 11px, `--text-muted`
- Left items: `⎇ main` (green icon + text), `✓ ready` / `● running` state
- Right items: `Ln 1, Col 1`, `{LANG}` (green-bright, 500 weight), `{N} lines`
- Rightmost item: `● Available for hire` — color `--green`, always visible, never truncated
- Items: `padding: 0 10px; height: 26px` hover shows `--hover` background

#### 6. `<AIChat>` (FAB + Panel)

- FAB: 52×52, fixed bottom-right (`right: 22px; bottom: 44px`), circular, glassmorphic, green glow border
- Panel: 380×520 (max 80vh), glassmorphic, 14px radius, `panel-in` animation on open
- Panel header: AI avatar (36×36, green gradient circle) + "Ask my AI Assistant" + "online · claude-haiku-4-5" sub
- Quick prompts (first message only): "Walk me through your eval pipeline", "How do you keep flake rate under 1%?", "Are you open to staff roles?"
- AI system prompt: reference Ruslan Kanatbek, 9+ yrs, Resmed/Gemini/Google/Citi, Playwright/Pytest expertise
- Phase 1: stub with canned responses keyed to quick-reply text; real API wiring is deferred

---

## Test Automator Tool UI (rendered inside editor pane)

When `activeFile === "test-automator"`, the editor content area renders the Test Automator instead of a file. The tool UI must feel native to the IDE — use the same color tokens, monospace conventions, and spacing as the surrounding IDE chrome.

### Input Area

**Location:** Top portion of editor content area (replaces the gutter+code+minimap layout)

**Mode toggle pill:**
- Pill group: `URL` | `User Story` — two segments, toggles on click
- Active segment: `--bg-elevated`, `--green` border-bottom or background tint, `--text` color
- Inactive segment: `--bg-hover`, `--text-muted` color
- Pill padding: `6px 16px`, radius: 6px on outer corners only (grouped pill shape)
- Font: Inter 12px 500

**Textarea:**
- Background: `--bg-deepest` (matches code area)
- Border: 1px `--border`, radius 6px
- Focus border: 1px `rgba(61,220,132,0.4)` (`--green` at 40% opacity)
- Font: JetBrains Mono 13px `--text`, 1.65 line-height
- Placeholder: `--text-faint`
- URL mode placeholder: `https://example.com — paste the page you want tested`
- User Story mode placeholder: `Describe the user journey to test in plain English…`
- Min-height: 120px, max-height: 240px (resizes to content up to max)
- Padding: 16px

**Validation error:**
- Inline, below textarea
- Color: `--red`, mono 11.5px
- URL mode: `"Must start with http:// or https://"`
- User Story mode: `"Please describe the user story"`

**"Try with example" button:**
- Below textarea, left-aligned
- Style: ghost/outline — `--surface` bg, `--border` border, `--text-muted` text, 12px Inter
- Hover: `--bg-hover`, `--text`
- Label: `Try with example`
- Behavior: fills textarea with User Story fixture, switches to User Story mode

**Meta-fixture content (User Story mode, per D-11/D-12):**
```
User visits the IDE portfolio, opens the Test Automator tool from
the sidebar, enters a user story, and receives streaming Playwright
and Pytest test code within 30 seconds.
```

**"Generate tests" button (primary submit):**
- Right-aligned below textarea, or full-width below example button
- Style: matches `.run-btn` — green gradient, `--green` border, `--green-bright` text, Inter 12.5px 600
- Label: `Generate Tests`
- Disabled: when textarea empty or URL invalid
- Loading: shows `Generating…` (no spinner — keep it minimal, terminal-style)

**"No persistence" notice:**
- Single line below the form, center or left-aligned
- Color: `--text-faint`, mono 10.5px
- Copy: `Your input is processed in-request and discarded — nothing stored.`

### Output Area

**Location:** Below input area, separated by a 1px `--border-soft` horizontal rule

**Sub-tab bar (language selector):**
- Same visual language as `.tabbar` but smaller — height 32px, `--bg-panel` bg
- Tabs: `▶ playwright.ts` | `▶ test_suite.py`
- Active tab: `--bg-elevated`, 2px top `--green` border, `--text` label
- Inactive tab: `--bg-deepest`, `--text-muted` label
- While generating: inactive tab shows `generating…` placeholder in `--text-faint` italic

**Code block (active tab):**
- Pre/code element: JetBrains Mono 13px, 1.65 line-height, `--bg-elevated` bg, padding 16px 24px
- Line numbers in left gutter: same `.gutter` styles as file viewer
- Syntax highlighting: use Shiki (restricted to `typescript`, `python`) for LLM-generated code output
- Horizontal scroll for long lines; no line wrapping

**Copy and Download buttons (appear per-tab after generation completes):**
- Top-right of the code block area
- Copy: lucide `Copy` icon + `Copy` label, 12px Inter 500, `--text-muted`, hover `--text`
- Download: lucide `Download` icon + `Download` label, same style
- Copy confirmation: replace label with `Copied!` in `--green` for 2000ms, then revert
- Button size: `padding: 5px 10px`, `--surface` bg, `--border` border, radius 5px

**Streaming behavior:**
- First token appears within ~1s — no blank-screen wait
- Code streams character-by-character into the active sub-tab's code block
- While streaming: "generating…" placeholder visible on inactive tab
- Cursor blink at end of streaming content (same `blink` animation as terminal cursor)
- After completion: copy/download buttons appear; rationale block animates in

**Rationale block (below code output):**
- Collapsible via chevron — default expanded
- Header: `# Rationale` — styled as `.tk-md-h` (green-bright, 600, mono 13px)
- Body: plain `--text-muted`, Inter 13px, 1.5 line-height, indented 8px from header
- Content: AI-generated explanation of selectors chosen, edge cases covered, test structure rationale

**Error states:**

| Scenario | Display |
|----------|---------|
| Rate limited (429) | Red banner below input: `Rate limit reached — try again in {N}s. (Retry-After: {N}s)` |
| Model error (5xx) | `Claude returned an error. Try again — your input was not charged.` |
| Timeout (>30s no first token) | `Request timed out. Try a shorter user story or a simpler page URL.` |
| Input too long (413) | `Input exceeds the {N} character limit. Please shorten your description.` |
| Invalid URL | Client-side validation inline (see above) |

Error banner style: `--red` background at 10% opacity, `--red` border, `--red` text, mono 12px, radius 6px, padding 10px 14px

**Loading state (between submit and first token):**
- Replace submit button with `Generating…` (disabled)
- Show single-line terminal-style status in output area: `Analyzing your input…` in `--text-faint`, mono 12px, with blinking cursor

---

## Interactions & Keyboard Shortcuts

| Interaction | Behavior |
|-------------|----------|
| File tree click | Opens file as new tab, sets as active, updates breadcrumb and outline |
| Tab click | Sets active file/tool |
| Tab close (×) | Removes tab; if active, falls back to last remaining tab |
| Run Smoke Test button | Clears terminal, streams `SAMPLE_LOGS` with 280–540ms jitter, sets `running = true`, button pulses |
| `⌘↵` / `Ctrl+↵` (global) | Triggers Run Smoke Test — same as button |
| Terminal resize | Drag top edge of terminal; min 120px, max 500px |
| AI FAB click | Expands panel with `panel-in` animation (220ms cubic-bezier(.2,.9,.3,1.1)) |
| AI quick-reply click | Fills input and sends |
| Theme toggle | Flips `data-theme` on `<html>`, persists to `localStorage["portfolio-theme"]` |
| Test Automator — mode toggle | Switches URL/User Story mode, updates placeholder + validation |
| Test Automator — Generate | Submits to `/api/tools/test-automator`, streams response into active sub-tab |
| Test Automator — Copy | Copies code to clipboard, shows 2s `Copied!` confirmation |
| Test Automator — Download | Triggers file download: `playwright-tests.ts` or `pytest-tests.py` |
| Test Automator — sub-tab click | Switches visible language output |

---

## Animations

All animations are defined in `styles.css` — port exactly.

| Name | Duration | Easing | Trigger |
|------|----------|--------|---------|
| `pulse-dot` | 1.8s | ease-in-out, infinite | Green status dots (CI badge, AI status) |
| `log-in` | 200ms | ease-out | New terminal log line appears |
| `msg-in` | 250ms | ease-out | New AI chat message |
| `panel-in` | 220ms | cubic-bezier(.2,.9,.3,1.1) | AI panel opens |
| `btn-pulse` | 1.2s | ease-in-out, infinite | Run button while `running === true` |
| `typ` | 1.2s | ease-in-out, infinite | AI typing indicator dots |
| `blink` | 1s | steps(2, start), infinite | Terminal cursor + streaming code cursor |

---

## Responsive Breakpoints

These are from `styles.css` and control IDE chrome visibility at narrower viewports.

| Breakpoint | Elements hidden |
|-----------|----------------|
| ≤1380px | `.badge-rail-3` (Tests badge) |
| ≤1200px | `.badge-rail-2` (Coverage badge) |
| ≤1060px | `.badge-rail-1` (remaining status badges), `.clock-tz`, `.branch-meta` |
| ≤920px | `.logo-text`, `.branch` indicator, `.clock` entirely |
| <720px | Mobile — out of scope for Phase 1 (deferred) |

---

## Copywriting Contract

| Element | Copy |
|---------|------|
| Primary CTA (Test Automator) | `Generate Tests` |
| Secondary CTA (example fixture) | `Try with example` |
| Run button (terminal animation) | `Run Smoke Test` |
| Run button (active state) | `Running…` |
| Generate button (active state) | `Generating…` |
| Empty state (no generation yet) | `Enter a URL or user story above to generate Playwright and Pytest tests.` |
| No-persistence notice | `Your input is processed in-request and discarded — nothing stored.` |
| Error: rate limited | `Rate limit reached — try again in {N}s.` |
| Error: model error | `Claude returned an error. Try again — your input was not charged.` |
| Error: timeout | `Request timed out. Try a shorter user story or a simpler page URL.` |
| Error: input too large | `Input exceeds the {N} character limit. Please shorten your description.` |
| Error: invalid URL | `Must start with http:// or https://` |
| Error: empty user story | `Please describe the user story` |
| Copy button confirmation | `Copied!` (2000ms, then reverts to `Copy`) |
| AI chat greeting | `Hi — I'm Ruslan's portfolio assistant. Ask me about test strategy, AI eval pipelines, or why you should hire him.` |
| AI FAB aria-label | `Ask my AI Assistant` |
| AI panel title | `Ask my AI Assistant` |
| AI panel sub | `online · claude-haiku-4-5` |
| Statusbar hire CTA | `● Available for hire` |
| Logo name (mono) | `ruslan.kanatbek` |
| Logo sub (mono) | `SDET · AI Automation` |
| Explorer header | `EXPLORER` |
| Explorer meta | `ruslankanat/portfolio` |
| Terminal prompt | `ruslan@portfolio ❯` |

---

## Content Substitutions (Alex Morgan → Ruslan Kanatbek)

| Original (files.js) | Phase 1 Value |
|---------------------|--------------|
| `"name": "Alex Morgan"` | `"name": "Ruslan Kanatbek"` |
| `"role": "Senior SDET & AI Automation Engineer"` | `"role": "Senior SDET / QA Automation Engineer"` |
| `"email": "alex@morgan.dev"` | `"email": "ruslankanat.b@gmail.com"` |
| `"github": "github.com/amorgan"` | `"github": "github.com/ruslankanat-sdet"` |
| `"linkedin": "in/amorgan-sdet"` | `"linkedin": "in/ruslankanat"` (verify real URL) |
| `"yearsOfExperience": 9` | `"yearsOfExperience": 9` |
| `alex.morgan` (logo mono text) | `ruslan.kanatbek` |
| `alex@portfolio` (terminal prompt) | `ruslan@portfolio` |
| `alex-morgan/portfolio` (explorer meta) | `ruslankanat/portfolio` |
| Lumen Systems / Northwind Robotics / Helix Health | Resmed / Gemini / Google / Citi (from real resume) |
| `SAMPLE_LOGS` test names | See terminal smoke test content above |

---

## Accessibility Requirements (Phase 1 baseline)

Full WCAG AA verification is Phase 5 scope. Phase 1 must not create barriers that block Phase 5 remediation.

| Requirement | Implementation |
|-------------|---------------|
| Icon-only buttons | `aria-label` on all `.act-btn`, `.icon-btn`, `.tab-close`, `.ai-fab`, `.ai-close`, `.ai-send` |
| Activity bar | Implement as `<nav aria-label="Activity bar">` |
| Terminal log region | `aria-live="polite"` on `.term-body` |
| AI panel | `aria-expanded` on FAB button |
| Test Automator form | `<label>` elements associated to textarea and mode toggle; `aria-describedby` for error messages |
| Tab close | `aria-label="Close {filename}"` |
| Color contrast | Dark theme token set achieves ≥4.5:1 on primary text (`--text` #e6edf3 on `--bg-elevated` #141c28) — do not introduce new color combinations without contrast check |
| Focus styles | Do not suppress `outline` — use `outline: 2px solid var(--green); outline-offset: 2px` on all interactive elements |

---

## Registry Safety

| Registry | Blocks Used | Safety Gate |
|----------|-------------|-------------|
| shadcn official | Button, Textarea, Dialog (if needed for error modals) | not required |
| No third-party registries | — | not applicable |

**Vetting gate:** No third-party shadcn registry blocks are declared for this phase. Gate not triggered.

---

## Pre-Populated From

| Source | Decisions Used |
|--------|---------------|
| `01-CONTEXT.md` (D-01 through D-17) | 17 locked decisions — all used |
| `styles.css` | All color tokens, spacing values, animation names, breakpoints, font stacks |
| `app.jsx` | State model, keyboard shortcuts, component decomposition |
| `workspace.jsx` | TopBar, Editor, Terminal, StatusStrip component specs |
| `sidebar.jsx` | Sidebar, activity bar, file tree specs |
| `ai-chat.jsx` | AI widget specs, quick prompts, system prompt content |
| `files.js` | File tree structure, content data model, SAMPLE_LOGS |
| `REQUIREMENTS.md` | FRAME-08/09/10/11 (loading/error/persistence states), AUTO-01 through AUTO-07 |
| `CLAUDE.md` | shadcn decision, Tailwind v4, Shiki for generated code |
| User memory | Ruslan Kanatbek real contact details, experience companies |

No questions were asked of the user — all design contract questions were answered by upstream artifacts.

---

## Checker Sign-Off

- [ ] Dimension 1 Copywriting: PASS
- [ ] Dimension 2 Visuals: PASS
- [ ] Dimension 3 Color: PASS
- [ ] Dimension 4 Typography: PASS (override declared — IDE chrome exception)
- [ ] Dimension 5 Spacing: PASS (override declared — IDE chrome exception)
- [ ] Dimension 6 Registry Safety: PASS

**Approval:** pending
