---
phase: 01-foundations-test-automator-live
plan: 02
type: execute
wave: 1
depends_on: [01]
files_modified:
  - .github/workflows/ci.yml
  - .planning/PROJECT.md
  - package.json
autonomous: true
requirements:
  - FOUND-02

must_haves:
  truths:
    - "On every push to main and every pull request, GitHub Actions runs lint + typecheck + build — green build is required before Vercel promotes"
    - "pnpm install uses --frozen-lockfile so any package.json/lockfile drift fails CI immediately"
    - "A test placeholder step exists that runs `pnpm vitest run` when vitest.config.ts is present (becomes live when Phase 3 ships tests)"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "CI pipeline running on every push to main + every PR: checkout, pnpm setup, install (frozen), lint, typecheck, build, test placeholder"
      contains: "pnpm lint"
    - path: "package.json"
      provides: "ci-check script alias that runs lint + typecheck + build locally — pre-push convenience"
      contains: "ci-check"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "package.json scripts (lint, typecheck, build)"
      via: "pnpm <script> invocations in workflow steps"
      pattern: "pnpm (lint|typecheck|build)"
---

<objective>
Wire the CI quality gate that keeps the codebase healthy on every push. Lint + typecheck + build must pass before Vercel promotes a commit to production.

Output: a `.github/workflows/ci.yml` that runs on push/PR and a `pnpm ci-check` script for pre-push confidence.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/PROJECT.md
@.planning/phases/01-foundations-test-automator-live/01-RESEARCH.md
@.planning/phases/01-foundations-test-automator-live/01-CONTEXT.md
@.planning/phases/01-foundations-test-automator-live/01-VALIDATION.md
@CLAUDE.md
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Developer commit → main branch | Any new code on `main` is auto-deployed to Vercel production — CI is the only automated gate between human keystroke and public URL |
| Production bundle (`.next/static/**`) → public web | Static JS chunks are served from CDN; any secret leaked here is irreversibly public |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-1-01 | Information Disclosure | `.next/static/**` (production bundle) | mitigate | CI step runs `pnpm build` then `grep -rE "sk-ant-[A-Za-z0-9_-]{10,}" .next/static/` — exit 1 if any match. This is the automated enforcement gate for FOUND-03 (CI grep). A second grep checks `src/` for any `NEXT_PUBLIC_.*KEY` pattern that would mark a secret as client-shipped. Both run on every PR + every push to main. |
| T-1-05 | Tampering | `package.json` dependencies | mitigate | CI step greps for forbidden dependencies (`"ai":` or `"@ai-sdk/"` from CLAUDE.md "What NOT to Use" + FOUND-08 decision). Exit 1 if reintroduced. Prevents drift from architectural decision via accidental `pnpm add`. |
</threat_model>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Author GitHub Actions CI workflow — lint + typecheck + build</name>
  <files>
    .github/workflows/ci.yml
    package.json
  </files>
  <read_first>
    package.json                                                                # Confirm pnpm scripts (lint, typecheck, build) exist from plan 01
  </read_first>
  <action>
1. Create `.github/workflows/ci.yml`. Workflow name: `CI`. Triggers: `push` to `main`, `pull_request` to `main`. Concurrency group: `ci-${{ github.ref }}` with `cancel-in-progress: true`.

2. Single job named `quality-gate` running on `ubuntu-latest`. Steps in this order:

   **(a) Checkout** — `actions/checkout@v4`.
   **(b) Setup pnpm** — `pnpm/action-setup@v4` with `version: 9`.
   **(c) Setup Node** — `actions/setup-node@v4` with `node-version: 20` and `cache: pnpm`.
   **(d) Install** — `pnpm install --frozen-lockfile`.
   **(e) Lint** — `pnpm lint`.
   **(f) Typecheck** — `pnpm typecheck`.
   **(g) Build** — `pnpm build`.
   **(h) Test placeholder** — run `pnpm vitest run` only if `vitest.config.ts` exists; otherwise print a skip message (Phase 3 wires real tests):
   ```yaml
   - name: Tests
     run: |
       if [ -f vitest.config.ts ]; then
         pnpm vitest run
       else
         echo "vitest.config.ts not present yet — Phase 3 wires this. Skipping."
       fi
   ```

3. Add `"ci-check": "pnpm lint && pnpm typecheck && pnpm build"` to `package.json` scripts.

4. Verify the YAML has no hard tabs: `node -e "require('fs').readFileSync('.github/workflows/ci.yml','utf8').split('\n').forEach((l,i)=>{if(l.match(/\t/))process.exit(1)})" && echo ok`
  </action>
  <verify>
    <automated>test -f .github/workflows/ci.yml && grep -c 'pnpm lint' .github/workflows/ci.yml && grep -c 'pnpm typecheck' .github/workflows/ci.yml && grep -c 'pnpm build' .github/workflows/ci.yml && grep -c 'pnpm/action-setup' .github/workflows/ci.yml && grep -c 'ci-check' package.json</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` exists and has no hard tabs
    - Triggers on `push` to main AND `pull_request`
    - Steps: checkout, pnpm setup, node setup (cache: pnpm), install --frozen-lockfile, lint, typecheck, build, test placeholder
    - `package.json` scripts contains `ci-check`
    - Concurrency group with `cancel-in-progress: true` is set
  </acceptance_criteria>
  <done>
    CI runs lint + typecheck + build on every push to main and every PR. Vercel deploys only after merge.
  </done>
</task>

</tasks>

<verification>
- Push a no-op commit to a feature branch; GitHub Actions CI turns green
- `pnpm ci-check` runs locally without error
</verification>

<success_criteria>
- FOUND-02: CI runs lint + typecheck + build on every push to main and every PR; green build required
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundations-test-automator-live/01-02-SUMMARY.md` recording: link to the first green CI run, screenshot or quoted output of the secret-leakage test fail, and the exact PROJECT.md row added for FOUND-08.
</output>
